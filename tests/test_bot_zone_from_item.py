"""Зона следует из пункта, когда её не называл человек (задача #218).

Живой прогон 07.09.2026: аудитор сказал про печь, а запись легла в холодный цех.
Разбор показал, что классификатор выбрал пункт ВЕРНО, а зону подставила система:
модель на вопрос без подсказки отвечает «места не знаю» (`UNKNOWN`), и на это
место подставлялась зона ПРОШЛОЙ записи.

Данные при этом знают правильный ответ. Пункт про печь методика держит только в
горячем цехе — таких пунктов 59 из 136 (замер того же дня). Значит там, где зону
не называл человек, её незачем угадывать: у пункта она одна.

Где угадывать нечем — у пункта зон несколько, — зоной становится ответ модели, и
никакой подстановки не происходит: памяти о прошлой записи с T264 не существует
вовсе (D118). Оговорки об источнике зоны стерегут тесты
`test_bot_zone_guess_on_model_path.py`.

**Слово человека сильнее — но не до невозможной пары.** Названную зону система
не подменяет молча. Если слово аудитора расходится со словарём карты кадров
(«холодный цех» про печь), расхождение НАЗЫВАЕТСЯ до фиксации и выбор делает
аудитор (T266): методика такой пары не даёт, и с T271 движок её не примет, то
есть молчаливая запись кончилась бы отказом уже после того, как аудитор ушёл с
точки.
"""

from __future__ import annotations

from pathlib import Path
from typing import Any

import pytest
from bot_harness import (
    AUDITOR_ID,
    CHAT_ID,
    callback_query,
    candidate,
    feed,
    make_bot,
    photo_message,
    stub_classify,
    suggestion,
)

from src.bot.app import build_dispatcher
from src.bot.config import BotSettings
from src.bot.texts import t
from src.domain import get_state, only_zone, start_inspection

# Пометка стоит у каждого асинхронного теста, а не на модуле целиком (T290):
# половина файла спрашивает методику синхронно, и общая `pytestmark` вешала
# `asyncio` и на них — три предупреждения на КАЖДОМ прогоне. Предупреждение,
# которое видно всегда, перестаёт читаться, и следующее настоящее уедет вместе с
# ним. Тот же приём в `test_bot_kind_code.py`.
SETTINGS = BotSettings(token="unused-in-tests", allowed_ids=frozenset({AUDITOR_ID}), mode="polling")


def начата() -> None:
    start_inspection(CHAT_ID, "Белград 2", "planned", "ru", date="2026-08-21", auditor="Гарро")


async def разобрать(
    dp: Any, bot: Any, monkeypatch: pytest.MonkeyPatch, *, код: str, зона: str, слова: str
) -> None:
    """Кадр с комментарием → ответ модели → аудитор подтверждает кандидата."""
    stub_classify(monkeypatch, suggestion(candidate(код, "D1", зона, "Нагар на поду печи")))
    await feed(dp, bot, photo_message("frame-1", caption=слова, message_id=601))
    await feed(dp, bot, callback_query("rec:pick:0"))


# --- то, что знает сама методика ----------------------------------------------


def test_единственная_зона_пункта_известна(domain_env: Path) -> None:
    """Печь методика держит в одной зоне — это и есть ответ, а не догадка."""
    assert only_zone("CLN05", chat_id=CHAT_ID) == "hot_kitchen"


def test_пункт_многих_зон_единственной_не_имеет(domain_env: Path) -> None:
    """Где зон несколько, выводить нечего — и выдумывать нельзя."""
    assert only_zone("CLN03", chat_id=CHAT_ID) is None


def test_незнакомый_пункт_зоны_не_даёт(domain_env: Path) -> None:
    """Отсутствие пункта — не повод падать в разговоре с аудитором."""
    assert only_zone("НЕТ-ТАКОГО", chat_id=CHAT_ID) is None


# --- поведение в разговоре -----------------------------------------------------


@pytest.mark.asyncio
async def test_печь_в_чужой_зоне_от_модели_спрашивается_а_не_пишется(
    domain_env: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    """Сам дефект живого прогона, запертый с той стороны, с которой он случился.

    Модель отвечает `CLN05` в холодном цехе — ровно то, что уехало в отчёт
    07.09.2026. Методика этот пункт держит в одной зоне, значит пара невозможна
    и с T271 движок её не примет. Спросить ДО фиксации лучше, чем отказать
    после: аудитор на точке видит кнопку, а не сообщение о неудаче, и кнопка
    здесь ровно одна — других зон у пункта нет.

    Подпись выбрана так, чтобы до модели вообще дошло дело: по «грязной печи»
    виден класс, и запись делает быстрый путь без всякой модели. По «печь,
    посмотри что тут» класс не виден — грязь это или поломка, — и материал
    уходит разбору, как этому тесту и нужно.
    """
    начата()
    bot, session = make_bot()
    dp = build_dispatcher(SETTINGS)

    await разобрать(
        dp, bot, monkeypatch, код="CLN05", зона="cold_kitchen", слова="печь, посмотри что тут"
    )

    до_ответа = get_state(CHAT_ID)
    assert до_ответа is not None and not до_ответа.findings, (
        "невозможная пара «пункт + зона» записана молча"
    )
    assert session.last_text == t("record.ask_zone_for_item", "ru", code="CLN05")
    # Все зоны, зона пункта — первой (D206): место находки выбирает человек.
    assert session.keyboard_data()[0] == "rec:zp:hot_kitchen", "зона пункта не стоит первой"

    await feed(dp, bot, callback_query("rec:zp:hot_kitchen"))
    состояние = get_state(CHAT_ID)
    assert состояние is not None and состояние.findings, "запись не появилась"
    assert состояние.findings[0].zone == "hot_kitchen"


@pytest.mark.asyncio
async def test_пункт_многих_зон_зоной_берёт_ответ_модели(
    domain_env: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    """Выводить зону не из чего — и выдумывать её неоткуда: памяти больше нет.

    У `CLN03` зон много, слова «грязно» не называют ни места, ни объекта карты.
    Зоной становится ответ модели, и ровно он — никакой подстановки прошлой
    записи на этом месте не происходит (T264).
    """
    начата()
    bot, _ = make_bot()
    dp = build_dispatcher(SETTINGS)

    await разобрать(dp, bot, monkeypatch, код="CLN03", зона="cold_kitchen", слова="грязно")

    состояние = get_state(CHAT_ID)
    assert состояние is not None and состояние.findings
    assert состояние.findings[0].zone == "cold_kitchen"


@pytest.mark.asyncio
async def test_расхождение_слова_со_словарём_называется_до_фиксации(
    domain_env: Path, monkeypatch: pytest.MonkeyPatch
) -> None:
    """Развилка, названная владельцем в решении D118: место и объект спорят.

    Правило 6 отдаёт зону аудитору, и подменять сказанное им система не вправе.
    Но словарь карты кадров держит печь в горячем цехе, а методика не даёт
    пункту про печь никакой другой зоны: молчаливая запись кончилась бы отказом
    движка уже после того, как аудитор ушёл с точки. Поэтому расхождение
    называется вслух, и выбор делает человек — зона объекта или другой пункт
    (T266). Третьей кнопки «оставить мою зону» нет: она предлагала бы отказ.
    """
    начата()
    bot, session = make_bot()
    dp = build_dispatcher(SETTINGS)

    await feed(
        dp,
        bot,
        photo_message("frame-1", caption="в холодном цеху грязная печь", message_id=601),
    )

    состояние = get_state(CHAT_ID)
    assert состояние is not None and not состояние.findings, (
        "невозможная пара записана молча — ни одна из двух сторон расхождения не названа"
    )
    assert "Печь" in session.last_text, "аудитору не сказано, ЧТО разошлось со словарём"
    assert session.keyboard_data() == ["rec:cz", "rec:ci"]
