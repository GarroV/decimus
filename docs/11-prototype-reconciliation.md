# Прототип Decimus 2 против заложенного: сверка

Хенд-мейд документ. Составлен 18.09.2026 по папке `Бриф системы аудитов
Decimus 2/` (эталон от владельца, D133) против `docs/08-design-brief.md`
(задание, из которого прототип вырос), решений D024–D135 и фактического
состава кода (`src/db/migrations`, `data/`).

Назначение — не потерять при переносе ни одного из трёх: функций прототипа,
которых не было в задании; требований задания, не доехавших до прототипа;
конфликтов, которые решает владелец.

## Что в папке

| Файл | Что это | Визуальный язык |
|---|---|---|
| `Dodo Audit Service.dc.html` | первый заход, холст-канвас: экраны + **список добавленного сверх задания** (15 строк) + пограничные состояния | Space Grotesk / Manrope, палитра `#5B57F2` |
| `Dodo Audit Prototype.dc.html` | второй заход, кликабельный | то же |
| `Dodo Audit Prototype v2.dc.html` | **актуальный**, 4170 строк, 9 разделов навигации | то же |
| `Dodo Design System.dc.html` + `dodo-ds.css` | каталог компонентов на ядре дизайн-системы | Golos Text / IBM Plex Mono — **ядро `forma`** |
| `uploads/08-design-brief.md` | копия задания, байт в байт совпадает с `docs/08-design-brief.md` | — |

Ядро в папке (`dodo-ds.css`) **разошлось** с каноном
`forma/dodo/core/dodo-ds.css` — сверять перед использованием, канон в
`GarroV/forma`, не в папке эталона.

## Разделы прототипа v2

`Обзор · Проверки · Планы · Предписания · Страна · Календарь · Методика ·
Проект · Мини-апп`

## 1. Функции прототипа, которых в задании не было

Помимо 15 строк, которые прототип перечислил сам (артборд «Добавлено сверх
задания» в `Dodo Audit Service.dc.html` — принимать построчно), в v2 появились
целые сущности. Они дороже: это данные и права, а не экран.

| # | Что добавлено | Чего требует от системы | Есть в коде |
|---|---|---|---|
| P01 | **Кластеры** — группировка точек, переключатель в шапке рядом с арендатором | ещё один уровень иерархии над точкой | нет |
| P02 | **География страна → город → точка** как управляемый справочник, партнёр назначается точке | таблицы стран/городов, связь точка↔партнёр | частично: `units`, `unit_aliases` |
| P03 | **Экран «Страна»** — страновой срез, выгрузки CSV / XLSX / PDF | агрегаты по стране + экспорт | нет |
| P04 | **Календарь проверок** и «Назначить проверку» | плановая проверка как сущность (до факта) | нет |
| P05 | **Типы проверок: Аудит УК / Командировка / Самопроверка** партнёра | тип влияет на то, кто проводит и что видит партнёр | нет (вид проверки есть, самопроверки нет) |
| P06 | **Несколько чек-листов с аудиторией** (Аудит УК, Экспресс-проверка, Самопроверка партнёра) | чек-лист — данные с идентификатором и аудиторией | заложено в roadmap, в коде — CSV |
| P07 | **Библиотека блоков** — переиспользуемый набор пунктов, «сохранить зону как блок», вставка копирует пункты с новыми кодами | отдельное хранилище блоков | нет |
| P08 | **Дерево меток для модели** (зона → объект → признак) с генерацией | замена/надстройка над синонимами `phrase_aliases` | частично |
| P09 | **Планы действий** как документ: основание, срок, готовность, автор, закрытие | таблица планов | нет |
| P10 | **Предписание как документ по итогам проверки** (описание ситуации, пункты с причинами, требования со сроками), а не строка «что исправить к сроку» | таблица предписаний + состав | нет |
| P11 | **Маршрут согласования и отправки** письма, редактируемый по шагам | маршрут + состояния согласования | нет |
| P12 | **Редактор шаблонов письма** с подстановками, два шаблона | шаблоны в данных, не в коде | частично |
| P13 | **Пользователи УК и партнёры кластера** — список, добавление, снятие | роли и доступ | `mcp_admins`, `mcp_tokens` — не то |
| P14 | **Перепроверка с прошлого раза** в мини-аппе: «Исправлено / Повторилось» | связь записи с записью прошлой проверки | поле связи есть, логики нет (D029) |
| P15 | «Неподтверждённое не влияет на оценку. **Потенциально ещё N %**» | расчёт потенциальной потери | нет |
| P16 | **«Подтвердить все предложения модели»** одной кнопкой | — | нет |
| P17 | **Системные нарушения**: один пункт — много точек, кандидаты на обучение и правку методики | агрегат по пункту через сеть | нет |
| P18 | **Сохранённые виды** фильтров реестра, буквы как фильтр | — | нет |
| P19 | **Самопроверки видны партнёру в его кабинете** | кабинет партнёра раньше, чем планировали | нет |

## 2. Что заложено, но в прототипе не доехало

| # | Требование | Источник | Состояние в v2 |
|---|---|---|---|
| L01 | **Аналитика по процессам** (7 процессов методики), итог по процессам в отчёте **и на сайте админки**; вклад процесса в потерю итога | D120, D121 — прямое требование владельца | слова «процесс» в прототипе **нет ни разу**; вместо этого «потери по блокам» |
| L02 | **Три языковых поля**: язык интерфейса, язык речи аудитора, язык отчёта — и в интерфейсе они не смешиваются | бриф, D-решения, CLAUDE.md | слова «Язык» в интерфейсе **нет**; в редакторе только RU/EN формулировки |
| L03 | **Повторная отправка уже выданного отчёта** — отдельное действие, не правка и не отзыв | D122 | не найдено |
| L04 | **Обязательное фото на пункте** — признак в чек-листе | бриф, экран 8 | в редакторе пункта нет; вместо него подсказки кадров (`shots`) |
| L05 | **Покрытие чек-листа** («проверено 46 из 120») как внутренняя отметка | roadmap | обход по зонам в мини-аппе есть, отметки покрытия в отчёте нет |
| L06 | **Общие виды зон** как запись с нулевым весом (обязательны для проверки партнёром) | D024 | не найдено, при том что самопроверка партнёра добавлена (P05) |
| L07 | **Критерии D1/D2/D3 как справочник** с объяснением, почему пункт получил класс | roadmap | поле «Критерии класса» в редакторе есть; подсказки аудитору в мини-аппе нет |

## 3. Конфликты — решает владелец

| # | Конфликт | Стороны |
|---|---|---|
| **Q1** | **Визуальный язык.** Прототип нарисован на Space Grotesk / Manrope и палитре `#5B57F2`; единая дизайн-система линейки (`GarroV/forma`, ядро + `decimus/domain.css` с классами, буквами, состояниями предписаний) — на Golos Text / IBM Plex Mono. Заведено как задача #273. | брать из прототипа **раскладку и поведение**, а цвет/шрифт/компоненты — из `forma` **/** менять ядро линейки под прототип |
| **Q2** | **Зона или блок.** В данных и методике — 10 зон с долями (`data/zones.csv`), в прототипе — 12 «блоков», и тем же словом назван переиспользуемый набор пунктов из библиотеки (P07). Одно слово на два понятия. | оставить «зона» для доли методики, «блок» только для библиотеки **/** переименовать зоны в блоки и придумать другое имя библиотеке |
| **Q3** | **Разрез аналитики.** D121: процент по процессам, вкладом в потерю итога, в отчёте и в админке. Прототип весь построен на разрезе по зонам/блокам. | добавить разрез по процессам рядом с зонами **/** заменить **/** отложить |
| **Q4** | **Календарь.** D134: «годовой календарь это отдельно», #279 снята со спринта. В прототипе календарь — полноценный раздел навигации, и от него зависит «Назначить проверку» (P04), на которую ссылаются экраны точки и обзора. | планирование проверок берём, годовой календарь отдельно **/** весь раздел за границу спринта |
| **Q5** | **Мультиарендность.** В коде арендатор = `tenants`, в прототипе шапка показывает арендатора **и** кластер, а админка называется «Проект». Три слова, неясно, сколько за ними уровней. | арендатор = проект, кластер внутри **/** кластер = арендатор **/** три уровня |
| **Q6** | **Самопроверка партнёра** (P05, P19) выводит кабинет партнёра из «разметить на будущее» в работающую функцию: партнёр проводит проверку сам и видит её у себя. Это меняет и права, и объём. | берём в объём **/** рисуем, но не строим |
| **Q7** | **«Подтвердить все предложения модели»** (P16) против жёсткого требования «ни одна запись не попадает в отчёт без подтверждения аудитора». Кнопка формально подтверждение, фактически — способ не смотреть. | оставить как есть **/** убрать **/** оставить с ограничением (только после просмотра каждой) |
| **Q8** | **Перепроверка «Исправлено / Повторилось»** (P14) против D029: логика повторов отложена на следующий год, неисправленное считается невыполнением плана действий. | прототип опередил решение — вернуть логику повторов **/** переформулировать экран в терминах плана действий |
| **Q9** | **Предписание против плана действий.** В прототипе это две разные сущности с двумя разделами навигации (P09, P10); в спеке — одно понятие «что и к какому сроку исправить». | две сущности **/** одна |
| **Q10** | **Где живёт папка эталона.** 3,9 МБ артефактов лежат в рабочем дереве `decimus/` вне git. `MAXIMUS` держит свою дизайн-систему внутри репозитория. | класть в репозиторий **/** держать в `workbench/` и ссылаться |

## Закрытые конфликты (18.09.2026)

| Вопрос | Решение | Запись |
|---|---|---|
| Q1 визуальный язык | раскладка из прототипа, стиль из `forma` | D136 |
| Q3 разрез аналитики | процессы **рядом** с зонами, шкала из D121 | D137 |
| Q6 самопроверка партнёра | рисуем, не строим; форма данных закладывается | D138 |
| Q4 календарь | планирование и календарь в объём не входят | D139 |

Дополнительно введено требование, которого не было ни в задании, ни в
прототипе: **непостроенное выглядит непостроенным** — лента «в разработке» в
навигации и явный экран-заглушка при входе (D138).

Открытыми остаются Q2, Q5, Q7, Q8, Q9, Q10.

## Что дальше

1. Владелец проходит по таблице 3 (Q1–Q10) и по 15 строкам артборда «Добавлено
   сверх задания» — принять/отклонить построчно.
2. Принятое раскладывается на задачи: сущности из таблицы 1 — это схема базы,
   а не экраны, и их цена в разы выше.
3. Таблица 2 — блокирующая: это уже принятые решения, которые прототип не
   отражает. Они возвращаются в макет, а не молча исчезают.
