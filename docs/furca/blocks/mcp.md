# Блок: mcp

## Назначение

Работа через Claude с накопленными проверками (история точки, повторяющиеся нарушения, сводка по сети) и с самой методикой — чек-листом, по которому эти проверки считаются (D049). Форма взята с работающего `swarm-mcp`: JSON-RPC по HTTP, личный токен, мост stdio↔HTTP (D055).

Содержимое проверки не пишет ни один инструмент. Мест, где блок перестаёт быть только чтением, два, оба включаются отдельной настройкой и по умолчанию выключены целиком: правка методики (D049) и снятие проверки из истории (D086/D089) — пометка на выданном документе, без единого знака правки внутри него.

## API-контракт

Тридцать три инструмента: шесть о проверках, один о снятии, два об исходнике эталона (открыты всякому токену), двадцать один о методике и карте кадров, три о карте синонимов (открыты тем же правом, что методика). **Аргумента `tenant` нет ни у одного:** код арендатора приходит из токена и ниоткуда больше. У инструментов методики нет и аргумента `store`: хранилище версий подставляет точка входа.

```
# проверки — только чтение. Инструмента, пишущего проверку или находку, нет и не будет:
# проверку заводит и завершает аудитор через бота, а в базу её кладёт слив (T093)
list_inspections(unit?, date_from?, date_to?, limit?)     unit_history(unit, limit?)
network_summary(date_from?, date_to?)                     get_inspection(id, lang?)
findings_by_unit(unit, limit?)                            inspection_letter(id, lang?)

# снятие проверки из истории (T211, D086/D089) — ровно один инструмент, право личное токену
retract_inspection(id, reason, confirm_unit, confirm_date)

# исходник эталона (T315, D132) — открыт ВСЯКОМУ токену: проверяемый обязан видеть, по
# какому эталону его проверяют. Записи в этих двух нет вовсе, обработчики в своём модуле
checklist_source(version?, process?)       checklist_source_item(code, version?)

# методика: чтение версий и правка (T098, D049)
checklist_versions()   checklist_items(version?, process?)   checklist_item(code, version?)
add_checklist_item(process, question_ru, levels, code?, process_en?, question_en?, zones?,
                   days?, criteria?, kind?, version_name?, note?)
edit_checklist_item(code, ...то же, кроме kind...)   # меняются только названные поля
remove_checklist_item(code, hard?, version_name?, note?)   restore_checklist_item(code, ...)
add_zone(code, name_ru, name_en?, share?, equal_shares?, version_name?, note?)
remove_zone(code, keep_shares?, equal_shares?, version_name?, note?)
set_zone_shares(shares, version_name?, note?)        # доли зон набором «код → процент»
rename_zone(code, name_ru?, name_en?, version_name?, note?)   # имена; код не меняется никогда
route(version?)                                      # порядок обхода, как его получит аудитор
set_route(zones?, items?, version_name?, note?)      # названная половина целиком, вторая цела
publish_checklist_version(version)                   # сделать версию той, что читает движок

# карта кадров (T144) и слова аудиторов
photo_cues(version?)   add_photo_cue(section, phrase, codes, ...)   edit_photo_cue(phrase, ...)
remove_photo_cue(phrase, ...)   set_photo_cue_zone(phrase, zone, ...)  # зона — свой вызов, D125
photo_cue_suggestions(date_from?, date_to?, min_confidence?, limit?, version?)
uncovered_phrases(limit?)          # слова, которые звучат, но записи не дают (T270)

# карта синонимов формулировок (T294, поверх слоя T292) — право методики, а не снятия
learned_phrases(lang?, item_code?, include_retracted?, limit?)
retract_learned_phrase(phrase, lang, reason)
repoint_learned_phrase(phrase, lang, item_code, reason)
```

**Форма ответа.** JSON внутри текстового блока MCP, ключи английские: это машинная поверхность, агент пересказывает её человеку сам. У каждой выдачи есть `status` словами — «ничего не найдено» отличимо от «не смогли прочитать», а `truncated` говорит, что чтение упёрлось в предел. `chat_id` наружу не отдаётся.

## Откуда берётся арендатор

**Из токена.** Связка `MCP_TOKENS=арендатор=токен,...` разбирается на старте; запрос приносит токен в `Authorization`, сервер переводит его в код арендатора и передаёт инструментам сам. Аргумент `tenant` в вызове получает явный отказ, а не отбрасывается тихо (T110). Один токен у двух арендаторов — отказ на старте; несколько токенов у одного — законно (по человеку на токен); токен короче 24 знаков — отказ; в `repr`, в текст отказа и в лог токен не попадает; сверка идёт `secrets.compare_digest`. Вторая дверь (T253, D098/D099) — личный токен, который выпускает бот и хранит отпечатком в базе; права на снятие проверки он не даёт.

## Правила, которые здесь дороже удобства

- **Правка не переписывает действующую методику:** каждый вызов кладёт НОВУЮ версию рядом, публикация — отдельный вызов (D049, D050). Отклонённая правка приходит отказом словами движка, а не «готово, но»; отказы самого хранилища (пустая правка, занятое имя) ложатся в журнал наравне с отказами движка (T238).
- **Правила методики живут в движке, и здесь их не пересказывают.** Исключение ровно одно и не исключение вовсе: файлы, которых движок не читает, — карта кадров (`photo-cues.md`) и маршрут (`route.csv`). Про них `manage.py validate` не знает, поэтому коды сверяет сам блок, а записанное перечитывает разбором ПРОДУКТА (`src.recognize.cues`, `src.domain.route`), а не своим: свой разборщик означал бы, что блок согласен сам с собой, а на точке видно другое.
- **Сущности связываются кодами.** Переименование зоны не трогает код; маршрут и карта кадров уходят в файл в написании справочника, а не в том, как код набрали в вызове.
- **Оценка не считается заново** — держится контрактом `lint-imports`: `src.mcp` не импортирует движок.
- **Абсолютных путей в ответе нет** ни в успехе, ни в отказе (T120): путь печатается в лог процесса, агенту достаётся имя переменной окружения и то, что с ней не так.
- **Новый вид инструмента объявляется в ПЯТИ местах** (проверено на T294, T315, T313, T317): `src/mcp/catalogue.py`; `src/mcp/rpc.py` — только если вид новый; `tests/test_mcp_catalogue.py` — счётчик и имена по видам; `tests/test_mcp_server.py` — перечень имён и требование, чтобы обработчик вида лежал в названном модуле; `tests/test_mcp_no_paths.py` — обе таблицы аргументов, где годные обязаны доводить до успешного ответа, а негодные до отказа. Пропущенное место видно только прогоном тестов MCP.

## Definition of Done блока

- [x] Инструменты чтения отвечают на боевых данных, ответы сверены с прямым запросом к базе.
- [x] Доступ закрыт токеном; без него сервер не отдаёт ничего, включая перечень инструментов.
- [x] «Пусто» и «не смогли прочитать» — разные ответы, сказанные словами.
- [x] Ни один инструмент не считает оценку заново (контракт `lint-imports` и сверка с `domain.score()`).
- [x] Правка методики идёт новой версией, проходит проверку движком и попадает в журнал (T098).
- [x] Снятие проверки закрыто личным правом токена, подтверждается точкой и датой, чужого текста наружу не выпускает (T211).
- [x] Издание проверки опознаётся отпечатком, а не именем (T236); нелатинское имя набора читается всеми поверхностями блока (T243); письмо не молчит о находке без формулировки (T237).
- [x] Карта кадров правится через агента, колонки узнаются по заголовку (T144, T289); карта синонимов видна управляющей компании и правится ею (T294).
- [x] Партнёр читает исходник эталона, и это право отдельно от правки — разными хранилищами, а не флагом (T315).
- [x] Зона не только заводится, но и настраивается: доли набором и переименование (T313).
- [x] Порядок обхода точки задаётся из разговора, коды сверяет блок, пометки человека переживают правку (T317).

## Открытые вопросы к владельцу

- **Партнёрские наборы методики.** Методика одна на всю сеть, правка не разделена по арендаторам. `docs/02-domain.md` обещает, что «у партнёров со временем появятся свои наборы»; тогда хранилищу понадобится измерение арендатора, а инструментам — понимание, чей набор они правят. Сейчас этого нет намеренно.
- **Право инструментов ставить строке карты кадров зону** — поднято блоком `binding`.
- **Q057: нужен ли партнёру `route.csv` и рабочие колонки УК в исходнике эталона.** Сегодня в исходник уходит эталон (пункты, зоны с долями, классы, сроки, критерии), маршрут обхода — нет. За владельца не решено, инструменты чтения партнёром не тронуты.

## Состояние

**Где стоим.** T317 закрыта: порядок обхода точки (`route.csv`) правится через MCP — `route` (чтение) и `set_route` (правка), оба вида `KIND_CHECKLIST`, то есть право управляющей компании (D132). В каталоге 33 инструмента. Схема данных и роли блоком не трогались, `src/web` не трогался вовсе.

**Как устроено.** `src/mcp/route.py`: сверка кодов со справочниками ТОЙ версии, перезапись файла по местам строк данных (пометки `#` остаются там, где лежали), сверка записанного разбором продукта. В `checklist_tools.py` — две тонкие обёртки поверх него, как у карты кадров. Хранилище и ход правки общие с остальной методикой (`checklist.apply_edit`: снимок → правка копии → проверка движком → новая версия). Арифметики и правил методики в блоке не прибавилось.

**Чем проверено.** Тесты до кода: `tests/test_mcp_route.py` (16). Порча в семь заходов — снятая сверка кодов, код в чужом написании, файл, пересобранный без строк человека, переписанная неназванная половина маршрута, снятый отказ на дубль, снятый отказ на пустой вызов, чтение файла вместо порядка обхода: шесть покраснели своими тестами сразу. Седьмой (дубль) НЕ покраснел — дубль ловил разбор продукта уже после записи, то есть проверка сторожила не то, что обещала; тест усилен до «отказ говорит про ВЫЗОВ, а не про сломанную методику» и покраснел. Восстановление копией файла, md5 сверен. Гейты `fmt`, `lint`, `types`, `bounds` зелёные; `make regress` — 97.5% A 5×D1 и 97.0% A 6×D1.

**Живой запуск** (127.0.0.1:18291, копия боевой методики, хранилище версий пустое): прочитан маршрут — 10 зон из 10 в порядке обхода, 0 пунктов из 136; опечатка `terasa` — отказ с перечнем существующих зон, версии не появилось; перестановка мойки в конец обхода — версия `imf-2026-09-18-fb750e578449`, `published: false`, все 9 строк-пометок человека на месте, порядок прочитан назад тем же инструментом; 136 пунктов и 10 зон целы, указатель действующей версии не сдвинут. Стенд погашен, порт 18291 свободен; база не поднималась — инструменты в неё не ходят.

**Что дальше.** Хвостов у T317 нет. Ближайшая дыра `docs/10-checklist-configuration.md`, до которой блок не дотягивается правом: ставки вычетов и пороги букв (`scoring.json`) — она за границей «настраивается то, ЧТО спрашивают, а не сколько стоит ответ», и без владельца её не трогать. Ближайшая дыра внутри права — смена вида существующего пункта: сегодня переключается только `violation ↔ off`.

**Чужое, чинить не наше.** Полный `make test`: 2627 прошло, 417 пропущено (нужен Postgres), 6 красных — все вне `src/mcp` и не от этой правки. Пять: сервис `web` в `docker-compose.yml` поднимается без своего профиля (`tests/test_db_stand.py`, `tests/test_infra_compose.py`: `['bot', 'mcp', 'web'] == ['bot', 'mcp']`). Шестой: `tests/test_methodology_leak.py` находит цитаты боевой методики в каталоге `Бриф системы аудитов Decimus 2/` (файлы владельца, лежат в рабочей копии и в `main`, в git не заведены). Плюс `make dead` красный на `src/web/app.py:226` — `_no_frames` (хук Flask `@app.after_request`, vulture его использования не видит), чинится строкой в `.vulture-ignore`.
