<!-- managed by forge: формат строк ниже не менять — файл парсят forge-build и forge-status -->
# Вопросы владельцу

<!-- id: Qnnn (Q001, Q002, ...). статус: open (ждёт ответа) | answered (ответ в колонке «ответ») | auto (решено автономно диспетчером; колонка «ответ» содержит ссылку вида «→ Dnnn» на decisions.md). "блокирует" — id задач из tasks.md через запятую или «—». -->
<!-- В колонке «вопрос» последней строкой ставится пометка «отправлен ГГГГ-ММ-ДД» — без неё «вопрос записан» и «вопрос задан» неразличимы, и следующая сессия либо задаёт его второй раз, либо ждёт ответа, которого владелец не обещал, потому что вопроса не видел. -->

| id | статус | блокирует | вопрос | ответ |
|---|---|---|---|---|
<!-- | Q001 | open | T004 | Пример вопроса владельцу? | — | -->
| Q001 | answered | T001 | Нужен отдельный бот через @BotFather: токен работающего бота другого продукта переиспользовать нельзя — Telegram отдаёт обновления одному получателю, второй молча выбивает первого. Значение в `.env` как `TELEGRAM_BOT_TOKEN`. (отправлен 2026-08-28) | Заведён отдельный бот, токен получен и положен в `.env`; смоук `getMe` пройден |
| Q002 | answered | T002 | Ключ LLM для разбора комментариев: Anthropic или OpenAI — какой доступен? (отправлен 2026-08-28) | OpenAI, тот же ключ, что в Swarm → D010. Значение владелец кладёт в `.env` как `OPENAI_API_KEY` |
| Q003 | answered | T002 | Нужен новый ключ OpenAI: ключ соседнего продукта лежит в секретах управляемой платформы и обратно не выдаётся ни через CLI, ни через API. Отдельный ключ к тому же отзывается независимо. Значение в `.env` как `OPENAI_API_KEY`. (отправлен 2026-08-28) | Заведён отдельный ключ, положен в `.env`; смоук `/v1/models` вернул 200, нужные модели доступны |
| Q004 | answered | — | Внешние аудиторские проверки: подрядная контора и проверки от девелоперов приходят на своём бланке, по своему чек-листу и со своей буквенной оценкой, а учитывать их надо наравне со своими. Как стыкуем: приводим их шкалу к нашей таблицей соответствия, храним как отдельный тип проверки без пересчёта или требуем перенос в наш чек-лист? Владелец: «как мы здесь состыкуемся, я не знаю, но это вопрос такой, как раз-таки хороший для нашего обсуждения». Вынесено на обсуждение с командой (записан 2026-09-01) | Закрыт как неприоритетный → D056 |
| Q005 | answered | — | Повтор x2: что считается тем же нарушением. С предметными находками ясно (грязная печь, незаправленный дозатор, свисающий потолок — то же место, тот же объект), но «отсутствие маркировки» или «просроченные продукты» повторяются на других позициях. Сверяем строго по паре «пункт + зона» или нужен признак объекта? Владелец: «отсутствие маркировки здесь, наверное, будет сложнее». Вынесено на обсуждение с командой (записан 2026-09-01) | Снят: повтор в MVP не считается вовсе. Команда 01.09: неисправленное с прошлой проверки учитывается не как повтор нарушения, а как факт невыполнения экшн-плана, само нарушение фиксируется заново; функционал повторов отложен на следующий год → D029 |
| Q006 | answered | — | Потолок потерь по зоне (`cap_zone_loss_at_share`): сейчас выключен, поэтому зона может унести больше своей доли. Проверено прогоном движка: 21 × D1 в зале дают 89,5 % и букву C (вычтено 10,5 п.п. при доле зоны 10 %), а те же 21 × D1 плюс один D3 в той же зоне дают 90,0 % — критическое нарушение поднимает процент, потому что зона с D3 стоит ровно свою долю и остальные находки в ней не вычитаются. Владелец: «в теории, скорее всего, да, но надо это всё обсудить». Задача в трекере — #54. Вынесено на обсуждение с командой (записан 2026-09-01) | Процедурно: методику весов и обнуления зон в MVP не трогаем, вопрос отложен до согласования общей методологии с внешними аудиторами → D030. Парадокс остаётся известным дефектом, #54 висит как дол| Идём как заложено в методике, правка ожидается в будущем → D083 |
| Q007 | answered | — | Качество приготовленного продукта: в чек-листе есть PRD13 «Соблюдаются стандарты приготовления блюд» и PRD14 «Соблюдаются правила нарезки и фасовки», оба только D1 (0,5 п.п.). Достаточно ли этого веса за криво приготовленный продукт и нужен ли отдельный пункт про внешний вид готового изделия. Вынесено на обсуждение с командой (записан 2026-09-01) | Закрыт как неприоритетный → D056 |
| Q008 | answered | — | Назначенные проверки (D024): кто назначает и с какой периодичностью, какие зоны требуют обязательного общего вида, что считать выполненной проверкой, если зона недоступна, и влияет ли результат назначенной проверки на рейтинг точки наравне с выездной. Вынесено на обсуждение с командой (записан 2026-09-01) | Закрыт как неприоритетный → D056 |
| Q009 | answered | — | Редактура готового отчёта: правим ли уже собранный отчёт, если аудитор вспомнил ошибку или всплыли новые факты, и как это видно партнёру — молча или версией с пометкой об изменении. Вынесено на обсуждение с командой (записан 2026-09-01) | Отчёт не пересчитывается и не меняется задним числом — ни при правке чек-листа, ни при возражениях партнёра → D033 |
| Q010 | answered | — | Форма отчёта: ссылка на живой документ или PDF-файл. Ссылка позволяет держать у всех актуальную версию и править, файл не поддаётся правке и не отзывается. Определяет, что считать отчётом. Вынесено на обсуждение с командой (записан 2026-09-01) | PDF плюс письмо в привычном формате; письмо формируется в Google Почте и отправляется человеком вручную после проверки. История писем и проверок хранится в системе → D033, D035 |
| Q011 | answered | — | Оспаривание: может ли партнёр оспорить нарушение и можно ли поправить отчёт после отправки письма; кто в управляющей компании принимает решение по спору — проверявший аудитор или спор уходит выше. Вынесено на обсуждение с командой (записан 2026-09-01) | Оценка нарушения остаётся в отчёте. Если аргументы партнёра признаны обоснованными — можно не выписывать предписание. Решение принимает проводивший проверку бизнес-девелопер → D034 |
| Q012 | answered | — | Провальная проверка и события внутри управляющей компании: подключаются ли другие отделы (сопровождение бизнеса) при оценке D, и какие события система обязана сообщать и кому — повтор, просрочка устранения, давно не проверявшаяся точка. Предписание в этот вопрос не входит: его делает отдельный отдел. Вынесено на обсуждение с командой (записан 2026-09-01) | Приоритет уведомлений: сроки предписаний, незагруженные исправления, свежие проверки с D3. Напоминания «давно не проверялись» не нужны. Полная система уведомлений — после запуска бота → D039 |
| Q013 | answered | — | Что показывает веб: какая статистика, какие разрезы и какой функционал. Гипотеза владельца — общий поток проверок, общий уровень, разбивка по странам, ключевые проблемы, топ проблем, лучшие и худшие точки. Нужно понять, чего не хватает и что лишнее. Вынесено на обсуждение с командой (записан 2026-09-01) | Веб после MVP, ориентир — MD Audit: аналитика, хранение данных, управление чек-листами, настройка пунктов, весов и буквенных оценок. Демо ориентировочно к середине ноября → D037 |
| Q014 | answered | — | Сравнимость во времени: методика правится, версии чек-листа замораживаются. Считаем ли динамику точки сквозь смену версий или сравниваем только проверки внутри одной версии. Вынесено на обсуждение с командой (записан 2026-09-01) | Не считаем: отчёт заморожен на момент проверки и задним числом не пересчитывается → D033 |
| Q015 | answered | — | История до сервиса: заливаем ли в систему проверки, сделанные вручную, чтобы аналитика заработала не с нуля, и в каком виде эти данные существуют сегодня. Вынесено на обсуждение с командой (записан 2026-09-01) | Исторические проверки за три года прогоняются через систему разработкой ради статистики; ручного внесения старых проверок не будет → D035 |
| Q016 | answered | T090 | Где живёт Postgres под слив проверок (D027): свой контейнер на домашнем сервере (порт 5442 подобран и свободен), ноутбук с уже установленным Postgres или управляемый сервис. Выбор площадки — за владельцем, не за разработчиком. Рекомендация: контейнер на домашнем сервере рядом с ботом — там же, где бот, ночные бэкапы на площадке уже идут (записан 2026-09-01) | Supabase: Postgres, Storage и Edge Functions одним стеком; бот остаётся контейнером на домашнем сервере → D053 |
| Q017 | answered | T095 | Как подключается MCP-сервер к базе проверок: локальный stdio на машине владельца (доступ только у того, кто за ноутбуком, наружу ничего не торчит) или удалённый HTTP с авторизацией (доступен из любой сессии, но это публичная поверхность к коммерчески чувствительным данным партнёров). Рекомендация: начать с stdio, удалённый — после того как появится, кому его давать (записан 2026-09-01) | По образцу swarm-mcp: Edge Function и личный токен → D055 |
| Q018 | answered | T094 | Фотографии в базе: `file_id` телеграма живёт только внутри бота и снаружи не открывается — если проверки складываются в базу ради истории и аналитики, кадры надо выгружать в своё хранилище при завершении проверки. Выгружаем (нужно место на диске площадки и решение, где хранилище) или в базу идут только записи без картинок (записан 2026-09-01) | Выгружаем в Supabase Storage на завершении проверки, за S3-совместимым интерфейсом → D054 |
| Q019 | answered | — | «Метод Паши», взятый на созвоне за основу логики MVP: это то же, что уже заложено в `engine/` и `data/scoring.json`, или другой расчёт? Протокол требует прогнать через него последнюю проведённую проверку и сверить результат. Нужно имя проверки и её материалы (записан 2026-09-01) | Это переданный скилл Claude Desktop (docs/_legacy-skill.md), на нём построен движок → D052 |
| Q020 | answered | — | Себестоимость обработки: на созвоне названы ~15 центов за фотографию и ~2 евро за проверку с 50 кадрами (D040), наш замер на боевом кадре дал $0,0146 за вызов и ~$0,58 на проверку из 31 кадра — расхождение больше порядка. Откуда цифра команды и какую закладывать в ценообразование (записан 2026-09-01) | Обманки нет: 50 кадров по $0,0146 это $0,73 против названных 2 евро — наша оценка втрое дешевле, расхождение на порядок было в цене за кадр, а не за проверку |
| Q021 | answered | T060 | Режим «по чек-листу» (D031): показывать все 136 пунктов по зонам подряд, только реперные точки (5–7 на зону) или пункты, помеченные критичными? От ответа зависит, сколько нажатий делает аудитор на обходе (записан 2026-09-01) | Снят D042: не полный обход и не реперные точки, а выбор зоны и список пунктов, которые в ней ещё можно посмотреть |
| Q022 | answered | — | Предписания: процесс не описан, до конца сентября его прорабатывают отдельно. Система должна уметь фиксировать предписание или принимать его в полуручном режиме — в каком виде и на каком этапе это встраивать (записан 2026-09-01) | Закрыт как неприоритетный → D056 |
| Q023 | answered | T092 | Откуда берётся справочник пиццерий (D032): выгрузка из Dodo IS, ручной список стран и точек управляющей компании или что-то третье? Нужен состав полей — идентификатор, название, страна, партнёр — и способ обновления, когда точка открывается или закрывается (записан 2026-09-01) | Снят: справочника в MVP нет, название вводится текстом → D051 |
| Q024 | answered | T090 | Слот в Supabase: в организации бесплатный план даёт два активных проекта, оба заняты (Swarm Brain и English_bot). Под базу проверок нужен третий. Варианты: поставить English_bot на паузу (слот освобождается, данные целы), перейти на Pro (~$25/мес), завести отдельную организацию. Рекомендация — пауза English_bot, если он не используется. Схема и миграции пишутся на локальном Postgres и от ответа не зависят (записан и отправлен владельцу 2026-09-02) | Снят D057: Supabase живёт в отдельной учётной записи, слот не проблема. Владелец: «я тебе уже 10 раз сказал что СУПАБЕЙЗ БУДЕТ НА ДРУГОМ АККАУНТЕ» |
| Q025 | answered | T090,T094,T095 | Доступ к Supabase на той отдельной учётной записи (D057). Схема, миграции и слив проверок работают на локальном Postgres и уже проверены; чтобы поднять то же самое в облаке, нужны ключи от той учётки — их у стройки нет и добыть их самостоятельно нельзя. Варианты: (а) положить `SUPABASE_URL` и сервисный ключ в `.env` и сказать об этом, (б) создать проект самому и прислать реквизиты, (в) отложить облако — MVP пройдёт на локальной базе, но MCP-сервер и выгрузка кадров тогда ждут. Рекомендация — (а), это разблокирует три задачи разом (записан и отправлен владельцу 2026-09-03) | Снят D061: код базы нейтрален (чистый Postgres, psycopg, DATABASE_URL), идём на локальном, перенос — смена одной переменной. Доступы к облаку сейчас не нужны |
| Q026 | answered | T073 | Раскатка на домашний сервер: команда стройки её не делает по построению — нужно явное «да» в разговоре и ночное окно 23:00–05:59. Всё, что проверяется без сервера (подъём одной командой, перезапуск и пересоздание контейнера без потери проверки), стройка закрывает локально задачей T105. Вопрос только про сам подъём на сервере: делаем ближайшей ночью, ждём готовности остального или пока не нужно (записан и отправлен владельцу 2026-09-03) | Катим на домашний сервер, ночное окно не ждём → D088 |
| Q027 | answered | T117 | Подтверждение нажатием для ТЕКСТА: спека и принцип 3 конституции требуют, чтобы ни одна запись не попадала в отчёт без подтверждения аудитора — обосновано замером на слепых фотографиях (одна верная фиксация из десяти). Владелец говорит, что кнопок для текста не обсуждали, только для картинки: «мы скинули нарушение, система у себя посчитала». Разница реальная: на фото система угадывает, а во фразе «горячий цех, пол грязный» аудитор сам назвал и зону, и суть. Вопрос: снимаем подтверждение для текста, где зона и нарушение названы словами, оставляя его для кадров? Проверено на его же примерах — быстрый путь на всех трёх НЕ срабатывает, а на «кассовая зона, просрочка чизкейк» слова поднимают единственный код CLN02 («оборудование в зоне мойки без загрязнений»), тогда как верный — PRD10 (просроченный продукт). То есть без подтверждения аудитор получил бы неверную запись молча (записан и отправлен владельцу 2026-09-03) | Снят D064: подтверждение с текста снимаем, для кадров остаётся. Владелец: «снимаем с текста подтверждение, потом добавим| Разрешено, плюс признак «фото обязательно» по пунктам списком от УК → D071. Пометка «без фотофиксации» в отчёте не решена — вынесена в Q029 |
| Q036 | answered | — | Нарушение без фотографии зафиксировать сегодня НЕЛЬЗЯ: голый текст бот отвергает («Не вижу кадра, к которому это относится»), даже когда фраза заведомо срабатывает в быстром пути — проверено прогоном на «горячий цех, печь в нагаре». Фиксация словами работает только как подпись к кадру. Часть нарушений не фотографируется по природе: документы и журналы, поведение персонала (людей снимать нельзя), отсутствие чего-либо, показание на дисплее. Разрешать ли запись без кадра — и если да, помечать ли такую запись в отчёте партнёру как «без фотофиксации»? Рекомендую разрешить с пометкой: отчёт остаётся честным, а аудитор перестаёт терять находки. (отправлен 2026-09-04) | Закрыт решением D078 (04.09.2026): фотофиксация нужна ВСЕГДА, записи без кадра не будет; бот обязан внятно сказать, что кадр нужен. Пометка «без фотофиксации» остаётся страховкой для записей, пришедших из истории |
| Q028 | answered | — | После каждой записи бот показывает текущий процент («#1 CLN05 · D1 · Горячий цех · 99.5%»). Аудитор видит, как оценка падает с каждой находкой. Это может влиять на его поведение — соблазн не записывать мелочь, чтобы не портить цифру, — и такое влияние незаметно ни в отчёте, ни в базе. Оставить как есть, показывать только в конце или сделать настройкой? Рекомендую показывать только в конце: во время обхода число ничего не даёт аудитору, а давление создаёт. (отправлен 2026-09-04) | | Показывать только в конце → D072 |
| Q029 | answered | T160 | Запись, сделанная БЕЗ фотографии, должна ли помечаться в отчёте партнёру как «без фотофиксации»? Партнёр читает отчёт как доказательный документ, и вычет без кадра отличается от вычета с кадром: возразить на него нечем, кроме слова аудитора. Рекомендую помечать — отчёт остаётся честным, а аудитор всё равно не теряет находку. (отправлен 2026-09-04) | | Пометку ставим. Владелец: «партнёр видит, на чём запись держится, и не ищет фото, которого нет» → D074 |
| Q030 | answered | — | Публичный репозиторий называет партнёра: город и названия точек встречаются в 77, 35 и 10 файлах — в документах, журнале изменений, движке и фикстурах тестов, по построению. Формулировки самих находок при этом НЕ утекли ни одной из семнадцати (проверено), то есть что именно нашли у партнёра — закрыто; открыто то, что проверки вообще были и у кого. Ваше решение о публичности касалось методики; идентификация партнёра — другая ось, поэтому спрашиваю отдельно. Вариантов два: оставить как есть или обезличить названия в репозитории (работа на несколько часов, тронет 60+ файлов). Рекомендую оставить: скрыть постфактум всё равно нельзя — история правок публична, — а новых упоминаний можно не добавлять. (отправлен 2026-09-04) | | Оставляем как есть: «проверки что я тебе передавал - они для примера, секрета тут нет все нормально» → D079 |
| Q031 | answered | T174 | Комментарий без кадра сейчас не удерживается: аудитор написал фразу, узнал что нужен кадр, и пишет её заново. Удержать текст до следующей фотографии удобнее, но тогда пара «комментарий десятиминутной давности + пришедший кадр» склеится молча, и запись уйдёт в отчёт партнёру с чужим описанием. Терпим повтор или принимаем риск склейки? Рекомендую терпеть: повтор раздражает, но виден, а склейка не видна никому. (отправлен 2026-09-04) | | Снят: решение D081 меняет привязку комментария целиком — правка идёт ответом на сообщение бота, и прежняя развилка не возникает |
| Q032 | answered | T145 | В одной из ваших эталонных проверок пункт записан в зоне, которую методика для него не допускает: пункт про чистоту оборудования стоит в зале, а список его зон зал не включает. Движок такую пару принял (он зоны пункта не сверяет), бот сегодня отклонит. Вопрос управляющей компании: верна запись или методика? Если запись — список зон пункта надо расширить; если методика — в эталоне ошибка, и её стоит поправить, потому что на нём стоит регрессионный якорь. Остальные пять «расхождений», которые я проверил, ложные: у тех пунктов допустима любая зона. (отправлен 2026-09-04) | | Запись верна: кассовая зона — спорная территория, ближе к производству. Эталон не трогаем → D085 |
| Q033 | answered | T180 | Вид проверки в самом отчёте не печатается вовсе — он есть только в письме, хотя место под него в шаблоне заведено и пустует. Оживить подпись (тогда вид появится и в отчётах, пересобранных по старым проверкам) или убрать заготовку? Рекомендую оживить, если партнёру важно видеть в документе, плановая проверка или повторная. (отправлен 2026-09-04) | | Печатать и в отчёте → D084 |
| Q034 | answered | T118 | Таблица слов готова и измерена: 275 строк против нынешних 75, бот сам угадывает пункт в 7 записях из 17 вместо 1, неверных срабатываний ноль. Вы правите её сами и просили не применять, пока правите. Скажите, когда закончите, — применение занимает минуту, и после него бот начнёт узнавать пункт по словам вчетверо чаще. Сессия, которая таблицу вела, закрылась; черновик цел и лежит вне репозитория. (отправлен 2026-09-05) | ВАЖНО, добавлено 05.09: применять таблицу нужно ПОСЛЕ починки T195 — быстрый путь сейчас записывает нарушение по комментарию, который его отрицает («в одной таре не смешаны» даёт тот же пункт, что «смешаны»), и запись идёт без подтверждения. Сегодня он срабатывает раз из семнадцати, после таблицы будет семь — вчетверо больше шансов на такую запись в отчёте партнёр| Владелец таблицу не правит, применять можно → D082 |
| Q035 | answered | T202 | Кадр с комментарием сейчас уходит в модель сразу, без вашего нажатия — платится за разбор с картинкой. Требование спеки написано строго: «без моего нажатия ни один кадр не уходит в модель». Без картинки обходится только случай, когда слова однозначно подняли один пункт — это 1 раз из 17 на боевых записях. Как правильно: (1) спрашивать нажатие и на кадр с комментарием — плюс одно действие на каждый кадр на обходе; (2) считать комментарий согласием на разбор — тогда поправить формулировку в спеке, чтобы она не обещала того, чего продукт не делает. Рекомендую 2, если вас устраивает нынешний расход: комментарий и правда означает «разбери это». (отправлен 2026-09-05) | | Перестроено по потокам: кадр с комментарием разбирается по комментарию, кадр без — по кадру → D081 |
| Q037 | answered | T210 | Снятие сданной проверки (D086): снимать пометкой или удалять строку истории насовсем? Рекомендую пометкой — история проверок точки это документ, и бесследно исчезнувшая строка в нём хуже видимой пометки «снята»: по пометке видно, что проверка была и почему её больше нет, а по пустому месту не видно ничего. Пометка к тому же обратима, удаление нет. Кадры снятой проверки при этом убираются из хранилища в обоих вариантах — они занимают место и содержат съёмку точки | Пометкой: проверка остаётся в истории, причина снятия обязательна, снятые видны базово только администраторам → D089 |
| Q038 | answered | T205 | Голосовое, присланное ДО всякого кадра, сегодня получает отказ: одним комментарием запись не станет, фотофиксация нужна всегда (D078). Значит дословный случай D081 — «наговорил, бот разобрал, ответом на своё же голосовое прислал фото» — работает только начиная с момента, когда запись уже есть. Сделать иначе можно: придержать расслышанные слова и собрать запись в момент прихода кадра. Это новое поведение, D078 его прямо не разрешает и не запрещает. Придерживать слова до кадра или оставить как есть? | Придерживать: бот отвечает «жду фото», собирает запись по приходу кадра, даёт отбивку о сохранении; та же пара для всех добавлений, ответ на отбивку правит запись → D090 |
| Q039 | answered | — | Пачка кадров: альбом до десяти штук даёт до десяти вызовов модели подряд, а обновления обрабатываются последовательно — по замеру 5,3 с на кадр это до ~50 секунд, в течение которых бот не отвечает никому. Это следствие вашего решения D081, а не дефект. Ставить ли предел на размер разбираемой пачки (например, шесть кадров, остальные — отдельной пачкой)? | Предел пять кадров; подача пользователю вынесена в Q040 → D091 |
| Q040 | answered | — | Предел пачки кадров назван — пять (D091). Открыто, как он выглядит для аудитора. Рекомендую принимать всю пачку и разбирать порциями по пять с немедленной отбивкой «принял N кадров, разбираю» — тогда ничего не теряется и бот не молчит; вариант «разобрал первые пять, остальные пришлите отдельной пачкой» проще, но перекладывает работу на аудитора прямо в точке. Отдельно: сам предел лечит симптом — бот молчит потому, что обновления обрабатываются последовательно, разбор в фоне снял бы молчание без предела вовсе | Исключение: разбираются пять, про остальные бот говорит словами — прислать отдельной пачкой → D093 |
| Q041 | answered | T224 | Замер качества разбора по комментарию: после решения D081 кадр с комментарием в модель не уходит, разбирается комментарий. Что померено бесплатно и детерминированно: эталонная пара «пункт + класс» лежит в перечне, который видит модель, 17 из 17, и перечень в обоих плечах один и тот же — потолок разбора правка не двигает. Непокрыт ровно один риск: станет ли выбор модели ВНУТРИ того же перечня хуже без картинки. Оснастка готова и проверена вхолостую (при подставном ключе честно говорит «замера не произошло» и возвращает единицу), прогон стоит 34 обращения к модели по вашему ключу и идёт меньше минуты. Запускать? (отправлен 2026-09-16) | Владелец: «проверяй» → замер разрешён, D113. Задача T224 разблокирована |
| Q042 | answered | — | Развилка между двумя вашими решениями, найденная блоком bot18. В D090 пример отбивки о сохранении показан с числом баллов («минус один балл»), а D072 запрещает показывать оценку по ходу обхода — только в конце. Вдобавок цена: балл за запись пришлось бы считать вызовом движка на КАЖДУЮ запись, а не один раз в конце. Блок сделал отбивку БЕЗ вычета и развилку не решал. Показывать балл в отбивке или оставить без него? | Без балла — «это бот, а не приложение», текущий прогресс показывать незачем. Владелец: «нам не надо текущий прогресс показывать, зачем? это ж не приложение, а бот» → D109 |
| Q043 | answered | T253 | Три развилки к личным токенам MCP (D098), каждая меняет работу. **1. Видна ли команда в меню.** Телеграм показывает меню всем, кто открыл бота, и спрятать пункт можно только переобъявляя меню каждому человеку отдельно — рассинхрон при этом видно. Дешевле и надёжнее команда БЕЗ пункта в меню: не видна никому, работает у того, кому положено. Рекомендую так. **2. Что делать с сегодняшними токенами из файла окружения** — оставить работающими до отзыва или объявить недействительными в момент перехода. Рекомендую оставить: иначе переход отрежет доступ в тот же миг, а он у вас единственный. **3. Может ли назначенный человек назначать дальше** — или это остаётся только у вас. Рекомендую только у вас: право раздавать доступ к истории проверок сети не то, что стоит делегировать по цепочке | Отвечено D099: команда ВИДНА в меню избранному кругу; админы добавляют админов; про судьбу прежних токенов владелец справедливо ответил, что это не его вопрос — внутренняя механика, вынесенная ему по ошибке |
| Q044 | answered | — | Публичный адрес тестовой площадки (он же имя вашей личной машины в сети) попал в `progress.md` и уехал в историю публичного репозитория коммитом от 15.09. Из рабочего дерева я его убрал, но история правок видна всем. Оценка риска: за адресом стоит дверь, требующая токен — раскрыта точка входа, а не доступ; в issues имени нет (проверено). Убирается это только двумя способами, и оба ваши: переписать историю с принудительным пушем (ломает клоны, включая копию на площадке) или перевести репозиторий в приватные (тогда минуты Actions станут платными — сейчас там крутится сторож доступности каждые 15 минут, это ~2900 минут в месяц, впритык к тарифу). Третий вариант — оставить как есть. Рекомендую оставить и больше адресов не писать: скрыть постфактум всё равно нельзя, а цена двух других вариантов выше раскрытого имени хоста за дверью с токеном. (отправлен 2026-09-16) | Владелец: «забить пока, все равно живых пользователей нет» → история публичного репозитория не переписывается и репозиторий не переводится в приватные, D114 |
| Q045 | answered | T234 | В отчёте партнёру печатается служебный префикс класса из методики: когда пункт выбран руками по кадру и комментария не было, формулировкой записи становится вопрос пункта целиком — вместе со служебной пометкой. На боевой методике так размечен 31 пункт из 136, то есть партнёр это увидит. С кнопки префикс уже снят, в отчёте — нет. Варианты: снять префикс и в отчёте (формулировка станет короче, но останется вопросом методики, а не словами аудитора); заменить такие записи нейтральной формулировкой пункта; оставить как есть. Рекомендую снять префикс: он внутренний, партнёру не значит ничего. (отправлен 2026-09-16) | Владелец: «не понимаю о чем речь, сделай как рекоммендуешь» → служебный префикс класса снимается и в отчёте, D115 |
| Q046 | answered | T223 | Ваша прямая речь стоит цитатами в комментариях кода: 53 места, 43 из них уже опубликованы. Приток я закрою в любом случае (вместо цитаты — ссылка на номер решения). Вопрос про уже написанное: вычищать старые цитаты или оставить. Чистка тронет 53 файла и из публичной истории всё равно не уберёт — там они останутся. Рекомендую оставить старое и закрыть приток: работа заметная, а эффекта на публичность уже нет. (отправлен 2026-09-16) | Владелец: «а что там за прямая речь? если убрать из публичной все равно не можем, то пофиг» → старые цитаты не чистим, приток закрываем, D116 |
| Q047 | answered | T203 | Чтобы снять прежний рецепт отпечатка (T203), нужно доказать, что строк того рецепта не осталось ни в одной базе — снятие необратимо, запечатанная проверка не правится. Доказательством считается полный ноль проверок (`select count(*) from inspections`, включая снятые) на трёх базах: моей, вашей и площадки. На моей — ноль. На площадке прогнать не смог: чтение боевой базы мне закрыто классификатором, и обходить его я не стал. Либо разрешите мне этот запрос разово, либо выполните сами одну строку: `ssh muspelheim "powershell -NoProfile -Command 'docker exec decimus-infra-db-1 psql -U dodo_audit -d dodo_audit_service -tAc \"select count(*) from inspections\"'"` — и скажите число. Если где-то не ноль, рецепт не снимаем и задача остаётся ждать. (отправлен 2026-09-16) | Владелец в канале не возражает против запроса, но фактически выполнить его не удалось: среда блокирует чтение боевой базы, и ответ из канала этого ограничения не снимает (D117). Число по-прежнему нужно — либо правилом в настройках, либо прогоном владельца |
| Q048 | answered | T272 | Замеры после снятия памяти о прошлой зоне УПАЛИ, и контракт блока ждал обратного. Точность разбора: было 94%, стало 87%. Доля быстрого пути: было 41% (7 из 17), стало 35% (6 из 17), неверных ноль и там и там. Прежние 94% при этом сняты НЕЧЕСТНО — замер подавал модели подсказку `zone_hint = правильная зона`, то есть уже известный ответ (тот самый дефект #100, который мы чинили в T125). Падение доли быстрого пути объясняется тем же: память о прошлой записи давала зону девяти записям из семнадцати, но давала УГАДЫВАНИЕМ — на живой пробе она увела печь в холодный цех. Потолок не сдвинулся: эталонный пункт лежит в перечне 31 раз из 31. И главное: колонка «Зона» в карте кадров заполнена в 1 строке из 275 — числа вырастут без единой правки кода, как только карта пополнится. Рекомендую принять новые числа как честную базу и записать их рядом с прежними, пометив прежние как снятые с подсказкой ответа. Альтернатива — считать это регрессией и возвращать память о прошлой зоне, то есть возвращать и её ошибки. (отправлен 2026-09-16) | Владелец не увидел здесь развилки (D118): путь именно такой, как заложен в T263-T267 — аудитор фиксирует конкретно, система сопоставляет с методикой. Новые числа приняты честной базой, прежние 94% помечены как снятые с подсказкой ответа; память о прошлой зоне остаётся снятой |

| Q049 | answered | T203 | Вы написали «команду прогнал», но не назвали число. Речь про Q047: `select count(*) from inspections` на базе площадки. Снятие прежнего рецепта отпечатка необратимо, поэтому нужен факт, а не предположение. Ноль — снимаю рецепт (T203); не ноль — задача остаётся ждать. (отправлен 2026-09-16) | Владелец: «не было никакого числа» — команда вывода не дала. Значит факта по-прежнему нет; уточнено в Q052 |
| Q050 | auto | T277 | Рабочий `.env` в основной копии указывает `AUDIT_DATA_DIR` на внешний каталог с неполной методикой — живой запуск движка на нём падает, а 2618 тестов молчат, потому что берут методику из фикстур. Пример окружения (`.env.example`) корректен. Править вслепую не стал: каталог лежит вне git и внешний путь может быть осознанным (боевая методика с данными партнёров) — тогда чинить надо содержимое каталога, а не путь. Скажите, что верно: (а) вернуть путь на `./data` в репозитории, (б) каталог внешний намеренно и я его не трогаю, вы дополняете методику там. (отправлен 2026-09-16) | — → D124. Владелец: «опять, уже второй раз. я не понимаю вопроса. Реши сам.» |
| Q051 | auto | T133 | Настоящий маршрут обхода от управляющей компании: `data/route.csv` сейчас просто повторяет порядок методики, а порядок пунктов внутри зоны УК не задавала. Механизм (T061) готов и проверен — не хватает только данных. Если маршрута от УК нет и не будет, скажите — тогда закрываю задачу как неактуальную, а не держу её в графе вечно. (отправлен 2026-09-16) ПЕРЕЗАДАН простыми словами 2026-09-17 (владелец ответил «не понимаю вопроса» / «задавай по-человечески») | Владелец: «фурка 123 займись решением» — решаю сам → D126 |
| Q052 | auto | T203 | Команда на площадке ничего не напечатала — скорее всего не совпало имя контейнера или базы, и ошибка ушла мимо вывода. Два пути на выбор: (а) пришлите вывод двух команд подряд — сначала `docker ps --format "{{.Names}}"`, затем ту же `psql`-строку с `2>&1` на конце, чтобы ошибка была видна; (б) разрешите мне разовое чтение боевой базы правилом в настройках — тогда посчитаю сам и больше не побеспокою. Без факта прежний рецепт отпечатка не снимаю: снятие необратимо. (отправлен 2026-09-16) ПЕРЕЗАДАН простыми словами 2026-09-17 (владелец ответил «не понимаю вопроса» / «задавай по-человечески») | Владелец: «фурка 123 займись решением» — решаю сам → D127 |
| Q053 | auto | T287 | Блок engine-fix починил письмо: если аудитор слов не написал, в письме теперь стоит «пункт стандарта «…»», а не выдуманная за него формулировка. Рядом нашлась та же дверь: когда пункт выбирается по фотографии без комментария, бот сам записывает в качестве слов аудитора формулировку вопроса методики — она попадает и в отчёт, и в письмо, и партнёр читает утверждение нормы («продукты размораживаются по стандарту») там, где зафиксировано нарушение. Как правильно: (а) так же, как в письме — не выдавать это за слова аудитора, печатать ссылкой на пункт; (б) оставить как есть, формулировка пункта считается достаточной записью. (в накопителе, не отправлен) ПЕРЕЗАДАН простыми словами 2026-09-17 (владелец ответил «не понимаю вопроса» / «задавай по-человечески») | Владелец: «фурка 123 займись решением» — решаю сам → D128 |
| Q054 | answered | T288 | Проверка на утечку методики смотрит только код и тесты — документы она не проверяет. В документах проекта дословных строк методики набралось 25 в 10 файлах, и 22 из них уже в origin, а репозиторий публичный. Затирание текста раскрытия не снимает: история правок на GitHub видна всем. Варианты: (а) перевести репозиторий в приватные — дешевле всего, но у публичных репозиториев минуты Actions бесплатны, а у приватных считаются по тарифу, и CI проекта тяжёлый; (б) переписать историю (`git filter-repo`) — снимает раскрытие, но ломает все существующие клоны и ссылки на коммиты; (в) считать приемлемым: опубликованы формулировки пунктов чек-листа, критериев и внутренних стандартов среди них нет. Рекомендация: (а), если тяжесть CI позволит, иначе (в) с явной записью решения. Отправлен 2026-09-16 |  Владелец дословно: «Плевать , нет тайны. Плюс ещё меняться будет», на повторный вопрос — «похую». Вариант (в) → D123 |
| Q055 | answered | T293 | Ставить зону строке карты кадров инструменты MCP по-прежнему не умеют: `edit_photo_cue` правит коды и колонку «Откуда», а колонку «Зона» бережёт и показывает отдельным полем ответа — то есть заполнять её управляющая компания сможет только руками в файле. Вопрос поднял блок binding в T262 и оставил владельцу, блок mcp в T289 его не решал (T289 была про отказ на всех строках, а не про новое право). Варианты: (а) научить `edit_photo_cue` ставить зону — заполнять станет можно из разговора, но появится право менять то, что сегодня меняется только осознанно; (б) оставить как есть и заполнять карту руками; (в) отдельный инструмент `set_photo_cue_zone`, чтобы право было названо явно и отдельно от правки кодов. Рекомендация: (в) — заполнение зон предстоит массовое, а смешивать его с правкой кодов в одном вызове значит потерять границу. Отправлен 2026-09-16 |  Владелец дословно: «нужно дать возможность гибко настраивать чеклист. как ты сам то будешь выделять зоны?» — форму выбора оставил мне, взята рекомендация (в) → D125. Требование про гибкую настройку чек-листа заведено задачей T302, встречный вопрос про зоны отвечен в канале |
| Q056 | answered | T302 | **Отдавать ли управляющей компании правку `data/scoring.json` — ставок вычетов, порогов букв, потолков сроков по классам?** Это единственное место, где «гибкая настройка чек-листа» упирается в цифру, которую партнёр читает как факт. Варианты: (а) не отдавать — состав проверки настраивает УК, цена ответа остаётся у владельца методики; (б) отдать ЧТЕНИЕМ — УК видит, по каким ставкам считают её версию, но не меняет; (в) отдать правкой наравне с пунктами. Рекомендация блока и моя: **(б) сейчас, (а) как граница для правки**. Довод: правка ставки не портит ни одной уже посчитанной проверки (снимок издания, T169), но делает несравнимыми проверки до и после — а сводка по сети молча смешает две цены. (в) становится разумным, когда появится видимая пометка о несравнимости, и это отдельная задача. Разбор целиком — `docs/10-checklist-configuration.md`. Отправлен 2026-09-17 | Отвечен 2026-09-18, решение D132: правка остаётся у УК (она автор эталона, а не арендатор), партнёру — чтение исходника |
| Q057 | answered | T315 | **Что ещё партнёр видит в исходнике эталона, кроме состава проверки?** По D132 партнёру отдано чтение эталона: пункты, зоны с долями, классы нарушений, сроки, критерии. Блок сознательно НЕ включил в исходник две вещи и просит подтвердить границу: (а) `route.csv` — порядок обхода пиццерии аудитором; это рабочий маршрут проверяющего, а не то, по чему считается оценка, но партнёр по нему видит, в каком порядке к нему придут; (б) собственные колонки УК в `checklist.csv` (T109/T289) — служебные поля управляющей компании, заводятся свободно и партнёру не предназначались. Рекомендация: оставить как сделано (ни то, ни другое), потому что исходник — это основание оценки, а не рабочая кухня проверяющего; маршрут можно отдать отдельным правом, если партнёру он нужен. Заведён 2026-09-18 | Хватит того, что есть: партнёр видит основание оценки и ничего сверх — маршрут обхода, внутренние подсказки и кухня проверяющего остаются внутри (ответ владельца 2026-09-18) |
| Q058 | answered | — | **Методика управляющей компании лежит в публичном репозитории: что делаем?** Коммит `c4cbce2` (папка эталона «Бриф системы аудитов Decimus 2», 19 файлов) запушен 18.09 в 10:44 и доступен на GitHub; прототип v2 цитирует 20 формулировок методики дословно. Проверка `test_methodology_leak` красная, репозиторий PUBLIC (проверено `gh repo view`). Удаление файла новым коммитом раскрытие НЕ убирает — только перевод в приватные или переписывание истории с force-push. Варианты: (а) перевести репозиторий в приватные — полно и быстро, но минуты Actions станут платными из квоты при прогоне в 6 минут; (б) переписать историю и force-push — публичность сохраняется, снятые клоны останутся с методикой; (в) считать, что методика УК раскрытием не является, и тогда завести проверке исключение на папку эталона, иначе она красит каждый прогон. Рекомендация: (а), потому что решение D146 о публичности принималось до того, как в репозиторий легла методика. (отправлен 2026-09-18) | Методика секретом не является → D150; проверке заводится исключение на папку эталона |
| Q059 | answered | T324 | **Где веб-админка живёт постоянно и публикуем ли её наружу сейчас?** По D154 админку открывают другие люди снаружи. Ноутбук для этого не годится: сон по крышке гасит её без предупреждения. Остаётся MUSPELHEIM — но перенос туда это раскатка на площадку, то есть отдельное «да» и ночное окно (23:00-06:59). Funnel там уже занимает все три допустимых порта, поэтому веб выходит путём на нашем 10000, а не своим портом. Наружу — только после T325 (ограничение перебора), он сейчас в работе. (отправлен 2026-09-18) | MUSPELHEIM, раскатку разрешил («ебаш на муспельхейм») → D156, порядок раскатки → D157 |
| Q060 | open | — | **Логин почты: под какой учёткой Google заводим приложение и нужен ли доступ к ящику?** Это ДВА разных доступа, и цена у них разная. (а) **Вход в админку по Google** (D163): скоупы `openid email profile`, проверки Google не требуют, круг допущенных — список почт; работает на нынешнем адресе (`muspelheim.tail48dfee.ts.net`), делается сразу. (б) **Черновик письма в ящике аудитора** (`gmail.compose`): скоуп чувствительный — приложение заводится в Google Cloud, и на корпоративном домене `dodoteam.io` это требует согласия администратора организации; по документации Google чувствительный скоуп может требовать проверки приложения даже у внутреннего, и там же требуется ПОДТВЕРЖДЁННОЕ ВЛАДЕНИЕ доменом приложения — `ts.net` принадлежит Tailscale и подтвердить его нельзя, то есть (б) упирается ещё и в свой домен, которого по D163 пока не планируется. Вопрос: (1) под какой учёткой заводим приложение — рабочей `dodoteam.io` (нужен доступ к Google Cloud организации и, вероятно, согласование IT) или отдельной; (2) берём ли (б) вообще сейчас, или пока хватит выгрузки письма файлом и копирования текста. Рекомендация: делать (а) сейчас, (б) отложить до своего домена — она не отменяется, а дешевеет вместе с ним. (отправлен 2026-09-21) | |
