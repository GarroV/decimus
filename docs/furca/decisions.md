<!-- managed by forge: формат строк ниже не менять — файл парсят forge-build и forge-status -->
# Решения

<!-- id: Dnnn (D001, D002, ...). кто: owner (решил владелец) | auto (решено автономно диспетчером). -->

| id | дата | решение | почему | кто |
|---|---|---|---|---|
<!-- | D001 | 2026-07-29 | Пример решения | Пример обоснования | owner | -->
| D001 | 2026-08-28 | Репозиторий на GitHub — публичный, под личным аккаунтом GarroV | Прямое решение владельца: «Я сказал паблик. чувствительную инфу вынеси за скобки или оставь здесь, это всё равно референс» | owner |
| D002 | 2026-08-28 | Чувствительное не уходит в git: `data/` (методика УК, 136 пунктов чек-листа, критерии D1/D2/D3) и `examples/` (31 фото реальных пиццерий, два PDF-отчёта партнёров) остаются локально и внесены в `.gitignore` | Репозиторий публичный, а это данные партнёров и внутренняя методика. Файлы нужны на месте как эталон регресса и референс, поэтому не удаляются, а исключаются из индекса до первого коммита | auto |
| D003 | 2026-08-28 | Целевая площадка — домашний сервер (домашний Windows-сервер, Docker) | Решение владельца вопреки рекомендации использовать Dodo AI Platform. Для бота на long polling публичный адрес не нужен, Postgres и бэкапы на площадке уже есть | owner |
| D004 | 2026-08-28 | Переезд с домашнего сервера закладывается архитектурно с первого дня: docker compose, все настройки через переменные окружения, Postgres по `DATABASE_URL`, фотографии через S3-совместимый интерфейс, переключение long polling → webhook переменной | Владелец: «нам всё равно будет надежнее перенести потом с домашнего сервера на что-то другое». Публичный адрес понадобится примерно через месяц, когда появится веб для партнёров; ноутбук с батареей вместо UPS и туннель без SLA прод для партнёров не держат | owner |
| D005 | 2026-08-28 | MVP — это запуск телеграм-бота. Веб, аналитика, админка чек-листов и мультиарендность идут следующими этапами, но форма данных под них закладывается сразу | Владелец: «МВП ЭТО ЗАПУСК БОТА, а дальше уже пойдет разработка остальной части проекта». Совпадает с `docs/07-roadmap.md`: форма данных сейчас, функции потом | owner |
| D006 | 2026-08-28 | Соседний проект на управляемом Postgres не сносим и не переиспользуем под этот продукт | Снос не даёт ничего: у той учётной записи свой лимит бесплатных проектов, а приостановленные проекты в него не считаются. Новый проект при необходимости заводится рядом, данные соседнего остаются целы | auto |
| D007 | 2026-08-28 | Состояние проверки — файл `inspection.json` в папке на чат, как в `docs/06-mvp-bot.md`. Базы в MVP нет | Решение владельца вопреки рекомендации класть данные сразу в Postgres. Следствия, которые становятся обязательными: запись состояния делается атомарно (временный файл + переименование + блокировка), иначе параллельная обработка альбома бьёт JSON; доступ к состоянию идёт через один слой, чтобы переезд на базу был заменой слоя, а не переписыванием бота | owner |
| D008 | 2026-08-28 | Голосовые комментарии входят в первую версию: транскрипция через OpenAI, `ffmpeg` в образе для перекодировки OGG/Opus | Владелец выбрал включить. На точке руки заняты — это главная боль пользователя, цена $0.003 за минуту | owner |
| D009 | 2026-08-28 | Рендерер PDF — WeasyPrint 69.x, зафиксирован в образе вместе с кириллическим шрифтом. Вид отчёта приводится к эталону правкой CSS и сверяется глазами на боевых данных | wkhtmltopdf, которым собраны эталоны, архивирован с января 2023. Headless Chrome отвергнут: образ больше гигабайта, лишняя точка отказа, нет нумерации страниц | owner |
| D010 | 2026-08-28 | Провайдер LLM — OpenAI, тот же ключ, что используется в Swarm. Вызов изолируется за одной функцией, чтобы смена провайдера была правкой конфига | Владелец: «опен аи, который завязан в сворме. нам хватит». Один ключ закрывает и разбор комментариев, и транскрипцию голоса | owner |
| D011 | 2026-08-28 | Имя проекта — `dodo_audit_service`, через нижнее подчёркивание, одинаково у репозитория и папки на машине. Репозиторий остаётся публичным | Решение владельца: продукт делается для компании, и компания знает, что репозиторий публичный. Возражение о том, что имя бренда в личном публичном репозитории читается как утечка, снято этим согласованием | owner |
| D012 | 2026-08-28 | Для бота заводится отдельный Telegram-бот и отдельный ключ OpenAI, а не переиспользуются учётные данные соседнего продукта | Telegram отдаёт обновления только одному получателю: второй процесс с тем же токеном выбивает первого, и выбитый молча перестаёт получать сообщения — соседний продукт с живыми пользователями встал бы незаметно. Ключ управляемая платформа обратно не выдаёт, а отдельный ключ ещё и отзывается независимо и показывает расход именно этого проекта | auto |
| D013 | 2026-08-28 | Модель разбора по умолчанию — флагман линейки (`gpt-5.6-sol`), а не дешёвая `gpt-4o-mini`, как было в первом варианте плана. Имя модели — параметр конфига | Ключ открывает всю текущую линейку. Разница в стоимости — 93 цента на проверку, которая занимает у человека четыре часа, при 100 проверках в месяц это $96 против $3. Цена ошибки несопоставима: слепой прогон дал одну верную фиксацию из десяти, а неверная буква уже уходила партнёру. Выбор подтверждается замером на боевых данных, а не остаётся мнением | auto |
| D014 | 2026-08-28 | Правила RUF001–RUF003 (кириллические буквы, похожие на латинские) выключены в конфиге линта | Проект пишет тексты, имена тестов и комментарии по-русски: правило давало 65 сообщений на первых же файлах при нуле настоящих находок. Гомоглифы — угроза для англоязычного кода, здесь кириллица осмысленная. Выключено целиком, а не списком букв, который заведомо неполон | auto |
| D015 | 2026-09-01 | Повторное нарушение считается с удвоенным вычетом: тот же пункт в той же зоне, что был в прошлую проверку точки и не устранён, снимает вдвое больше (D1 −1 п.п., D2 −4 п.п.; D3 и так обнуляет зону). Ставка удвоения живёт в `scoring.json`, в коде не дублируется | Владелец: «если у нас уже было нарушение в прошлый раз и оно повторяется в этот раз, условно говоря, не заправлено душитель либо там грязная печь, ещё что-то такое. Соответственно, как бы, мы это всё делаем x два». Требует связи повторной проверки с исходной; открытыми остаются строгость совпадения (пункт+зона или шире) и срок давности повтора | owner |
| D016 | 2026-09-01 | Справочник пиццерий обязателен, вместе с картой синонимов: система распознаёт сокращения и опечатки («БГ2» → «Белград 2») и связывает проверку с точкой по постоянному идентификатору, а не по введённой строке | Владелец: «Нет справочника, видимо, придётся его создать. Либо создать какую-то карту синонимов... справочник плюс список синонимов это как мастхэв». Сегодня реестр живёт в головах аудиторов; без идентификатора точки не связать повторную проверку с исходной, а значит не посчитать удвоенный вычет (D015) и не построить историю по точке | owner |
| D017 | 2026-09-01 | Данные принадлежат владельцу пространства: всё, что собрано в пространстве управляющей компании, принадлежит ей; партнёр, ведущий свою деятельность в своём кабинете, владеет своими отчётами и распоряжается ими сам | Владелец: «если у нас пространство идёт управляющей компанией, значит и вся информация, которая внутри принадлежит управляющей компании. Если партнёр покупает для себя право вести в своём кабинете свою деятельность, все внутренние отчёты... это его собственность, он может с ними делать что угодно». Снимает вопрос о выгрузке и разделении прав: граница проходит по пространству | owner |
| D018 | 2026-09-01 | Чек-лист управляющей компании — канон. Партнёр берёт его за основу и заводит собственные производные наборы (для менеджеров, управляющих, внутренних проверок), но канон остаётся общей базой | Владелец: «есть канон, который мы к управляющей компании используем, и партнёр волен брать эти каноны за основу и на своём для себя делать какие-то дополнительные чек-листы». Даёт общее ядро пунктов, по которому сравнение точек между собой остаётся корректным | owner |
| D019 | 2026-09-01 | Бланк внешней аудиторской конторы строится на основе нашего стандарта, поэтому её чек-лист можно забрать к себе. В системе закладывается функционал загрузки чек-листов извне — не в первой версии, но форма данных под импорт чужого набора предусматривается | Владелец: «бланк этот будет составляться в том числе на основании нашей работы и адаптированный под наши внутренние требования стандарты... нам нужен некий функционал подгрузки чек-листов. Это, конечно, проблема в будущем, но тем не менее важно это предусмотреть». Снимает часть Q004: шкалы сходятся, потому что чужой бланк производен от нашего | owner |
| D020 | 2026-09-01 | Пункты, по которым нарушение не зафиксировано, считаются выполненными. Принято как есть, покрытие в оценку не вводится | Владелец: «Да, факт, так и есть». Совпадает с исходным скиллом (`docs/_legacy-skill.md`) и с плоской моделью расчёта | owner |
| D021 | 2026-09-01 | Список из десяти зон полон: туалет входит в зал и связанное с ним, зона выдачи и доставки относится к кухонным зонам. Новые зоны не заводятся, доли остаются по 10 % | Владелец: «туалет относится к залу и всему, что с ним связано, зону выдачи, доставка и всё относится к кухне. То есть тут по идее нет противоречия никакого» | owner |
| D022 | 2026-09-01 | Замеры температур, настройки печи и фотографии продукта остаются записями уведомительного характера: попадают в отчёт и хранятся для истории, на оценку не влияют. Косячный продукт при этом фиксируется нарушением по пунктам чек-листа, а не информационной записью | Владелец: «это чисто уведомительный характер для нас на будущее... но при этом, если продукт косячный, у нас же есть в чек-листе проверка в том числе и качество продукта». Подтверждает механику D0 из методики | owner |
| D023 | 2026-09-01 | Устранение нарушений живёт в системе: партнёр заходит в прошедшую проверку и по каждому пункту прикладывает доказательства — фотографию, комментарий, документы. Отдельная поверхность в виде листа доработок с чек-листом по каждому нарушению | Владелец: «партнёр заходит в эту проверку, которая прошла, и туда, как устранение нарушения, подгружает конкретную информацию... некий бланк со списком доработок, которые необходимо закрыть, некий чек-лист» | owner |
| D024 | 2026-09-01 | Управляющая компания может назначить проверку партнёру через сервис, с обязательным приложением фотографий общего вида зон. Самопроверки, которые партнёр проводит в своём кабинете для себя, управляющей компании не интересны и в её рейтинг не идут | Владелец: «по поводу самопроверок — то, что партнёр делает в своём личном кабинете, вообще плевать. Но функционал того, что мы можем назначить проверку через наш сервис, при этом с обязательным приложением фотографий общего вида зон, надо заложить». Возвращает в работу отложенную идею общих видов зон из `docs/07-roadmap.md` | owner |
| D025 | 2026-09-01 | Языки расширяются без правки кода: старт — русский и английский, дальше любые языки сети (румынский, венгерский и прочие). i18n-слой закладывается с первой версии | Владелец: «языки необходимо сразу же заложить возможность для расширения... насколько я понимаю, это не проблема, если мы подключаем i18n». Совпадает с принципом «форма данных сейчас, функции потом» | owner |
| D026 | 2026-09-01 | Срок первой версии — 20 сентября 2026. К этой дате телеграм-бот проводит проверку целиком и выдаёт на выходе то же, что раньше выдавал скилл в Claude Desktop: PDF-отчёт и текст письма. Объём бота под срок не расширяется | Владелец: «Наша задача запустить до двадцатого сентября телеграм-боты, которые уже позволят провести проверку и сгенерировать отчёт то же самое, что мы делали в Claude Destopie, то же самое должно получиться на выходе у бота». Эталон выхода — PDF из `examples/`, а не описание в спеке | owner |
| D027 | 2026-09-01 | База данных и MCP-сервер входят в работу вторым потоком, параллельно боту, а не откладываются на второй этап: завершённые проверки сливаются в Postgres, поверх него встаёт MCP-сервер, чтобы с накопленными проверками можно было работать через Claude. Уточняет D007: `inspection.json` остаётся рабочим состоянием идущей проверки, БД становится хранилищем завершённых | Владелец: «нам в любом случае, по сути, видимо, нужно уже закладывать MCP, уже закладывать базу данных, куда проверки будут сливаться. И, соответственно, чтобы можно было с этой базой данных работать через Claude». Слив при завершении не трогает уже принятые блоки `engine-fix` и `domain` и не ставит бота в зависимость от базы: упавшая база задерживает аналитику, но не проверку на точке | owner |
| D028 | 2026-09-01 | Созвон с командой от 01.09.2026 имеет приоритет над первичным планом: там, где решения D015–D025 расходятся со спекой, техпланом и дорожной картой, правятся документы, а не игнорируется созвон | Владелец: «этот созвон имеет бОльший приоритет над первичным планом». Первичный пакет писался до разговора с командой и до части этих решений | owner |
| D029 | 2026-09-01 | Повторные нарушения в MVP не считаются и удвоенного вычета нет. Неисправленное с прошлой проверки учитывается не как повтор нарушения, а как факт невыполнения экшн-плана; само нарушение фиксируется заново. Функционал повторов, D0-предупреждений и контроля незакрытых экшн-планов отложен на следующий год. **Отменяет D015** | Протокол созвона 01.09: «Функционал учёта повторных нарушений, D0 и предупреждений отложен на следующий год: партнёров нужно заранее предупредить о новой логике, а бизнес-девелоперам — выработать дисциплину контроля экшн-планов». Градации штрафов за незакрытый экшн-план обсуждались, но не утверждены | owner |
| D030 | 2026-09-01 | Методика весов и обнуления зон в MVP не пересматривается: чек-лист берётся как есть, до согласования общей методологии с внешними аудиторами. За основу логики MVP берётся «метод Паши», уже частично заложенный в разработку, и проверяется прогоном последней проведённой проверки | Протокол: «Логика влияния обнуления одной зоны на итоговую оценку и конкретная методология весов остались без финального решения... отложен до согласования общей методологии с внешними аудиторами». Значит парадокс из #54 остаётся известным долгом, а не правится в обход методологии | owner |
| D031 | 2026-09-01 | Бот поддерживает два режима проверки: без чек-листа (опытный аудитор фиксирует нарушения в свободном порядке) и по чек-листу (менее опытный проходит пункты по зонам). Чек-лист структурируется по маршруту обхода, чтобы не возвращаться между участками. Входит в MVP | Протокол: «Для проверок нужен режим с чек-листом и без него». Расширяет спеку: там был только свободный порядок | owner |
| D032 | 2026-09-01 | Пиццерия выбирается из единого списка, а не вводится текстом; дата проверки и проверяющий подставляются автоматически, проверяющий — по Telegram ID. Геоподбор ближайших точек обсуждался, но в MVP не входит: возможность в Telegram не подтверждена | Протокол: единый список исключает опечатки и обеспечивает корректную статистику. Совпадает с D016 и переводит справочник точек из второго этапа в MVP | owner |
| D033 | 2026-09-01 | Отчёт не пересчитывается и не меняется задним числом — ни при правке чек-листа, ни при возражениях партнёра. Подтверждённые исключения действуют с момента подтверждения и применяются только к будущим проверкам | Протокол. Закрывает Q009 и Q014: динамика точки считается по замороженным отчётам, а не пересчётом | owner |
| D034 | 2026-09-01 | При споре партнёра оценка нарушения остаётся в отчёте. Если аргументы признаны обоснованными, предписание можно не выписывать. Решение принимает бизнес-девелопер, проводивший проверку | Протокол. Закрывает Q011 | owner |
| D035 | 2026-09-01 | История проверок и писем хранится в системе. Исторические проверки за три года заливаются разработкой программно ради статистики; ручного внесения старых проверок не будет | Протокол. Закрывает Q015 и подтверждает D027: база нужна не «на будущее», а как место, где эта история живёт | owner |
| D036 | 2026-09-01 | Температурные нормы едины для всех стран. Исключение возможно только при лабораторных исследованиях, подтверждающих иной режим; под исключения нужен отдельный функционал, принимающий подтверждающие документы, а не заявления | Протокол: «не на основании неподтверждённых заявлений или документов, сгенерированных ChatGPT». В MVP не входит, форма данных предусматривается | owner |
| D037 | 2026-09-01 | Сроки: 20 сентября — первая проверка, к ней бот готов для работы и тестирования (чек-лист может ещё требовать отладки). Вся система — полтора-два месяца. Демо веб-интерфейса — ориентировочно середина ноября. Партнёрам сервис показывается в следующем году. Уточняет D026 | Протокол, раздел «Сроки». Важное смягчение: к 20.09 нужен бот, годный для работы и тестирования на живой проверке, а не вылизанный продукт | owner |
| D038 | 2026-09-01 | Веб после MVP делается с ориентиром на MD Audit: аналитика, хранение данных, управление чек-листами, настройка пунктов, весов и буквенных оценок. Принцип — вручную только сама проверка, всё остальное автоматизировано | Протокол. Закрывает Q013 | owner |
| D039 | 2026-09-01 | Приоритет уведомлений: сроки предписаний, незагруженные исправления, свежие проверки с результатом D3. Напоминания «точка давно не проверялась» приоритетом не являются. Полная система уведомлений — после запуска работающего бота | Протокол. Уточняет Q012 | owner |
| D040 | 2026-09-01 | Сервис платный. Ориентир по себестоимости, названный на созвоне: около 15 центов за фотографию, около 2 евро за проверку с 50 фотографиями. Модель ценообразования не определена, считается отдельно с оглядкой на MD Audit (плата за точку в месяц) | Протокол. Расходится с нашим замером на боевом кадре ($0,0146 за вызов, около $0,58 на проверку) более чем на порядок — расхождение вынесено в Q020 | owner |
| D041 | 2026-09-01 | Первая версия бота — только свободная фиксация, в том виде, как задумывалось изначально: аудитор идёт по пиццерии и присылает то, что нашёл. Режим по чек-листу (D031) остаётся в продукте, но делается после первой версии, а не к 20.09 | Владелец: «первично бота мы делаем в том формате, в котором изначально он задумался, что у нас идёт просто проверяющий, он идёт по пиццерии, смотрит все это». Подтверждено счётом по методике: полный обход по чек-листу — 495 пар «пункт × применимая зона», тогда как на реальных проверках фиксировалось 7 и 10 записей. Режим обхода дороже свободной фиксации примерно в пятьдесят раз по числу действий аудитора | owner |
| D042 | 2026-09-01 | Режим по чек-листу в MVP не делается совсем; его будущая форма — выбор зоны, вход в неё и список пунктов, которые в этой зоне ещё можно посмотреть. Уточняет D031 и снимает Q021 в пользу третьего варианта: не полный обход и не реперные точки, а оставшиеся пункты зоны списком | Владелец: «в МВП мы идём без чек-листа. Его докрутим немножко позже. Я предполагаю, что это будет просто сообщение, которое будет списком давать пункты, которые ещё можно посмотреть в той или иной зоне. То есть предварительно мы выбираем зону, в неё проваливаемся» | owner |
| D043 | 2026-09-01 | Фотография без комментария разбирается ботом самостоятельно: он смотрит кадр, выделяет проблему и предлагает найденное пунктами, коротко и без воды; аудитор подтверждает в моменте и идёт дальше либо уточняет. **Отменяет требование спеки** «фотография без комментария вызывает уточняющий вопрос и не фиксируется сама» | Владелец: «если просто прилетела фотография, бот должен сам посмотреть, сам выделить проблему, и после этого уже зафиксировать то, что есть... лучше, наверное, в моменте, чтобы девелопер сразу подтвердил, окей, и пошёл дальше, либо посмотрел, уточнил». Принцип «модель предлагает, фиксирует человек» не нарушается: без подтверждения запись по-прежнему не появляется | owner |
| D044 | 2026-09-01 | У каждой записи хранится источник: комментарий аудитора или самостоятельное распознавание по кадру. Записи второго рода подсвечиваются при предвычитке отчёта | Владелец: «там, где идут комментарии, там ответственность девелопера. Там, где просто фотка пришла и нейронка сама что-то выделила» — ответственность за формулировку разная, и это должно быть видно | owner |
| D045 | 2026-09-01 | Самостоятельный разбор кадра не запускается сразу: после фотографии бот ждёт комментарий короткое окно, и только при тишине разбирает сам. Слова аудитора всегда сильнее догадки по картинке, поэтому комментарий, пришедший позже окна, но до подтверждения записи, отзывает предложение бота и заменяет его разбором по словам. Голосовое ждёт транскрипции, до неё ничего не фиксируется | Владелец: «он скинул фотографию и потом скинул голосовуху. Вот тут нужно как-то этот механизм продумать, чтобы бот не начинал генерировать свою мысль по фотке, если он потом получил голосовуху». Одним окном ожидания задача не решается: голосовое записывается дольше любого разумного окна, а Bot API не сообщает, что пользователь сейчас говорит в микрофон. Поэтому окно короткое (стартовое значение 6 секунд, калибруется на первом выезде), а надёжность даёт отзыв предложения | owner |
| D046 | 2026-09-01 | Кадр сам собой не разбирается. На фотографию бот отвечает вопросом «Разобрать?» с кнопкой: нажал — кадр уходит в модель; не нажал и прислал голосовое или текст — разбор идёт по комментарию, а самостоятельный не запускается вовсе. **Заменяет D045**: окно ожидания и отзыв предложения не нужны, их роль выполняет явное действие человека | Владелец, личное решение: «скидываем фотографию, бот тут же спрашивает: разобрать? И если мы нажимаем да, то оно разбирается. Если мы ничего не делаем, просто присылаем голосовуху, значит, ничего не разбирается. Мы так, собственно, защитимся от бездумного расходования на лишние разборы». Дешевле и предсказуемее таймера: ни одного вызова модели без решения человека | owner |
| D047 | 2026-09-01 | Отдельного шага «выбор зоны» нет и обязывать аудитора обозначать зону при входе в помещение система не будет. Зона берётся из слов аудитора в самом комментарии: «пол в горячем цеху грязный» — этого достаточно. **Отменяет T066** и возвращает поведение, описанное в `docs/06-mvp-bot.md`: зона из комментария, а не из вида кадра | Владелец: «мы не будем обязывать пользователя обязательно обозначать зону при входе в неё... Он записывает, что вот пол горячий цех грязный, и всё». Лишний шаг в диалоге стоит дороже, чем выигрыш в точности: аудитор идёт по точке с занятыми руками | owner |
| D048 | 2026-09-01 | Последняя названная зона запоминается и подставляется как первая догадка для следующих кадров. Смена зоны — на аудиторе: перешёл в другое помещение, проговорил это в комментарии. В подтверждении зона видна всегда и меняется одним нажатием, поэтому ошибка памяти стоит одного касания | Владелец: «запоминать последнюю зону есть смысл... просто нужно подмечать, что если зона поменялась, нужно проговорить». По точке ходят зонами, а не вперемешку, поэтому память угадывает чаще, чем ошибается | owner |
| D049 | 2026-09-01 | Управление чек-листами через агента (Claude по MCP) — следующий шаг после чтения: заводить новые наборы, править существующие, добавлять и снимать пункты и зоны. Правка не трогает боевой набор напрямую: создаётся новая версия, она проходит проверку целостности и только потом публикуется; проверки, сделанные по прежней версии, остаются на ней и не пересчитываются | Владелец: «было бы очень неплохо дать возможность через Claude или через любой другой агент заводить новые чек-листы и проводить работу с теми чек-листами, которые есть уже... это следующий уровень, и с этим точно нужно прям идти». Опора уже есть: `engine/manage.py` умеет add, edit, remove, restore, zone-add, zone-remove, validate, import-xlsx. Версионирование обязательно из-за D033: отчёт заморожен на своей версии методики | owner |
| D050 | 2026-09-01 | Любое изменение чек-листа, даже мелкое, порождает новую версию с датой — железно, без исключений. Идентификатор версии составной: имя набора, дата публикации и отпечаток данных, например `imf-2026-09-01-a1b2c3`. Имя и дата нужны человеку, отпечаток — машине: он не даёт выпустить изменённую методику под прежним именем, если дату забыли поднять. Старые версии не удаляются никогда | Владелец: «у каждого чек-листа делать версионирование и подписывать дату. Если мы внесли изменения даже небольшие, значит, это уже новая версия чек-листа с новой датой железно». Механизм наполовину готов: `checklist_version()` уже пишет версию в каждую проверку и умеет считать отпечаток данных, но при наличии `checklist_version.txt` явное имя перекрывает отпечаток — и правка данных проходит под старым именем | owner |
| D051 | 2026-09-01 | Справочника пиццерий в MVP нет: название точки вводится текстом, как и было в исходной спеке. Справочник и выбор из списка добавляются позже. **Отменяет эту часть D032 и D016** | Владелец: «сейчас мы этот справочник не вводим... Сейчас мы делаем просто чистое поле, куда мы вбиваем название пиццерии». Снимает Q023 и внешнюю зависимость от выгрузки точек — блокера у срока больше нет | owner |
| D052 | 2026-09-01 | «Метод Паши» — это переданный владельцем скилл Claude Desktop, сохранённый как `docs/_legacy-skill.md`; на нём построены `engine/` и методика в `data/`. Отдельного расчёта заводить не нужно, сверка сводится к прогону эталонных проверок, который уже есть в `make regress` | Владелец: «метод Паши — это тот плагин, который я тебе передавал, на котором у нас и начало устраиваться всё это обсуждение». Снимает Q019 и риск переделки ядра | owner |
| D053 | 2026-09-01 | Данные продукта живут в Supabase: Postgres под проверки, Storage под фотографии, Edge Functions под MCP — тем же стеком, что уже работает в Swarm Brain. Бот остаётся контейнером на домашнем сервере: ему нужен постоянный процесс для long polling, которого Edge Functions не дают. **Уточняет D003 и D007** | Владелец: «у нас есть Supabase свободный, куда можно всё это поставить... там база данных тогда можно смело разместить». Один стек вместо двух, MCP по готовому образцу, хранилище файлов из коробки. Условие: в организации `GarroV's Org` бесплатный план даёт два активных проекта, оба заняты (Swarm Brain и English_bot) — нужен слот | owner |
| D054 | 2026-09-01 | Фотографии выгружаются в Supabase Storage при завершении проверки. Доступ к хранилищу изолируется за S3-совместимым интерфейсом (D004), поэтому переезд на Google Drive или другое хранилище остаётся правкой конфига | Владелец: «можем ли мы временно как хранилище использовать... и в перспективе мы просто поменяем». Замер на боевых данных: 31 кадр занимает 7 МБ, средний кадр 232 КБ — проверка из 40 кадров это около 9 МБ | owner |
| D055 | 2026-09-01 | MCP-сервер делается по образцу `swarm-mcp`: Edge Function, личный токен, тот же мост. Ничего нового изобретать не нужно. Снимает Q017 | Владелец: «у Swarm Brain у нас как-то всё работает, всё нормально». Схема проверена в бою — этим же способом достан протокол созвона 01.09 | owner |
| D056 | 2026-09-01 | Вопросы веса качества продукта (Q007), внешних аудиторских бланков (Q004), назначенных проверок (Q008) и фиксации предписаний (Q022) закрываются как неприоритетные: к разработке MVP не относятся, возвращаемся к ним по мере надобности | Владелец: «вес качества продукта, внешние аудиторы, назначенные проверки — это всё пофигу» | owner |
| D057 | 2026-09-03 | Supabase под этот продукт живёт в **отдельной учётной записи**, не в организации, которую отдаёт MCP-коннектор (там Swarm Brain и English_bot). Слот бесплатного плана проблемой не является, ничего ставить на паузу и переводить на Pro не нужно. **Снимает Q024**, разблокирует T090 | Владелец, дословно: «я тебе уже 10 раз сказал что СУПАБЕЙЗ БУДЕТ НА ДРУГОМ АККАУНТЕ». Решение было устным и в `decisions.md` не попадало ни разу — именно поэтому вопрос про слот всплыл заново и владелец объяснял его повторно | owner |
| D058 | 2026-09-03 | Главный риск, который владелец хочет закрыть, — не выбор площадки, а **способность продукта работать под нагрузкой**. Требование заводится задачей T101: сначала измерить фактический потолок (одновременные проверки, разбор кадров, сборка PDF, конкурентная запись состояния) и назвать его числом, затем устранить найденные узкие места. Целевое число владельцем не задано и **вслепую не выбирается**: сначала измеренный потолок, потом решение, хватает ли его | Владелец, дословно: «вопрос в первую очередь про способность работать под нагрузкой как надо». В спеке и техплане слово «нагрузка» не встречается ни разу — требование новое и незакрытое | owner |
| D059 | 2026-09-03 | Демо не отстаёт от основной ветки: разворачивается из `main` и получает те же доступы (переменные окружения и ключи), что и основной стенд, отличаясь только данными — синтетическими и английскими. Заводится задачей T102 | Владелец, дословно: «а почему демо у нас отстаёт от мастера? та мпо сути надо настроить доступы = как у мастера». Ответ пришёл без реплая, адресат не проставлен — отнесён к этому проекту, потому что демо-стенд и сид тут появились в тот же вечер (блок infra, T074); если трактовка неверна, стоимость правки — одна задача | owner |
| D060 | 2026-09-03 | T073 («поднять бота на домашнем сервере») разделена: проверяемое на тестовой площадке проверяется стройкой, фактический подъём на домашний сервер — раскаткой. Заводится T105: `docker compose up` локально, перезапуск контейнера не теряет незавершённую проверку, состояние переживает пересоздание. T073 остаётся задачей раскатки и в стройке не закрывается; T080 и T101 перевешиваются на T105, чтобы сквозной смоук и замер нагрузки не ждали раскатки | Тестовая площадка проекта — `local` (профиль Forge), а домашний сервер по D053 целевая: там продукт должен жить. Команда стройки раскатку не делает по построению — она требует отдельного «да» владельца в том же разговоре и ночного окна. Без разделения встали бы три задачи разом, из них две проверяемые локально в полном объёме | auto |
| D061 | 2026-09-03 | База проверок делается и остаётся пока **на локальном Postgres**, перенос в Supabase — позже и сменой `DATABASE_URL`, без правки кода. Проверено фактом, а не обещанием: в `src/db` нет ни одного упоминания Supabase, PostgREST, ключей и ролей; ходим через `psycopg` по строке из `DATABASE_URL`; миграция `0001_initial_schema.sql` — шесть таблиц на чистом Postgres, без RLS-политик, без `auth.` и `storage.`. Привяжут к Supabase только две вещи, и обе позже: выгрузка кадров в Storage (T094, за S3-совместимым интерфейсом по D004) и MCP через Edge Functions (D055). **Снимает Q025** — доступы к облаку прямо сейчас не нужны | Владелец, дословно: «Ты пишешь так чтобы на супабейз залить? Все окей? Или нам удобнее сделать на локальном сервере и в будущем перенести?». Ответ по факту: код нейтрален, поэтому дешевле идти на локальном и не ждать доступов — три задачи разблокируются сразу | owner |
| D062 | 2026-09-03 | Замер нагрузки повторяется **на живом**: настоящие вызовы модели на боевых кадрах, с замером задержки, поведения под параллельной очередью и на отказе провайдера. Это платная операция, и владелец её прямо санкционировал; расход ограничивается десятками вызовов и называется числом в журнале. Живая часть через Telegram ограничена тем, что бот отвечает только разрешённым идентификаторам — слать ему сообщения от имени владельца стройка не может и не будет | Владелец, дословно: «проверь на живом. Нам надо реально понять ограничения системы и знать их на будущее». Ответ на честно названную границу прошлого замера: тот мерил обработку, а не сервис целиком — настоящая сеть и настоящая модель в него не входили | owner |
| D063 | 2026-09-03 | Разбор получает **быстрый путь без модели**: если слова аудитора однозначно указывают на один пункт, он показывается сразу кнопкой подтверждения, и модель не зовётся. Не однозначно — как сейчас. Модель не заменяется: замер показал, что на текущей карте слов детерминированное сужение ни разу не дало однозначного ответа (0 из 17 боевых записей), поэтому основная работа не в коде, а в данных — усилить `data/photo-cues.md` до однозначности на частых нарушениях. Заводится T113 | Владелец, дословно: «у нас тут не нужны размышления, а нужна сверка с текущим списком нарушений. давай подумаем как эту логику построить». Его догадка про время верна по сути и подтверждена замером: текст без кадра идёт 4.3 с против 5.3 с с кадром — ждём не картинку, а рассуждение модели. А вот вторая половина («нейронка не всегда нужна») на сегодняшних данных не подтверждается, и это сказано числами, а не мнением | owner |
| D064 | 2026-09-03 | **Подтверждение нажатием при фиксации ТЕКСТОМ снимается.** Аудитор написал словами — запись появляется сразу, без кнопки. Для кадров подтверждение остаётся: там система угадывает, а в тексте зона и суть названы человеком. Это **уточняет принцип 3 конституции** («ни одна запись не попадает в отчёт без подтверждения аудитора») — он остаётся в силе для разбора кадра, но не для фиксации словами. Заводится T121 | Владелец, дословно: «снимаем с текста подтверждение, потом добавим». Оговорка, высказанная ему до решения и оставшаяся в силе: сопоставление слов с номером пункта тоже промахивается — на его же примере «кассовая зона, просрочка чизкейк» слова поднимают единственный код CLN02 («оборудование в зоне мойки»), а верный PRD10. Промах станет тихим. Владелец решение подтвердил, «потом добавим» означает возврат к вопросу, а не отмену | owner |
| D065 | 2026-09-03 | **ОТМЕНЁН 2026-09-04 решением D066 — читать D066, не это.** Карта слов (`data/photo-cues.md`) пополняется **позже и управляющей компанией**, не стройкой. Семь строк, предложенных блоком recognize, в боевую карту не вносятся. (Числа «35% вместо 18%», стоявшие здесь, — ВЕРХНЯЯ ГРАНИЦА при подстановке эталонной зоны, живому боту недостижимая: замер был починен на T125, и как зовёт бот получается 24% вместо 6%.) T118 снимается с очереди и ждёт данных УК | Владелец, дословно: «карту слов явно будем потом составлять». Это его данные: какими словами аудиторы говорят на точке, знает УК, а не стройка | owner |
| D066 | 2026-09-04 | **D065 отменён владельцем: карту слов составляет стройка**, приблизительно и по каждому пункту чек-листа, а управляющая компания правит потом. Форма — таблица, которую можно отсмотреть и прокомментировать. T118 возвращается в работу; её ведёт сессия `dodo-audit-service-2e` в ветке `feat/word-map` на копии методики, боевой `data/photo-cues.md` перезаписывается одним движением после слова владельца. Следствие для T125: числа замера быстрого пути снимаются ПОСЛЕ применения карты, иначе устареют в тот же час — файл вне git и физически один на все копии | Владелец, дословно: «т118 составь примерно сам. предположи по каждому пункту чек листа какие наборы слов могут быть. со временем поправим. или создай таблицу где можно будет отсмотреть и дать комменты». Прямо снимает основание D065 («это его данные»): черновик от стройки дешевле ожидания данных УК, а правка остаётся за ней | owner |
| D067 | 2026-09-04 | Живого бота проходят **дважды критическим взглядом человека из общепита** — не «работает ли», а удобно ли им пользоваться на точке. Заводится T130, находки оформляются задачами | Владелец, дословно: «при этом запиши себе протестировать бота 2 раза с критической точки зрения по юзабилити как человек из общепита». Класс дефектов, который тесты не ловят по построению: они проверяют заранее сформулированное утверждение, а неудобство никто не формулирует | owner |
| D068 | 2026-09-04 | **Карта слов необязательна, и её отсутствие не отказ, а деградация.** Сегодня файл необязателен на старте (входит в необязательные файлы методики), но обязателен при первом обращении: `recognize.cues` бросает отказ, и всплывает он не при подъёме стенда, а в руках аудитора на первом же комментарии — именно так умирает демо (#121). Правильное поведение: быстрый путь не срабатывает, разбор идёт моделью, как при любом несовпадении. Заводится T157 | Прямо следует из D063: быстрый путь идёт ДО модели, «не сошлось хотя бы одно — обычный разбор моделью, без изменений». Отсутствие карты и есть «не сошлось». Карта — ускоритель, а не условие работы продукта: без неё он обязан работать медленнее, а не отказывать. Развилку принесла соседняя сессия, разбирая T149 | auto |
| D069 | 2026-09-04 | **Информационная часть спрашивается в конце проверки, после подтверждения о её завершении и ДО сборки отчёта.** Ввод свободный: текстом или голосом, с возможностью приложить кадр; при голосе расшифровка показывается до записи и её можно поправить. **Скрытые поля отчёта снимаются** — `HIDDEN_INFO` в движке убирается, все поля печатаются партнёру. Заводятся T158 (бот) и T159 (движок) | Владелец, дословно: «вот этот блок надо в конце проверки вывести.я думаю что после подтверждения что проверка закончилась, и дальше идет информациоонная часть. и соответственно дать возможность написать текстом или голосом + прикреппить фото,. при голосе в любом случае показать сначала что вышло по транскрибации». На отдельный вопрос про скрытые поля ответил: «Собирать и печатать в отчёте» | owner |
| D070 | 2026-09-04 | Уточнения к D069, проверенные фактом: **INF02 (вид проверки) в информационную часть НЕ входит** — он уже спрашивается мастером на старте, второй вопрос был бы шагом назад. **INF09–INF11 тоже не входят** — это фотофиксация (настройки печи, показания оборудования, фото продукта), она уже работает обычным потоком кадров. Остаются семь: INF01, INF03, INF04, INF05, INF06, INF07, INF08, и они неоднородны — свободный текст (INF01, INF06, INF08), да/нет (INF03, INF04), даты (INF05, INF07). **Подтверждение расшифровки голоса не противоречит D064**: D064 снял подтверждение с текста, потому что человек написал сам, а расшифровка может ослышаться — это другой случай, и сводить их обратно нельзя. **Допущение, владельцем прямо не подтверждённое: все поля пропускаемые** — иначе аудитор в конце обхода упирается в семь обязательных вопросов; пропущенное поле не печатается, а срок плана падает на прежний расчёт | Разбор принесла соседняя сессия, факты сверены мной: команда движка `audit.py info` существует и работает, бот ею не пользуется вовсе, блок `info` в обеих эталонных проверках пуст (регресс от снятия скрытых полей не поедет), а INF07 уже читается письмом — после задачи срок начнёт браться у аудитора сам | auto |
| D071 | 2026-09-04 | **ОТМЕНЁН 2026-09-04 решением D078 — читать D078, не это.** **Нарушение можно записать без фотографии**, и вдобавок заводится признак «фото обязательно» ПО ПУНКТАМ — списком, который правит управляющая компания. Признак живёт ОТДЕЛЬНЫМ файлом рядом с методикой (как `route.csv`), а не колонкой в чек-листе: `import-xlsx` пересобирает чек-лист из выгрузки УК с `keep_foreign=False` (`engine/manage.py:584`), и колонка исчезла бы при первом же импорте. Заводятся T160 (бот) и T161 (домен). Пометка записи «без фотофиксации» в отчёте владельцем НЕ решена — вопрос Q029 открыт | Владелец, дословно: «тут может быть просто фото кассовой зоны или чего-то такого. в целом, такие пункты можно и пустить без фотофиксации. главное смотри чтобы алгоритм не сбоил. мы сможем отдельные пункты сделать обязательными с фото, а дургие нет?». Проверено фактом: разбор отсутствие кадра уже терпит (`classify(note, photo: Optional[bytes] = None)`), мешает только проверка в самом боте | owner |
| D072 | 2026-09-04 | **Текущий процент во время обхода не показывается — только в конце.** | Владелец ответил «показывать только в конце» на вопрос Q028. Причина, названная ему и оставшаяся в силе: во время обхода число аудитору ничего не даёт, а соблазн не записывать мелочь создаёт, и такое влияние не видно ни в отчёте, ни в базе. Заводится T162 | owner |
| D073 | 2026-09-04 | **Репозиторий остаётся публичным** — осознанно, повторным решением. Следствие, действующее с этого дня: в тела задач трекера и в новые документы НЕ попадают формулировки методики и дословные цитаты владельца. Уже опубликованное не затирается: история правок GitHub публична, и затирание раскрытия не убирает. Сведённые двумя сессиями числа опубликованного: 4 формулировки вопросов, 11 названий зон, 9 фрагментов карты кадров, 20 файлов | Владелец подтвердил публичность повторно, зная масштаб. Правило про новые задачи предложено соседней сессией и принято: оно ничего не стоит и закрывает канал на будущее, тогда как чистка прошлого стоила бы удаления задач или смены видимости | owner |
| D074 | 2026-09-04 | **ОТМЕНЁН 2026-09-04 решением D078 — читать D078, не это.** **Запись, сделанная без фотографии, помечается в отчёте партнёру как «без фотофиксации».** Закрывает открытый пункт D071 — задача идёт целиком, без открытых мест | Владелец, дословно: «партнёр видит, на чём запись держится, и не ищет фото, которого нет» | owner |
| D075 | 2026-09-04 | Файл признака «фото обязательно» обязан **работать пустым**: пустой список значит «фото не обязательно нигде», и это законное состояние, а не отказ на старте. Список правит управляющая компания — блок его не выдумывает | Тот же урок, что D068 про карту кадров: необязательный файл не должен становиться обязательным при первом обращении, иначе отказ всплывает в чате у аудитора, а не при подъёме стенда. Второй раз за сутки тот же класс, поэтому записан отдельным решением, а не оговоркой в задаче | auto |
| D076 | 2026-09-04 | **Ночное окно раскатки снято НА ЭТАПЕ СТРОЙКИ.** Границу читать точно: снято ограничение по времени суток, и только оно. Явное «да» владельца на конкретный пакет по-прежнему обязательно, смоук на самом сервере после подъёма — тоже. Когда у продукта появятся живые пользователи, окно возвращается | Владелец, дословно: «по поводу окна: на этапе стрйоки сейчас можно раскатывать когда угодно», и раньше в том же разговоре «вот как будешь готов так поднимай. машина работает, проблем и блокеров не вижу». Граница записана отдельным предложением намеренно: без неё следующая сессия прочитает решение как «раскатываем как хотим навсегда» | owner |
| D077 | 2026-09-04 | **Список слов учится на правках аудитора, но пополняется НЕ автоматически.** Расхождение «модель предложила одно, аудитор поправил на другое» сохраняется рядом с записью и собирается в предложения для управляющей компании — боевой список правит она. Автоматическое пополнение запрещено: быстрый путь показывает пункт без подтверждения (D064), поэтому неверное слово уехало бы в отчёт партнёру без чьего-либо ведома. Принцип «модель предлагает, фиксирует человек» распространяется и на слова. Заводятся T164, T165, T166 | Владелец, дословно: «при несостыковках, или если пользователь добавит что-то в духе "ГРЯЗЬ НА ПОЛКЕ В ГОРЯЧЕМ ЦЕХЕ, ЭТО ЧИСТОТА" то мы долполняем наш список терминов». Проверено соседней сессией: предложение модели сегодня НЕ сохраняется нигде — ни код, ни уверенность, ни то, что аудитор поменял, — то есть сигнал о промахе теряется целиком | owner |
| D078 | 2026-09-04 | **ОТМЕНЯЕТ D071 и D074: фотофиксация нужна ВСЕГДА.** Записи без кадра не будет; вместо этого бот обязан внятно сказать, что кадр нужен, — так, чтобы человек понял, а не упёрся в отказ. Признак «фото обязательно по пунктам» (T161) теряет смысл и снимается: обязательно везде. Пометка «без фотофиксации» в отчёте (T163, уже сделана) остаётся как страховка на записи, пришедшие из истории или созданные мимо бота, но штатным путём появляться не должна | Владелец, дословно: «Нарушение без фото: Нет, фото фиксация всегда нужна . Либо давай сделаем так что бот уведомляет что есть с проблема . В обще, надо чтобы человек понял что фото должно быть». Пересмотр вчерашнего ответа на тот же вопрос: сутки назад он же сказал «такие пункты можно и пустить без фотофиксации» | owner |
| D079 | 2026-09-04 | **Эталонные проверки в репозитории — не секрет.** Вопрос Q030 об узнаваемости партнёра закрыт: оставляем как есть. Отдельно владелец спросил, будут ли проверки в будущем лежать под защитой базы — ответ да, и это уже так устроено: слив в историю идёт в Postgres с изоляцией партнёров ключами схемы и политиками, закрывающими завершённое от правки | Владелец, дословно: «проверки что я тебе передавал - они для примера, секрета тут нет все нормально. в будущем проверки будут лежать под защитой БД, верно?» | owner |
| D080 | 2026-09-05 | **Сданный отчёт не правится.** Вернуться к завершённой проверке и дописать в неё находку нельзя: можно только удалить проверку целиком или завести новую. Функционал правки появится позже. Убирает задваивание истории (T201): вторая строка о том же обходе возникала именно из дописывания | Владелец, дословно: «отчеты мы не правим. в будущем доабвим функционал, сейчас мы можем только удалить его, или завести новый» | owner |
| D081 | 2026-09-05 | **Разбор перестроен по потокам, и правка идёт ОТВЕТОМ на сообщение бота.** Четыре случая: (1) кадр с комментарием — разбирается КОММЕНТАРИЙ, кадр в модель не уходит; (2) кадр без комментария — разбирается кадр, бот возвращает вычитанное, и правка вносится ответом на его сообщение; (3) голосовое — бот разбирает речь, а кадр аудитор присылает ОТВЕТОМ на своё же голосовое, и система подхватывает его в ту же запись; (4) пачка кадров — отбивка приходит списком отдельными сообщениями, и ответ на любое из них правит именно ту запись. Пример владельца: система распознала грязную линию начинения, но написала «стол в холодном цехе» — аудитор отвечает «горячий цех, линия начинения», и система ищет пункт уже по этому комментарию | Владелец, дословно: «фото с комментм - обрабатываем коммент. фото без коммента - разбираем фотку, возвращаем то что мы вычитали. Если комментарий надо поправить - ОТВЕТОМ на сообщение бота пользователь вносит корректировку… пользователь пишет голосовуху, бот разбирет. и ответом на свою же голосовуху присылает фото. система эту фотку подхватывает и забирает в отчет… прилетает отбивка со списком нарушений отдельными сообщениями. и ответ на одно из сообщений будет как раз вносить правки» | owner |
| D082 | 2026-09-05 | Таблицу слов можно применять: владелец её не правит. Отрицание в быстром пути починено (T195, T197), что было условием применения | Владелец, дословно: «таблица слов - на данный момент я ничего не правлю. забыли вобще про это» | owner |
| D083 | 2026-09-05 | Парадокс расчёта остаётся как есть: считаем по логике, заложенной в методике. Правка ожидается в будущем, но не сейчас. **Снимает Q006**, задача T132 закрывается как отложенная осознанно | Владелец, дословно: «парадокс расчета: идем так как было заложено в скиле. тут своя логика, и в будущем точно будет правиться» | owner |
| D084 | 2026-09-05 | Вид проверки печатается и в отчёте, не только в письме. **Снимает Q033** | Владелец, дословно: «вид проверки: в отчет тоже вложи. там дальше попраивм» | owner |
| D085 | 2026-09-05 | **Запись в эталоне верна, эталон не трогаем.** Пункт про чистоту полов и стен записан в кассовой зоне намеренно: это спорная территория, относящаяся скорее к производству. Продукт такую запись НЕ отклоняет — она проходит с пометкой «зона не из списка пункта», и для спорной территории это правильное поведение: не запрещает, но обращает внимание. Открытым остаётся только одно: добавлять ли кассовую зону в список зон этого пункта в самой методике — это данные управляющей компании. **Снимает Q032** | Владелец, дословно: «по белграду и чистоте: этотпункт был в кассовой зоне, это спорная территория и относится она все же больше к производству» | owner |
| D086 | 2026-09-05 | **Неверный отчёт не правится: заводится новый, старый удаляется — и удаляется через MCP, не в боте.** Это закрывает вопрос, оставленный блоком bot15: «убрать из чата» в боте остаётся уборкой копии, а настоящее снятие проверки из истории — операция управляющей компании. Значит в MCP нужен инструмент снятия проверки, а в базе — то, чем снятие записывается: сегодня в `src/db` нет ни одной операции удаления, слив идёт слепком с `on conflict do nothing`, кадры лежат в объектном хранилище отдельно | Владелец, дословно: «если мы сделали неверный отчет, то мы заводим новый и удаляем старый. Старый мы удаляем на данный момент через мсп». Уточнение «на данный момент» записано намеренно: способ временный, правка отчётов появится позже (D080) | owner |
| D087 | 2026-09-05 | **У бота появляется скрывающееся меню с командами.** Названы владельцем: «Новая проверка» и «Установка MCP» — последняя показывает готовую команду для терминала, как это сделано в соседнем продукте. Остальные пункты диспетчер предлагает, состав утверждает владелец | Владелец, дословно: «нам надо в боте сделать кнопки во скрывающемся меню. Новая проверка, установка мсп (внутри команда для терминала, как в сворм брейне), предлоиж еще пункты» | owner |
| D088 | 2026-09-05 | **Раскатка на домашний сервер идёт без ожидания ночного окна.** Владелец снял требование окна 23:00–05:59 для этого проекта на текущем этапе: продукта с живыми пользователями на стенде нет, ждать ночи не за чем. Закрывает Q026 и разблокирует T073 | Владелец, дословно: «катим на [домашний сервер]. ночное окно не ждем» | owner |
| D089 | 2026-09-05 | **Снятая проверка остаётся в истории пометкой, причина снятия обязательна, и снятые проверки базово видны только администраторам.** Строка истории не удаляется: видно, что проверка была и почему её больше нет. Причина — обязательное поле, а не свободная необязательность («правил ошибку»). Обычный пользователь снятых проверок в истории не видит, администратор видит. Кадры снятой проверки убираются из хранилища. Закрывает Q037, уточняет D086 | Владелец, дословно: «помечаем что была, но удалена с оставленной причиной (условно, правил ошибку). доступно базово только админам» | owner |
| D090 | 2026-09-05 | **Голосовое до кадра больше не отказ, а ожидание: бот отвечает, что ждёт фото, придерживает расслышанные слова и собирает запись в момент прихода кадра.** После сохранения бот обязан дать отбивку, что пункт сохранён, — и эта же пара «подтверждение + отбивка» распространяется на ВСЕ прочие добавления, а не только на голосовое. Отбивка становится точкой правки: ответ на неё правит запись, включая зону, и бот подтверждает правку новой отбивкой. Снимает отказ из D078 в части «голосовое пришло первым», механику правки берёт из D081. Закрывает Q038 | Владелец, дословно: «получили голосовое - кинули сообщение что ждем фото. после получения фото даем отбивку что пункт сохранен. эту же механику кстати надо сделать и для прочих добавлений... при ответном сообщении на отбивку бота можно поправить нарушение. условно: бот отписался что "грязные полы в ХЦ сохранен, -1 балл" - пользователь пишет ответное сообщение "это был горячий цех" - бот принимает к сведенью и дает новую отбивку что он поправил и сохранил» | owner |
| D091 | 2026-09-05 | **Пачка кадров разбирается порциями не больше пяти.** Предел назван владельцем. Как предел выглядит для аудитора — отдельный открытый вопрос Q040: обрабатывать первые пять и просить прислать остальные отдельно, или принимать всю пачку и разбирать её порциями по пять. Закрывает Q039 в части числа | Владелец, дословно: «думаю можно ограничить только 5 кадрами. только как это будет выглядеть для пользователя? или это просто внутри системы будет обрабатываться по 5 кадров из пачки?» | owner |
| D092 | 2026-09-05 | **Накопленные коммиты main вливаются и пушатся.** Разрешение дано в разговоре после проработки Q026, Q037, Q038, Q039. Репозиторий публичный, поэтому пуш идёт только после проверки состава на секреты, методику и боевые данные | Владелец, дословно: «Про коммиты: проработай вопросы выше и вливай все» | owner |
| D093 | 2026-09-05 | **Пачка больше пяти кадров — исключение, а не рабочий случай: разбираются пять, остальные аудитор присылает отдельной пачкой.** Бот обязан сказать об этом словами, а не отбросить лишние молча. Закрывает Q040 и разблокирует T232 | Владелец, дословно: «я не думаю что часто будут такие сценарии, давай это все же смотреть как исключение, и не больше 5 штук» | owner |
| D094 | 2026-09-05 | **Репозиторий остаётся публичным, имена инфраструктуры в него не пишутся.** В доках плейсхолдер `<HOST>`, реальные значения — в приватном репозитории инфраструктуры и в `~/.ssh/config`. Пуш стережёт `tools/pre-push-guard.sh`: список запрещённых значений лежит ВНЕ репозитория (`~/.config/git/secret-terms.txt`), иначе он сам был бы утечкой. Чистка лечит будущее, не прошлое: старые значения остаются в истории правок публичного репозитория навсегда — владелец это знает и принял | Владелец выбрал «оставить публичным, вычистить доки» из трёх вариантов; перевод в приватный не стоил бы ничего (CI в репозитории нет, прогонов Actions ноль), но публичность оставлена осознанно | owner |
| D095 | 2026-09-05 | **Раскатка выполнена при незаконченной стройке — по прямому требованию владельца.** На момент раскатки 27 задач в статусе todo и блок bot17 работал сам. Владелец знал картину: она была показана ему до раскатки. Ночное окно снято D088 | Владелец, дословно: «Раскатку на свервер можно делать». Снаружи продукт при этом выглядит готовым — поэтому недоделанное перечислено в CHANGELOG.md | owner |
| D096 | 2026-09-05 | **Второй пакет коммитов запушен по отдельному «да» владельца.** Разрешение D092 намеренно не переносилось на следующий пакет: одно «да» не должно превращаться в бессрочный доступ к публичному репозиторию и к тому, что стоит на сервере. Спрашивается каждый раз. Перед пушем проверено фактом: секретов и имён инфраструктуры в уходящих строках нет, контрольные цифры 97.5%/A/5×D1 и 97%/A/6×D1 сошлись, набор тестов 1874 passed / 230 skipped / 0 падений | Владелец, дословно: «да пуш». Про раскатку этого пакета на площадку он не высказался — площадка остаётся на 0f02053 до отдельного ответа | owner |
| D097 | 2026-09-05 | **ПОСТОЯННОЕ разрешение на пуш и раскатку для этого проекта — спрашивать каждый раз больше не нужно.** Отменяет порядок D092/D096, где разрешение бралось на каждый пакет отдельно: владельца это утомляло, и он снял требование явно. Пуш main и раскатка на площадку выполняются по мере готовности, без вопроса. Ночного окна нет (D088). Проверки перед пушем остаются обязательными и без владельца: состав на секреты и имена инфраструктуры, контрольные цифры движка, набор тестов, смоук после раскатки | Владелец, дословно: «задивай все в проект». Раздражение вызвано повторными вопросами о разрешении, а не проверками | owner |
| D098 | 2026-09-06 | **Подключение к MCP выдаёт сам бот, командой, видной не всем.** Право видеть и звать её есть у владельца и у тех, кого он назовёт. Это отменяет сегодняшний порядок, при котором токен MCP заводится руками в файле окружения, принадлежит СТОРОНЕ и открывает всю её историю проверок: команда, выдающая доступ, обязана выдавать его ЧЕЛОВЕКУ. Прямое следствие — токены становятся личными: выпуск на конкретный идентификатор, хранение отпечатком, отзыв поимённо. Заводится T253, объём и развилки — в задаче трекера | Владелец назвал это сам, увидев, что бот не печатает токен никому: нужна команда, видная только ему и тем, кого он укажет | owner |
| D099 | 2026-09-06 | **Уточнения к D098, три штуки.** (1) Команда ВИДНА в меню, но только избранному кругу аккаунтов — не «команда без пункта», как я предлагал. (2) В MVP админы могут добавлять админов: каждый добавленный может добавлять дальше. (3) Смысл всей затеи назван владельцем прямо: открыть доступ ещё одной команде — «сгенерировать команду для MCP настройки Claude Desktop». То есть бот отдаёт готовую строку настройки, а не «токен»: токен внутри неё деталь, и владельцу её решать не надо | Владелец, о моём вопросе про судьбу прежних токенов: это внутренняя механика, вынесенная ему по ошибке. Про объём: «сделай для меня небольшую админку в боте, птом разберемся» — то есть в MVP делается малое и работающее, а не полная система прав | owner |
| D100 | 2026-09-06 | **Доступ к MCP снаружи делается, и делается сразу — тремя частями.** (1) Сервер поднимается как сервис площадки, а не руками: сегодня он не входит в compose и не запущен вовсе. (2) Наружу он выходит ТУННЕЛЕМ, а не публикацией порта: сервер остаётся на петле, площадка общая, соседи по машине доступа не получают. Беру Cloudflare Tunnel как рекомендованный — владелец им уже пользуется; замена на ssh-туннель возможна и ничего в остальном не меняет. (3) Мост перестаёт требовать копию репозитория: человек получает то, что можно взять одним движением, а не клонировать наш код | Владелец увидел, что выданная ботом строка никуда не ведёт, и на разбор трёх причин ответил: «давай сразу в работу забирай». Выбор туннеля мой — сказать иначе он может в любой момент, работа от этого не пропадёт | owner |
| D101 | 2026-09-06 | **Cloudflare Tunnel принят, оговорка про третью сторону снята владельцем.** Трафик с историей проверок партнёров идёт через инфраструктуру Cloudflare в расшифрованном виде — владелец считает это приемлемым на время пилота и планирует переезд после него. Значит своё решение (VPS с обратным ssh-туннелем) не строится сейчас, но остаётся названным: переезд — работа, а не настройка, и заводить её надо заранее, а не в день отказа от Cloudflare | Владелец: проблемы в прохождении отчётов через третью сторону не видит, после пилота будут переезжать | owner |
| D102 | 2026-09-06 | **Наружу выходим ОБЩИМ входом площадки (Tailscale Funnel), а не своим туннелем Cloudflare.** Причина простая и проверенная: в учётке Cloudflare нет ни одной зоны, а имя для постоянного туннеля берётся из зоны — сделать его не из чего. Funnel даёт постоянный адрес без домена и без покупки, и на этой же машине им уже живёт соседний продукт. Сервис `tunnel` из compose остаётся для площадки со своим доменом; сегодня он не поднимается. Между входом и сервером стоит звено: сервер слушает петлю ВНУТРИ контейнера, и проброшенный порт до неё не доходит — соединение приходит на внешний интерфейс, которого сервер не слушает | Владелец: домена нет ни одного, «проверяй как сделано в ПНЛ сервисе» — там публичный вход сделан именно Funnel и по той же причине (T161 того проекта) | owner |
| D103 | 2026-09-07 | **Пункт настройки MCP спрашивать клиента не должен: поддерживается только Claude Desktop.** И **посредника на Node (`mcp-remote`) в подключении не будет** — вместо него мост stdio↔HTTP на bash+curl, который у продукта уже есть (`tools/mcp_bridge.sh`, D055), а файл настроек приложения правится штатным `/usr/bin/plutil`: ни Node, ни Python на машине человека не требуется. Задача — #222 | Владелец на живом прогоне 07.09.2026: развилка «Claude Code или Desktop» ему не нужна, а Node в подключении он отверг прямо, указав на уже работающий мост соседнего продукта. Проверено на копии конфига (macOS 26.6.2): `plutil` сохраняет чужие серверы и посторонние ключи, а на неразбираемом файле возвращает 1 и файл не трогает — то самое поведение «падать, а не чинить», ради которого прежняя команда и была написана. Дословная формулировка владельца не приводится (D073) | owner |
| D104 | 2026-09-07 | **Аудиторам вводится правило: зону называть словами всегда** — «горячий цех, пицца-печь грязная». Владелец проговорит это с коллегами при передаче инструмента. При этом система обязана знать привязку сама и не опираться на дисциплину: **у части оборудования зона жёсткая** (пицца-печь, линия начинения, станция закусок, станция упаковки — только горячий цех), у части нет (стеллажи бывают в разных цехах, там нужно уточнение). Отсюда: словарь «объект → зона» ведётся как данные, нераспознанные формулировки накапливаются из проходящих проверок и разбираются на пополнение словаря. Задачи — #218, #225, #226 | Проверено замером 07.09.2026, почему нельзя выводить зону из пересечения зон пунктов: «стеллаж» даёт единственную зону `dry_storage` — то есть выглядит однозначно и при этом неверно по словам владельца. Ложная однозначность хуже отказа: она не спрашивает. Значит жёсткость привязки задаёт человек в данных, а вывод из пунктов годится только как кандидат на пополнение | owner |
| D105 | 2026-09-07 | **Аудитора приводит приглашение по ЮЗЕРНЕЙМУ, а пускает по-прежнему числовой ID.** Список приглашений ведётся окружением стенда (`BOT_INVITES`), позже — серверной логикой сервиса проверок; ради этого источник спрятан за протоколом `InviteSource`, и подмена его не задевает проверку доступа. Приглашение срабатывает РОВНО ОДИН РАЗ: юзернейм владелец отпускает в любой момент, и занявший его посторонний иначе получил бы отчёты партнёров и историю проверок. Связки «юзернейм → ID» лежат в `STATE_DIR`, а не в базе — по принципу «отказ базы не мешает аудитору работать» (`src/bot/app.py`). Задача — #230 | Владелец, дословно: «возьми в работу вот эту доработку, чтобы определять айди после клика по старту или любой другой команде», и отдельно про источник списка: «список сейчас, потом решаем инетреснее. не забывай что у на сбудет целый сервис по проверкам и серверной логикой». Причина в том, что числа собрать нечем: Bot API не отдаёт ID по юзернейму, а swarm-brain их через MCP не отдаёт ни одной проекцией (проверено на `get_users`, `get_tasks`, `list_entries`) | owner |
| D106 | 2026-09-07 | Продукт называется **DECIMUS**. Имя здесь не заменяется, а появляется впервые: сейчас бот представляется описанием («Бот выездных проверок»), а отчёт озаглавлен «Отчёт о проверке пиццерии». Имя вводится в профиль бота, приветствие и шапку отчёта. Слово «Аудит» в имени файла отчёта НЕ трогается: от его длины посчитан бюджет байтов обрезки (T128, issue #103) | Владелец: «пока полностью под 0 не переименовываем, только визуальные части при контакте с пользователем. MAXIMUS — ПНЛ 🏛️, DECIMUS — Аудиты, MERIDIUS — Чек-листы». Разбивка по местам, ловушка с бюджетом имени файла и открытые вопросы (менять ли @username) — issue #231 | owner |
| D107 | 2026-09-09 | **Бот сейчас не трогается вовсе** (профиль в BotFather, `@username`, приветствие) — не горит явно, будет отдельным решением. **DECIMUS — имя всей системы (сервиса), а не персона бота**; у бота в будущем появится собственное имя. Сужает D106: из таблицы поверхностей остаётся только шапка отчёта партнёру (`engine/report.py:31`, `:56`) — отчёт представляет систему, а не бота. Закрывает все три открытых вопроса issue #231 (менять ли `@username`, куда ставить имя в приветствии, есть ли у DECIMUS эмблема для аватара бота) как неприменимые, пока бот не тронут | Владелец, дословно: «1. бота не трогаем пока, это не горит явно 2. фиксируем что ДЕЦИМУС это имя всей системы. у бота в будущем будет свое.» | owner |
| D108 | 2026-09-09 | **Ребрендинг DECIMUS расширяется на инфраструктуру: репозиторий на GitHub и локальная папка проекта переименовываются.** Это расширяет D106/D107 (которые ограничивали ренейм видимым человеку) — теперь под ренейм попадают и не-видимые пользователю технические имена верхнего уровня. Обязательное условие: переименование не должно ничего уронить или потерять — проверить git remote, локальные git worktree (их на момент решения около 30, часть — мёртвые остатки завершённой стройки), боевой деплой на MUSPELHEIM (`C:\projects\dodo_audit_service`, контейнеры `dodo_audit_service-infra-*`), имя Python-пакета (`pyproject.toml`, `src/mcp/rpc.py:64`), `docker-compose.yml` (`name:`, `POSTGRES_DB`), тесты и доки (`docs/08-deploy.md`, `scripts/deploy.sh`, `scripts/smoke.sh`). **Функции, классы, имена модулей и переменных — не горит**, массово не переименовываются этим заходом; но новый код лучше ориентировать на новое имя | Владелец, дословно: «Гитхаб, папку и все такое надо переименовать. но проверить что ничего нигде не отпадет и не потеряется. Функции классы и все такое - пока не горит, но новые лучше придерживаться к новому названию конечно» | owner |
| D109 | 2026-09-09 | **Балл в отбивке о сохранении не показывается.** Развилка Q042 (между примером D090, где отбивка показана с «минус один балл», и запретом D072 показывать процент по ходу обхода) закрыта в пользу D072: граница «никакой оценки во время обхода» распространяется и на отбивку после каждой записи, не только на общий процент. Блок bot18, наткнувшийся на развилку, уже сделал отбивку без балла — решение подтверждает этот выбор, а не меняет его | Владелец, дословно: «нам не надо текущий прогресс показывать, зачем? это ж не приложение, а бот» | owner |
| D110 | 2026-09-15 | **Пуш в публичный main разрешён без ночного окна, пока продуктом никто не пользуется.** Владелец дословно: «пуш то что надо запушить / проектом никто не пользуется». Решение снимает для этого проекта требование ночного окна раскатки и требование отдельного «да» на каждый пуш — на время, пока у продукта нет живых пользователей. | Ночное окно защищает живых пользователей от поломки посреди рабочего дня. Пользователей нет, защищать некого, а задержка пуша при этом стоит реального — работа живёт только на одной машине и гибнет вместе с ней. Условие названо владельцем явно и проверяемо: появятся пользователи — решение перестаёт действовать. | owner |
| D111 | 2026-09-15 | **Пункт «Установка MCP» присылает одну короткую строку, а не команду установки.** Прямым следствием: пункт настраивает **Claude Code** (`claude mcp add --transport http` с заголовком `Authorization: Bearer`), потому что наш сервер говорит по прямому HTTP-транспорту MCP — проверено живым подключением 15.09.2026, `✔ Connected`. **Claude Desktop этим путём не обслуживается:** приложение удалённый сервер по HTTP не умеет, ему нужен мост, и возвращать ради него простыню владелец отказался. Мост и сборщик его команды из продукта не удалены и покрыты тестами — понадобится Desktop, это отдельное решение. Дословная формулировка владельца не приводится (D073). Задача — #244. **Уточнено D112:** форма установки взята у соседнего продукта, Claude Desktop снова обслуживается | Владелец на живом прогоне потребовал того же, что сделано в соседнем продукте: короткая команда, которая просто прописывает сервер в настройки клиента, — и назвал прежние сорок строк с телом моста внутри усложнением. | owner |
| D112 | 2026-09-15 | **Установка MCP делается ровно так, как в соседнем продукте: публичный GET отдаёт bash-установщик, бот присылает одну строку `curl … \| DODO_MCP_TOKEN=… bash`.** **Claude Desktop снова обслуживается** тем же скриптом через мост: D111 в части «Desktop не поддерживаем» отменено. **Форма `"type":"http"` в файле настроек Desktop запрещена:** приложение её не понимает и молча стирает весь блок `mcpServers` целиком (anthropics/claude-code#37286). Задача — #244 | Владелец потребовал посмотреть, как это работает в swarm-brain, вместо того чтобы выбирать из предложенных мной вариантов — и там это решено именно так (`docs/SETUP_CLAUDE_DESKTOP.md`, `supabase/functions/swarm-setup/`): скрипт вынесен отдельным файлом ради `bash -n` и тестов слияния конфига, токен приезжает окружением, а не в `argv`. Наше единственное отличие — установщик настраивает и Claude Code (прямой HTTP, без моста), потому что наш сервер это умеет. Запрет формы `"type":"http"` поймано на живом отказе в соседнем продукте | owner |
| D113 | 2026-09-16 | **Замер качества разбора по комментарию прогоняется по ключу владельца.** Владелец дословно: «проверяй». Оснастка `tools/bench_run.py` идёт по 34 обращениям к модели, меньше минуты; результат ложится рядом с прежними числами D013. | Непокрыт был ровно один риск — станет ли выбор модели внутри того же перечня хуже без картинки. Оставить его неизмеренным означало бы принять правку D081 на веру. | owner |
| D114 | 2026-09-16 | **Публичный адрес площадки, уехавший в историю публичного репозитория, оттуда не убирается: история не переписывается, репозиторий остаётся публичным.** Владелец дословно: «забить пока, все равно живых пользователей нет». Из рабочего дерева имя убрано, новых упоминаний не добавляется. | Скрыть постфактум всё равно нельзя — история правок публична. Переписывание истории сломало бы клоны, включая копию на площадке, а перевод в приватные сделал бы минуты Actions платными при стороже, который ходит каждые 15 минут. За адресом стоит дверь с токеном: раскрыта точка входа, а не доступ. | owner |
| D115 | 2026-09-16 | **Служебный префикс класса из методики снимается и в отчёте партнёру, не только с кнопки.** Владелец дословно: «не понимаю о чем речь, сделай как рекоммендуешь» — то есть принята рекомендация снять. Формулировкой записи остаётся вопрос пункта методики, но без внутренней пометки. | Префикс внутренний и партнёру не значит ничего, а на боевой методике так размечен 31 пункт из 136 — партнёр это видит. | owner |
| D116 | 2026-09-16 | **Старые цитаты прямой речи владельца в комментариях кода не вычищаются; закрывается только приток — вместо цитаты ставится ссылка на номер решения.** Владелец дословно: «а что там за прямая речь? если убрать из публичной все равно не можем, то пофиг». | Чистка тронула бы 53 файла и из публичной истории цитаты всё равно не убрала бы. Приток закрыть дёшево, и он единственное, на что ещё можно повлиять. | owner |
| D117 | 2026-09-16 | **Ответ, пришедший в канал уведомлений, не считается разрешением расширить права диспетчера.** Владелец не возражал против запроса к базе площадки, но выполнить его не удалось: среда блокирует чтение боевой базы, и попытка записать ответ из канала как разрешение была отклонена там же. Ограничение снимается только правилом в настройках среды или прогоном самого владельца — задача T203 продолжает ждать числа. | Канал — транспорт, а не удостоверение личности: сообщение в нём написано кем угодно, у кого есть доступ к боту, и повышать по нему собственные права нельзя. Это ровно то, от чего защита и стоит. | auto |
| D118 | 2026-09-16 | Память о прошлой зоне остаётся снятой (T264), зона берётся из слов аудитора, затем из словаря объектов, затем спрашивается. Новые замеры (точность 87%, быстрый путь 35%, неверных записей ноль) принимаются как честная база; прежние 94% помечаются как снятые с подсказкой правильного ответа и базой сравнения не служат. Q048 закрыт этим решением | Владелец в канале: «я не понимаю в чем проблема у нас с поиском? если должный путь работы с системой это конкретная фиксация: горячий цех, просрочка или холодный цех, грязные полы и стены — и система берет это все, и сравнивает с чеклистом, находит совпадения и применяет. я не вижу в чем мы тут так сильно затупили». То есть развилки он в этом не видит: путь именно такой, как заложен в T263-T267, а угадывание зоны по прошлой записи в нём лишнее. Ответ пришёл не реплаем, адресность каналом не подтверждена — прочитан по смыслу и сверен с владельцем отдельным сообщением | owner |
| D119 | 2026-09-16 | Сопоставление формулировки с методикой копит синонимы: если аудитор сказал иначе или с ошибками и ПРЯМОГО совпадения не нашлось, разобранная формулировка складывается в базу как синоним пункта и работает при следующем поиске. Прямое совпадение синонима не заводит. Отдельно подтверждено, что возможность уточнить у аудитора остаётся (закрыто T266) | Владелец в канале: «да, но должна быть возможность для уточнения. + если пользователь формирует факт как-то по другому или с ошибками, мы должны это учитывать и складывать в базу как синоним. но толкьо если не нашли прямое совпадение». Это дополнение к D118: путь «сказал — сопоставили — применили» подтверждён, но система обязана учиться на непрямых попаданиях, а не переспрашивать одно и то же каждый раз | owner |
| D120 | 2026-09-17 | **Аналитика — по процессам (направлениям), а не по секторам пиццерии.** Разрез по зонам (горячий цех, холодный цех и прочие) остаётся там, где он нужен по делу — адрес, куда идти чинить, и обнуление зоны по D3, — но сводная картина и в итоговом отчёте, и на будущей странице аналитики строится по семи процессам методики. Классификатор для этого уже есть в `data/checklist.csv` (колонки `process_ru`/`process_en`): 136 пунктов, семь процессов, покрытие полное, состав совпадает с тем, что показывает Qualon. Открытая развилка, требующая ответа владельца до реализации: по какой шкале считать процент процесса — доля выполненных пунктов внутри процесса (как Qualon) или вклад процесса в потерянные проценты итога | Владелец, дословно: «хочется аналитику именно по категориям, направлениям. не по секторам пиццерии (гц, хц и прочее) а по процессам (чистота, менеджмент, и че там еще». Повод — сравнение нашего отчёта с отчётом Qualon на скриншотах | owner |
| D121 | 2026-09-17 | **Процент по процессу считается по нашей методике — вкладом в потерю итога, не долей выполненного внутри процесса.** Закрывает развилку, оставленную открытой в D120: шкала Qualon («выполнено 70.97% работы с продуктом») отклонена, берётся вклад: сколько процентов итоговой оценки потеряно из-за нарушений этого процесса. Свойство, которое шкала обязана сохранять: сумма вкладов всех процессов равна общей потере, то есть `100% − итог`. Итог по процессам показывается в двух местах — в отчёте о проверке и на сайте админки | Владелец, дословно: «считаем по нашей методике», «просто нужно показывать итог по процессам в отчете и на сайте нашей админки» | owner |
| D122 | 2026-09-17 | **Повторная отправка уже выданного отчёта — нужна как функция продукта; это не правка отчёта и не его отзыв.** Три разных действия, которые нельзя смешивать: править записанную проверку нельзя (запрет остаётся в силе), отзывать её через `retract_inspection` — крайняя мера, необратимая для партнёра и уничтожающая фото, а **повторная отправка** безобидна: тот же документ, тот же расчёт, та же версия методики, по которой проверку считали, просто доставляется заказчику ещё раз. Сейчас такой операции в продукте нет: `inspection_letter` отдаёт текст письма человеку, чтобы тот вставил его в почту руками | Владелец, дословно: «стоп. отправить отчет повторно надо сделать. мы говорили что нельзя править отчет, но отправка нужна» | owner |
| D123 | 2026-09-17 | **Формулировки методики в публичном репозитории считаются приемлемыми: репозиторий остаётся публичным, история не переписывается, цитаты из документов не вычищаются.** Задача T288 (расширить проверку на утечку методики на `.md`) снимается как неактуальная. Проверка на утечку в коде и тестах остаётся как есть — её никто не отменял | Владелец на Q054 дословно: «Плевать , нет тайны» и «Плюс ещё меняться будет», на повторный вопрос — «похую». То есть охраняемой тайны в формулировках чек-листа нет, а меняющийся текст вычищать из истории тем более незачем. Вариант (а) — приватный репозиторий — отпадает вместе с причиной: у публичных минуты Actions бесплатны, а CI проекта тяжёлый | owner |
| D124 | 2026-09-17 | **`AUDIT_DATA_DIR` в рабочем `.env` возвращается на `./data` в репозитории.** Внешний каталог владельца не трогаем и не дополняем. Тем же заходом бот на старте проверяет полноту методики и падает с внятным сообщением, а не молча (T277) | Владелец на Q050 дословно: «опять, уже второй раз. я не понимаю вопроса. Реши сам.» Решаю по дефолту: в репозитории лежит полная методика, на ней зелены и 2821 тест, и регресс (97.5% A, 97% A). Внешний путь давал падение живого запуска при молчащих тестах — то есть настройка, которую никто не проверяет. Осознанным он быть не переставал: если владелец ведёт методику снаружи, он вернёт путь сам, но тогда каталог придётся дополнить, и проверка на старте это скажет вслух | auto |
| D125 | 2026-09-17 | **Зона строке карты кадров ставится отдельным инструментом MCP `set_photo_cue_zone`, а не расширением `edit_photo_cue`** (вариант «в» из Q055) | Владелец на Q055 ответил встречным вопросом про то, как выделяются зоны, и требованием гибко настраивать чек-лист — выбор формы оставил мне. Беру рекомендацию, которая и была в вопросе: заполнение зон предстоит массовое, а смешивать его с правкой кодов в одном вызове значит потерять границу между «поправил код» и «переназначил зону». Требование про гибкую настройку чек-листа — отдельная работа, заведена задачей | auto |
| D126 | 2026-09-17 | **Маршрута обхода от управляющей компании нет и не будет — порядок остаётся порядком методики.** `data/route.csv` живёт как есть, механизм T061 не трогаем: настоящий маршрут, если УК его однажды задаст, это подмена одного файла данных без правки кода. T133 снимается как неактуальная | Владелец на перезаданные Q051, Q052, Q053 ответил дословно: «фурка 123 займись решением» — то есть по всем трём пунктам выбор отдан мне. Q051 задавался дважды, и данных не пришло ни раз: задача, ждущая их вечно, засоряет граф и на каждой сверке выглядит незакрытой работой. Механизм при этом проверен и остаётся — цена возврата к маршруту нулевая, поэтому снятие задачи ничего не закрывает навсегда | auto |
| D127 | 2026-09-17 | **Прежний рецепт отпечатка снимается по машинному вердикту, а не по словам.** Доказательство берёт `src/db/recipe_audit.py` там, где база живёт: шаг «прогнать `python -m src.db.recipe_audit` и записать вердикт» встраивается в порядок наката на площадку (`docs/08-deploy.md`), а T203 переводится из блокировки вопросом в зависимость от наката T304. До вердикта «снимать МОЖНО» на всех трёх базах совместимость остаётся жить — она безвредна, необратимо только снятие | Владелец на перезаданные Q051, Q052, Q053 ответил дословно: «фурка 123 займись решением» — то есть по всем трём пунктам выбор отдан мне. Вопрос владельцу снимается совсем, а не отвечается: он просил число, которое по построению знает машина, и оба захода за ним ушли в слова («команду прогнал» без вывода, затем «не понимаю вопроса»). Разовое чтение боевой базы правилом в настройках не нужно — модуль печатает вердикт и счёт строк, а не данные проверок, и роль с отключённой построчной политикой он требует сам | auto |
| D128 | 2026-09-17 | **Формулировка вопроса методики словами аудитора не выдаётся нигде** (вариант «а» Q053): при выборе пункта по кадру без комментария в запись идёт ссылка на пункт, а не текст нормы — тем же порядком, каким блок engine-fix уже починил письмо (T248, «пункт стандарта «…»»). T287 делается по этому решению | Владелец на перезаданные Q051, Q052, Q053 ответил дословно: «фурка 123 займись решением» — то есть по всем трём пунктам выбор отдан мне. Вариант «б» оставлял бы партнёру утверждение нормы («продукты размораживаются по стандарту») на месте зафиксированного нарушения, а правила формулировок требуют обратного: в поле формулировки идёт факт и ничего кроме факта (`docs/03-recording-rules.md`). Плюс письмо и отчёт снова разошлись бы на одних данных — ровно тот дефект, который чинила T248 | auto |
| D129 | 2026-09-17 | **Режим проверок разделяется по цене ошибки, журнал блока заменяется файлом состояния.** Четыре части: (1) на блоке гоняется затронутое плюс прогон движка на `examples/belgrade-1` и `belgrade-2` (0,037 с), полный набор — один раз на волне, а не на каждой приёмке; (2) требование «80% покрытия и TDD» перестаёт быть равномерным: плотно покрывается то, где сбой молчит (движок оценки, правила формулировок, данные методики), громкий сбой в обвязке (хендлеры бота, роутеры, инструменты MCP) проверяется запуском, тексты и интерфейс юнит-тестами не покрываются; (3) журналы блоков `docs/furca/blocks/*.md` (6358 строк — втрое больше всей продуктовой документации) заменяются коротким файлом состояния на блок, хроника берётся из `git log`; коммиты не сокращаются — они бесплатны; (4) тесты, переписанные в «переборках» под изменение поведения (T264, T271, T290 — 27 падений за один заход), удаляются, а не чинятся | Владелец дословно: по режиму тестов — «ниче не понял, но ок»; по удалению тестов-налога — «тоже ок»; по документации — «в чем проблема коммитов если они помогают сохранять прогресс? сами комиты разве требуют лимита нейронки? если можно срезать без потери качества - давай резать». Замер, вызвавший разговор: за неделю 10–17.09 в репозитории 57 176 строк тестов против 36 229 строк продукта (1,58:1), из 231 коммита продуктовых `feat` — 27 (12%), треть коммитов `fix`/`test`/`style` чинит оснастку проверок, а не продукт. Возражение владельца про коммиты принято как верное: коммит не стоит ничего, платится генерация журнала и его перечитывание целиком при каждом продолжении после обрыва по лимиту. Журнал блока при этом дублирует `git log`, который хранит ту же хронику точнее | owner |
| D130 | 2026-09-17 | **Стройка decimus остановлена: фокус уходит в оснастку проверок вместо продукта.** Новых заходов и блоков не начинать; T308 (срезать журналы блоков), T309 (удалить тесты-налог) и T310 (`make porcha`) не начинать. Планка числа выполненных тестов из T306 (2885 в `pyproject.toml`) перестаёт быть тем, на что опираются в отчётах: она цементирует ровно тот налог, который T309 велит удалять. Снимать её сейчас не нужно. Разбор самой системы идёт отдельно (GarroV/furca#131) | Владелец дословно (17.09, в разговоре с диспетчером furca, передано сессией furca-ba — в этом разговоре фраза не звучала): «тормози работу, и запускай рефактор фурки. надо жестко пройтись и проанализировать по настройкам агента. понять где работа сбоит, и почему мы превратились в конвейер тестов, вместо того чтобы быть конвейером по сборке проектов. фокус явно сместился, и мы теряем время в угоду неизвестно чего». Замер за неделю: тестов +8206 строк против +6271 продукта, docs-коммитов 102 из 240, волна не разворачивалась два захода подряд (in_progress ноль). При этом T060/T062 (режим проверки по чек-листу — ядро продукта) и T302 (прямое требование владельца из Q055) стоят нетронутыми | owner |
| D131 | 2026-09-17 | **Парадигма системы: пока идёт работа, проект строится.** Пять пунктов, принятых владельцем для FURCA и обязательных для этого проекта. (1) Каждая волна даёт кусок продукта, который можно открыть и показать; заход, не сдвинувший продукт, — сбой захода, два подряд — остановка и вопрос владельцу. (2) Служебные задачи (оснастка, форматы, журналы, гейты, переезды на новые правила системы) не берутся молча: копятся списком и выносятся владельцу — что накопилось, что предлагается, насколько подождёт продукт. Без жести: мелочь по дороге к продуктовой задаче делается походя и упоминается строкой в отчёте; сломанное (красный прогон, упавший стенд, битая миграция) чинится сразу как блокер стройки. (3) Тесты — там, где ошибка дорогая, и сразу (движок оценки, правила формулировок, права доступа, данные методики); обвязка — бот, MCP, хендлеры — проверяется живым запуском. Тест, не способный поймать ошибку, не пишется, найденный удаляется. **Число выполненных тестов не является мерилом прогресса** и в отчёт первой строкой не идёт. (4) Отчёт открывается тем, что пользователь теперь может сделать, чего не мог; числа прогона ниже, как подтверждение «не сломали». (5) Мерило волны — работающий сквозной сценарий, а не принятые блоки и зелёный прогон. Дополняет D129 и заменяет собой опору на планку из D130 | Владелец дословно: «основной акцент что ПРОЕКТ ДОЛЖЕН СТРОИТЬСЯ. все время работы фурки ПРОЕКТ ДОЛЖЕН СТРОИТЬСЯ. Ради этого мы собирали его», «давай договоримся что СЛУЖЕБНЫЕ задачи идут через запрос пользователя, если это чтоб-то важное», «не жестим, конечно. в рамках разумного чиним сами себя, но фокус на сборку продукта», «Про тесты я согласен. но опять же, мы не должны строить тесты ради тестов». Внесено в систему коммитами GarroV/furca 7657320 и 26d6f0a, разбор — GarroV/furca#132. Правила системы применяются сами, артефакты проекта — нет (см. D101 соседнего проекта), поэтому решение записано здесь | owner |
| D132 | 2026-09-18 | **Чек-лист — эталон управляющей компании; партнёр его не правит, но видит исходник.** Ответ на Q056 закрывает вопрос не тем разрезом, каким он был задан: правка `data/scoring.json` (ставки вычетов, пороги букв, потолки сроков) и состава чек-листа остаётся у УК, потому что УК и есть автор эталона, а не арендатор, которому что-то «отдают». Партнёру полагается **чтение исходника** — он должен видеть, по какому эталону его проверяют. Что партнёр делает со своей стороны дальше — его дело и не предмет продукта. T302 разблокирована: объём первой версии — довести настройку состава для УК (доля зоны, переименование, порядок обхода — T313) и дать партнёру чтение эталона (T315) | Владелец дословно: «Мы как ук создаем чеклисты для наших проверок, они являются эталонами. То что будет делать партнер - на совести партнера, но базово он должен будет видеть исходник». Мой вопрос был задан с перевёрнутой ролью — я спрашивал, отдавать ли правку «управляющей компании», считая её клиентом-арендатором, тогда как по спеке (`spec.md`, строка 7) УК — это сеть, которая проверяет пиццерии партнёров, то есть автор методики. Ответ владельца эту путаницу и снял | owner |
| D133 | 2026-09-18 | **Веб-админка строится по визуальному эталону владельца, а объём спринта — только базовая часть веб-версии.** Эталон прорабатывается отдельно (Claude Design) и придёт готовым; подвязываться к нему, а не рисовать своё. Вся веб-версия в спринт не входит | Владелец дословно: «Админка , ее визуал вернее, сейчас в проработке с клод дизайном. я ее принесу как эталон, к которому нужно будет подвязываться. просто веб версия очень большая, и от нее нам надо будет только базовую базу, которую определили для спринта» | owner |
| D134 | 2026-09-18 | **Годовой календарь проверок идёт отдельно от спринта.** Задача #279 из спринта снята, метка убрана; формулировка с доски остаётся обрезанной до уточнения | Владелец дословно: «годовой календарь это отдельно» | owner |
| D135 | 2026-09-18 | **Источник объёма — доска, согласованная с командой; спринты в Swarm Brain считать экспериментом, а пул задач проектов в Swarm — не спринтом.** Расхождение между пулом Swarm и доской — ожидаемое состояние, а не рассинхрон, который надо чинить | Владелец дословно: «Задачи с доски со скриншота - это то что с командой решили. спринты из сворма эт оэксперимент, котоырй точно поменяется. задачи из проектов в сворме это общий пул задач, которые могут идти в расход со спринтом» | owner |
| D136 | 2026-09-18 | **Визуал: раскладка из прототипа, стиль из дизайн-системы линейки.** Из папки эталона (`Бриф системы аудитов Decimus 2/`) берётся структура экранов, состав колонок, состояния и поведение; цвет, шрифт и компоненты — из `GarroV/forma` (`dodo/core/dodo-ds.css` + `dodo/decimus/domain.css`, где классы D1–D3, буквы A–D, состояния предписаний и «предложение модели / подтверждено» уже заведены). Палитра `#5B57F2` и пара Space Grotesk / Manrope из прототипа в продукт не переносятся. Задача #273 этим и закрывается. Копия `dodo-ds.css` в папке эталона разошлась с каноном — сверять с `forma`, не с папкой | Владелец на вопрос о разводе прототипа и общего ядра выбрал «Раскладка из прототипа, стиль из forma». Причина сохраняется из README `forma`: пользователь у MAXIMUS, DECIMUS и MERIDIUS общий, и «синее нажимается» он не должен переучивать трижды | owner |
| D137 | 2026-09-18 | **Разрез по процессам добавляется рядом с зонами, а не вместо них.** Зоны остаются там, где они по делу: адрес «куда идти чинить» и обнуление зоны по D3. Сводный разрез по семи процессам методики (`process_ru`/`process_en` в `data/checklist.csv`) добавляется вторым — в отчёте о проверке и в вебе, шкалой из D121 (вклад процесса в потерю итога, сумма вкладов = `100% − итог`). Прототип на этом месте дорисовывается, а не переделывается: слова «процесс» в нём нет ни разу | Владелец на вопрос о разрезе аналитики выбрал «Добавить процессы рядом с зонами». Исполняет D120 и D121, которые прототип не отразил | owner |
| D138 | 2026-09-18 | **Непостроенные части веба показываются явно нерабочими, а не выглядят готовыми.** Раздел, который ещё не построен, помечается в навигации лентой «в разработке» (по образцу строительной), а при входе даёт явный экран «ещё в разработке» вместо пустого или декоративного содержимого. Альтернатива, названную владельцем допустимой, — скрывать непостроенное совсем; берётся первый вариант, потому что скрытие лишает эталон карты продукта и не отвечает на вопрос «где остальное». Требование распространяется на все части: самопроверка партнёра и кабинет партнёра рисуются, но не строятся (форма данных — тип проверки, аудитория чек-листа, связь партнёр↔точка — закладывается сразу) | Владелец дословно: «рисуем но не строем. все блоки веба которые сейчас не работают, давай прям видимо выделим как нерабочие (нарисуй сверху что-то типо ленты, как на стрйоках или ченить такое. ну и при переходе в блок который еще не рабочий , прямо пишем что еще в разработке)» и «можно отобразить в вебе, но пока явно показать что в разработке. ну или можно скрывать то что мы еще не построили, чтобы не путались люди» | owner |
| D139 | 2026-09-18 | **Планирование проверок и календарь в текущий объём не входят.** «Назначить проверку», плановая проверка как сущность и раздел «Календарь» из прототипа показываются помеченными как в разработке (D138), но не строятся. Дополняет D134, где годовой календарь был вынесен отдельной работой, и распространяет то же решение на планирование, которое с календарём связано. Требует сверки с доской: задача #278 «Календарь проверок: логика и наложение на систему» помечена «спринт 1» — либо метка снимается, либо в спринте остаётся только проработка логики без экранов | Владелец дословно: «планирование тесно связано с календарем, но сейчас не берем». Отдельно: «Тут как раз таки важно свериться что строим по спринтам, у нас сейчас есть фокусные вещи» | owner |
| D140 | 2026-09-18 | **Зона — произвольная группа пунктов со своей долей, а не обязательно помещение.** Зона остаётся единицей методики: доля в процентах, обнуление по D3, адрес «куда идти чинить». Но её состав не обязан быть комнатой: группа вида «Документы» законна, и управляющая компания настраивает состав зон под свой чек-лист. Порядок обхода точки живёт отдельно от состава зон (D126: маршрут — файл данных). Слово в коде, данных и отчётах остаётся «зона»; «блоком» в прототипе назван переиспользуемый набор пунктов из библиотеки — ему нужно другое имя, чтобы одно слово не значило два разных предмета | Владелец на вопрос, помещение зона или произвольная группа, выбрал «Произвольная группа с долей». Повод: в прототипе 12 «блоков» с другим составом и неравными долями против 10 зон по 10 % в `data/zones.csv`, среди них «Документы» — не помещение вовсе | owner |
| D141 | 2026-09-18 | **Иерархия: кластер → страна → пиццерия; тенант заводится на страну партнёра.** Кластер — набор стран, объединённых по признаку. Страна в рамках IMF закреплена за конкретным партнёром. В стране — пиццерии. Управляющая компания делает единый чек-лист УК, который распространяется на кластер. Партнёр может подключиться к системе — тогда для него создаётся отдельный тенант на его страну: он ведёт свои чек-листы и одновременно пользуется чек-листами УК. Аналитика при этом общая. Слово «арендатор» в разговоре не используется: в базе таблица называется `tenants`, и это имя остаётся техническим — в интерфейсе и документах говорится «тенант», «кластер», «страна», «пиццерия» | Владелец дословно: «есть кластер (набор стран, объединенные по какому-то признаку), есть страна - в рамках ИМФ закрепленная за конкретным партнером, в стране есть пиццерии. ЧТо за термин арендатор ты выдумал - я не представляю. соответственно мы как УК делаем единый чеклист ук, который распространяется на кластер. а партнеры могут подключиться к системе, в этом млсучае для них будет создан отдельный тенант для страны, они могут делать свои чеклисты + пользоваться теми же что у УК. АНалитика общая. тут кажется могт быть еще вопросы в глубине». Моя рекомендация «страна и город — атрибуты точки» отклонена: страна — настоящий уровень, к ней привязан партнёр | owner |
| D142 | 2026-09-18 | **Повтор нарушения стоит вдвое: удвоение вычета возвращается в работу.** По итогам проверки с нарушениями партнёр обязан сделать план исправления. Если на следующей проверке фиксируется то же нарушение, что было описано в прошлый раз, вычет за него удваивается. Отменяет ту часть D029, где логика удвоения была отложена на следующий год, а неисправленное считалось только невыполнением плана действий. Связь повторной проверки с исходной в схеме уже заложена — теперь она несёт расчёт, а не только ссылку. Требует уточнения на реализации: удваивается ставка вычета (D1 0,5 → 1 п.п., D2 2 → 4 п.п.), а под повтором понимается совпадение по коду пункта с нарушением предыдущей проверки той же точки | Владелец дословно: «по итогам проверки, при косяках партнер обязан сделать план рисправления. если при повторной проверке мы фиксируем те же нарушения что были описаны в прошлый раз - значит мы снимаем х2 за это нарушение» | owner |
| D143 | 2026-09-18 | **Папка визуального эталона хранится в репозитории проекта.** `Бриф системы аудитов Decimus 2/` (3,9 МБ, артефакты Claude Design) кладётся под git рядом с кодом — тем же порядком, каким MAXIMUS держит свою дизайн-систему. Вынесенный в `workbench/` эталон разошёлся бы с продуктом молча, а найти его через полгода было бы нечем | Владелец дословно: «реши сам, главное не потеряй». Репозиторий публичный, но охраняемой тайны в методике нет (D123), демо-данные в прототипе вымышленные | auto |
| D144 | 2026-09-18 | **Сводная аналитика считается только по эталонному чек-листу.** По эталону УК проводятся все проверки, идущие в общую картину: проверки самой управляющей компании, проверки сторонней компании и самопроверки партнёров — поэтому их результаты сравнимы. Чек-листы, которые страна заведёт себе сама, в общую аналитику не попадают: для них нужна отдельная внутренняя статистика страны, и она не прорабатывалась — уходит в беклог, а не в текущий объём. Снимает развилку, которую я поднял при D141 (разные чек-листы несравнимы по пунктам): сравнивать по разным наборам не придётся | Владелец дословно: «аналитика только по эталонному чеклисту. по нему будут проводиться проверки УК, сторонней компании и самопроверки партнеров. НО важно предусмотреть что должна быть внутренняя статистика страны по тем чеклистам, что запустят они сами. мы это не прорабатывали, заложи пока в беклог» | owner |
| D145 | 2026-09-18 | **Кнопки «подтвердить все предложения модели» в продукте не будет.** В прототипе она стоит в двух местах — на экране проверки («Подтвердить все») и в мини-аппе аудитора («Подтвердить все предложения модели»). Она предполагает режим, которого в продукте нет: аудитор якобы кидает фото пачкой, а разбирает предложения потом. В боте поток другой — фото, предложение модели, подтверждение на месте, поэтому очередь неподтверждённого не копится, и подтверждать разом нечего. Кнопка убирается вместе с плашкой «потенциально ещё N %», которая её обслуживает; правило «модель предлагает, фиксирует человек» остаётся без исключений | Владелец дословно: «где эта кнопка? как может получиться что предложений много? что за бред». Проверено по прототипу: строки «Подтвердить все» и «Подтвердить все предложения модели» в `Dodo Audit Prototype v2.dc.html`. Моё предложение помечать записи «подтверждено пачкой» снимается вместе с кнопкой — помечать нечего | owner |
| D146 | 2026-09-18 | **Репозиторий `GarroV/decimus` остаётся публичным.** Глобальное правило «репозиторий проекта по умолчанию приватный» (решение 20.08.2026) на этот проект не распространяется: публичность здесь осознанная | Владелец дословно: «репо не трогаем, он и должен быть публичным». Вопрос поднят при разборе набора тестов 18.09.2026 — в репозитории лежит методика аудитов и примеры проверок по конкретным пиццериям, и по умолчанию это выглядело как недосмотр. Записано, чтобы следующая сессия не переоткрывала вопрос и не перевела репозиторий в приватные «на всякий случай». Следствие, которое надо помнить: в issues этого репозитория не попадает ничего, что не должно быть публичным, а находка по безопасности идёт приватным advisory, а не задачей | owner |
| D147 | 2026-09-18 | **Для decimus ночное окно раскатки сейчас снято.** Раскатывать и проверять на живой площадке можно днём, без ожидания 23:00–06:59. Разрешение точечное: оно про этот проект и про этот период работы, а не отмена правила вообще — на других проектах окно действует как прежде. Требование явного «да» владельца при этом никуда не делось | Владелец дословно: «мсп что тебе мешает проверить? в рамках этог опроекта сейчас можно забить на ночное окно». Повод: проверка healthcheck звена (#286) требовала пересоздать `mcp` на площадке, то есть уронить продукт на минуту | owner |
| D148 | 2026-09-18 | **Веб-админка строится серверным рендерингом в том же образе, без Node и сборщика; конкретный фреймворк выбирает блок `web` с обоснованием.** Рамка: Python, тот же `docker-compose.yml`, что у бота и MCP; синхронный код, как весь остальной продукт; статика дизайн-системы кладётся копией с указанием версии-источника из `GarroV/forma`. Веб — читатель существующих дверей (`src/db/queries.py`, `src/db/retract.py`, `src/domain`), схему данных в первой волне не меняет | HTTP-стека в проекте нет вовсе: `src/web` не существует, бот на aiogram, MCP по stdio. Рамку задаёт диспетчер, потому что иначе два блока разошлись бы по стекам; выбор внутри рамки — инженерное решение блока, и блок пока один, конфликтовать не с кем. Node в образе отвергнут до выбора: дизайн приходит готовым CSS из `forma` (D136), а сборщик ради этого тянул бы вторую экосистему в продукт, который сегодня целиком на Python | auto |
| D149 | 2026-09-18 | **Покрытие снимается с `addopts` и уезжает в отдельную цель `make cov`, считается по ядру (`src/domain`, `src/report`), а не по `src` целиком.** В `addopts` остаётся только `--junitxml`, по которому работает сторож планки | Покрытие стояло по умолчанию, то есть считалось на КАЖДОМ вызове pytest — включая точечные прогоны по ходу работы, ради дешевизны которых и принималось D129. Замер 18.09: 323 с на 2959 тестах без покрытия против больше 13 минут с ним, то есть покрытие стоило примерно восемь минут из тринадцати, а читают его раз на волне. Замер проекта от 03.09 («115 секунд без покрытия») к этому дню устарел втрое и правится тем же заходом. Считать по `src` целиком было и неверно по существу: ориентир 80% объявлен конституцией для слоя молчаливого сбоя, а поверхность юнит-тестами не покрывается по стандарту — процент по всему `src` требовал бы ровно ту переборку, которую D129 велит удалять. Решение реализует намерение владельца в D129 («давай резать»), собственной развилки не содержит | auto |
| D150 | 2026-09-18 | **Состав чек-листа секретом не считается: папка визуального эталона остаётся в публичном репозитории, проверка утечки получает исключение ровно на неё.** Тревога диспетчера о том, что коммит `c4cbce2` вынес 20 формулировок методики в публичный репозиторий, снята владельцем. Остальная проверка не трогается: `data/` и `examples/` остаются вне git (D002) — там не только формулировки, но и фото реальных пиццерий с отчётами партнёров | Владелец дословно, в ответ на алерт 18.09: «что секретного в методике? это чек лист на данный момент, в будущем через админку он еще поменяется 10 раз, для этого и строим проект». Диспетчер поднял вопрос по правилу о раскрытии в публичном репозитории и по красной проверке `test_methodology_leak`; правило исходило из D073, где формулировки методики были названы тем, что в публичное не уходит. Владелец эту посылку снял | owner |
| D151 | 2026-09-18 | **Планка прогона разделяется надвое: общая и планка ядра (`core_test_floor` = 534 по префиксам `tests.test_engine_`, `tests.test_domain_`, `tests.test_report_`).** Общую опускать при удалении переборки законно, планку ядра — отдельным осознанным движением | Одна планка на весь набор путала два слоя, которые D129 развёл по цене ошибки. Под D129 поверхностная переборка подлежит удалению, каждое удаление роняет общее число, и правится это опусканием планки — вместе с которой молча опускалась и защита ядра: удалил полсотни тестов бота, опустил число на полсотни и не заметил, что в том же коммите перестал собираться `tests/test_engine_*`. Исходная претензия задачи #283 («планка цементирует переборку») при разборе не подтвердилась и снята: планка ловит то, чего не ловит проверка на пропуски, — молчаливую потерю сбора, когда `skipped` остаётся нулём. Проверено порчей, три случая: завышенная планка ядра — красный с внятной причиной; ключа нет — отказ «ядро не сторожится»; как есть — зелёный | auto |
| D152 | 2026-09-18 | **Артефакты визуального эталона (`.dc.html`) выведены из проверки на цитирование методики.** Отбор по расширению, а не по имени папки; код, доки и данные рядом с эталоном проверяются по-прежнему | D143 положил папку эталона под git, и проверка утечки дала 20 находок разом: эталон рисует экраны аудита, а на экране аудита названия зон стоят по построению — то же основание, по которому из проверки давно вынесены `docs/`. Проверка писалась против кода и документов, куда методика просачивалась цитатой, и третьей категории — визуального эталона — не знала. Владелец на вопрос о сенситивности: «какая проблема с эталоном? что не так?» — то есть публичность названий зон осознана и проблемой не считается (см. D146). Существеннее было другое: красную проверку пересказали как «остальные 2933 теста прошли», и гейт ушёл в main красным. Негативная проверка получилась сама: первая редакция комментария к правке называла три зоны для наглядности, и проверка покраснела на ней самой — цитату в `.py` она ловит по-прежнему, сузилось только расширение артефактов эталона | owner |
| D153 | 2026-09-18 | **Стройка decimus возобновлена; D130 («стройка остановлена, фокус уходит в оснастку») снят.** Фокус захода — продукт: то, что человек может открыть и показать, а не проверки и журналы | Владелец дословно: «ебаш дальше стройку». Прямое указание продолжать, данное в разговоре после того, как остановка была записана; условие остановки (оснастка вместо продукта) снимается тем, что волна берётся продуктовая | owner |
| D154 | 2026-09-18 | **Веб-админка получает доступ снаружи: её открывают другие люди, не только тот, у кого она поднята.** Механизм (туннель, funnel, публикация порта) выбирает стройка; сам факт выхода наружу — решение владельца | Владелец дословно: «и да, мне веб нужен именно с доступом для других людей, через фунел или что там». До этого доступ был открытым вопросом и держал админку на петле (`docs/12-web-admin.md`, раздел «Что дальше») | owner |
| D155 | 2026-09-18 | **Наружу админка выходит только после входа: сначала строится опознание человека и защита маршрутов, потом публикация.** Пока входа нет, публикации нет ни в каком виде, включая «временно на время показа» | Публикация без входа означает историю проверок всей сети по ссылке для каждого, кто её получил, — партнёры, точки, формулировки нарушений, имена аудиторов. Владелец решил вопрос доступа (D154), а порядок работ решается стройкой; тот же порядок у соседних продуктов сети: у maximus порт опубликован потому, что там есть логин | auto |
| D156 | 2026-09-18 | **Веб-админка живёт постоянно на MUSPELHEIM, и владелец разрешил её туда раскатать.** Дословно: «ебаш на муспельхейм». Ноутбук как постоянный дом отпадает: сон по крышке гасит админку молча, и снаружи это выглядит как сломанный продукт. Наружу она выходит путём на уже занятом Funnel-порту 10000 (свободных портов у Funnel больше трёх не бывает), корневой 443 не трогаем — площадка общая, там чужой продукт. | Решение D154 требует доступа для других людей, а доступ снаружи требует машины, которая не засыпает. | owner |
| D157 | 2026-09-18 | **Раскатка веба выполняется отдельной командой и только после двух условий: закрыт T325 (ограничение перебора) и наступило ночное окно 23:00-06:59.** Разрешение владельца из D156 относится к месту и к самому факту раскатки; порядок остаётся прежним, потому что публикация до T325 оставляет между посторонним человеком и историей проверок всей сети один пароль. | Стройка (fabrica) раскатку на целевую площадку не выполняет — это её граница; плюс требование ночного окна. | auto |
| D158 | 2026-09-18 | **Запертый вход открывается командой, а не только временем: делаем снятие запрета, а не принимаем цену.** Снимает роль администратора истории — у неё уже есть отдельное подключение, и снятие запрета по смыслу того же рода, что снятие проверки. | Разбор безопасности на приёмке T325 показал: посторонний, знающий логин администратора, держит его запертым сколь угодно долго одним запросом раз в ~70 минут, и пароль для этого не нужен. Развилка закрывается дефолтом: команда снятия дешева, а вариант «принять цену» оставляет живой продукт без способа восстановить доступ. | auto |
| D159 | 2026-09-21 | **Накат 0012/0013 на площадку выполнен днём, вне ночного окна, по прямому указанию владельца перед днём тестовых проверок.** Вместе с накатом пересобран образ из кода, уже лежавшего на площадке (`809b370`), — без `git pull`, чтобы не тащить на площадку веб-админку и её миграции `0014`–`0016`. Отпечаток `0007` перепечатан штатной командой `--reseal`: SQL в нём не менялся ни разу, расходились только комментарии (нормализованный отпечаток одинаков во всех четырёх версиях файла) | Владелец дословно: «Накатить сейчас, до проверок» — в ответ на вопрос, где вариантом по умолчанию предлагалось дождаться ночного окна, а цена дневной раскатки была названа. Повод: карта синонимов на площадке лежала с 18.09, каждая формулировка уходила в модель, разбор занимал 8–23 с вместо быстрого пути. Проверки в этот день проводит @Indira56, уже работавшая через бота в Тбилиси | owner |
| D160 | 2026-09-21 | **Веб-админка выходит наружу через Cloudflare, а не через Tailscale Funnel.** T324 переписывается с Funnel на Cloudflare Tunnel: коннектор сидит в сетевом пространстве контейнера `web` (приём D100, как у MCP), порт наружу по-прежнему не публикуется. Звено socat (`link`, профиль `funnel`) остаётся у MCP и веба не касается | Владелец дословно: «Начни работу по выкату в веб это все. клаудфлейр у тебя есть». Отменяет в части веба D102 («наружу выходит не туннель, а общий вход площадки»): D102 принят потому, что домена в учётке Cloudflare не было — владелец говорит, что Cloudflare есть | owner |
| D161 | 2026-09-21 | **Веб-админка катится на боевые данные днём, вне ночного окна.** Ночное окно раскатки (23:00–06:59) для этого пакета снято владельцем: схема `0014`–`0016` накатывается на боевую базу и админка переключается на неё сразу, не дожидаясь ночи. Пакет аддитивный: две новые таблицы и гранты, существующих таблиц бота не касается, бот не пересобирается и не перезапускается | Владелец дословно: «катим сейчас. проект в разработке, так можно делать. функционал бота я отдал коллегам, остальное их работы не касается. а мне нужен визуал чтобы понимать что там и как идет по стрйоке». То есть ограничение окна защищает работу аудиторов через бота, а этот пакет её не задевает: админка — отдельная поверхность, поднятая отдельным стендом | owner |
| D162 | 2026-09-21 | **На стенде обкатки вход admin/admin, требование длины пароля к нему не применяется.** Заслон «не короче 12 знаков» остаётся в продукте для всех, кто заводит учётки командой; учётке `admin` на этом стенде пароль ставится прямой записью в базу. Замена на настоящий вход — по слову владельца, не раньше | Владелец, трижды и дословно в последний раз: «я уже 10 раз сказал, что доступ админ админ… это всё для обкатки. мне нужно обкатывать, я не хочу каждый раз вспоминать, какой пароль я там ставил… когда нужно будет заменить, я к тебе приду и скажу, что всё, теперь делаем по-взрослому». Цена принята владельцем осознанно: за адресом боевые проверки, адрес публичный | owner |
| D163 | 2026-09-21 | **Вход в админку по учётке Google — нужен, но после текущей волны; домен в Cloudflare не заводится.** Делается OAuth ВНУТРИ приложения, а не Cloudflare Access: Access требует зону в Cloudflare, а её не будет. Круг допущенных — список почт, который даёт владелец. До тех пор вход паролем, `admin`/`admin` (D162) | Владелец дословно: «админку гугла надо, домен на клауде пока не планирую. запиши задачу, но это на потом. пока пароле обойдемся». Отменяет в части веба вариант с Access, предложенный вместе с ним | owner |
| D164 | 2026-09-21 | **Конструктор чек-листов раскатан на стенд днём, вне ночного окна.** Владелец дословно: «кати уже» — сказано после прямого напоминания, что окно раскатки 23:00–06:59 и что без отдельного «да» я жду ночи. Основание то же, что у D161: админка — отдельная поверхность, отдельный compose-проект и отдельный образ, боевые контейнеры бота и MCP не пересобираются и не перезапускаются, работа аудиторов не задевается | Ограничение окна защищает работу через бота. Этот пакет её не касается: пересобран только образ админки, схема базы не трогалась вовсе | owner |
| D165 | 2026-09-21 | **Хранилище версий методики у стенда своё, а не боевая полка.** `MCP_CHECKLIST_STORE` на стенде смотрит в отдельный том `stand-methodology`, боевые методика (`/app/data`) и полка снимков (`/app/state`) остаются примонтированными только на чтение. Следствие, которое надо знать: состав чек-листа на экране — боевой и настоящий, а правки ложатся в хранилище стенда и на бота НЕ влияют; опубликовать правку в боевую методику отсюда нельзя | Прямое чтение боевой полки не работает вовсе: хранилище создаёт каталог версий при первом обращении и упирается в read-only. Открыть полку на запись значило бы дать админке править методику, по которой считает бот, — то есть ровно ту работу аудиторов, которую бережёт D161. Владелец просил визуал, а не право менять боевую методику мимо MCP; когда такое право понадобится, это отдельное решение | auto |
| D166 | 2026-09-21 | **Хранилище файлов поднимается своё, на MUSPELHEIM, с расчётом на переезд.** Владелец дословно: «мы можем хранилище наладить пока на муспельхеймем? и потом перевести». Внешнее хранилище (R2, Supabase) не заводится сейчас; выбор боевого места откладывается до того, как будет что считать — настоящие проверки с десятками кадров | Переезд заложен в устройство: прослойка `src/db/storage.py` говорит S3-совместимым языком именно для того, чтобы смена поставщика стоила правки переменных окружения и ничего больше (D054, D061). Значит своё хранилище сейчас не создаёт долга, а снимает срочную беду: доказательства двух проведённых проверок висят на `file_id` телеграма (#318). Цена отсрочки известна и принята: файлы живут на домашней машине, которая уже лежала два часа после ребута, и бэкап площадки умеет только дампы Postgres — том с файлами в него не входит | owner |
| D167 | 2026-09-21 | **Схема и бот раскатаны днём, вне ночного окна.** Владелец дословно: «ебаш» — сказано в ответ на прямо названную развилку «скажешь накатывай — сделаю сейчас, скажешь ночью — сделаю ночью». Накачена миграция `0018`, пересобран образ, перезапущен бот | Ограничение окна бережёт работу аудиторов через бота, и здесь она проверена, а не предположена: последняя активность в состояниях чатов — 13 часов назад, ни одной идущей проверки нет, а прерванная переживает перезапуск по устройству (DoD MVP). Пакет аддитивный: новая таблица, существующих не касается | owner |
| D168 | 2026-09-21 | **Вход через Google настраивается сначала как Internal, партнёры — потом, отдельным заходом.** Владелец дословно: «у меня нету домена на данный момент, поэтому давай пока настроим только интернал. потом перенастроим». Приложение заводится в проекте `decimus-509320` организации `dodopizza.com`; на первом шаге входят только сотрудники, партнёрский вход переносится на заход с переключением типа на External. Цена переноса низкая: тип аудитории переключается в разделе Audience, Client ID, секрет и адрес возврата при этом сохраняются, код входа переписывать не придётся. Ограничение, названное до решения: Internal не пустит никого вне организации, то есть партнёра — по определению, а не по настройке | Своего домена нет, а чувствительный скоуп почты у опубликованного External-приложения требует проверки Google с подтверждением владения доменом — `ts.net` подтвердить нельзя. Internal проверку не проходит вовсе, поэтому вход сотрудников заводится сегодня и без домена | owner |
| D169 | 2026-09-22 | **У письма простой редактор форматирования, и уезжает оно в черновики Google, а не файлом.** Набор форматирования маленький и закрытый: жирный, курсив, зачёркнутый, гиперссылка — ровно чтобы текст можно было скопировать в почту со сохранённым видом. Вторая кнопка складывает то же письмо в черновики Google. Выгрузка простым текстом снимается: копирование из поля решает ту же задачу без файла. Отправка из системы в этот объём не входит | Владелец дословно: «Под редактором я подразумеваю, что нам нужна какая-то небольшая управление. Типа сделать жирным выдернуть, зачеркнуть, добавить гиперссылку. В общем, что-то такое для очень простого форматирования текста, чтобы потом, опять же, этот текст сформатированный можно было скопировать в Google, ну, либо по кнопке еще одной, перенести в Google черновики». Про файл: «скачать файлом, ну, честно, я не знаю, зачем его скачивать файлом, если мы отсюда можем скопировать». Про отправку: «в будущем у нас будет кнопка Отправить... поэтому сейчас пока не парься на эту тему» | owner |
| D170 | 2026-09-22 | **Письмо обезличенное, и уходит оно группе, а не человеку.** Обращения по имени в письме нет и не заводится: получателей всегда несколько — партнёр плюс люди команды, отвечающие за направления. Отсюда два списка рассылки: постоянный (владелец, его руководитель, коллеги — получают всё) и свой набор контактов по каждой стране. Поле «контакт» у проверки как имя для обращения теряет смысл (см. #330), а оговорка «не восстановлен контакт» уходит вместе с ним | Владелец дословно: «у нас есть партнеры, и обычно, как правило, мы пишем партнеру плюс отдельным людям, которые в команде отвечают за разные направления, в том числе операционные... сформируем, во-первых, список постоянных почт, на которые нам необходимо отправлять письма, то есть это я, мой начальник, мои коллеги... Плюс по каждой стране есть свой набор будет контактов, которым идет рассылка. То есть это не одно лицо, это как бы группа лиц. Поэтому и сообщения, по сути, мы тоже делаем обезличенное» | owner |
| D171 | 2026-09-22 | **Письмо остаётся несохранённым, пока человек сам его не зафиксировал.** Состояние «письмо ещё не сохраняли» верное и остаётся: запись появляется только по кнопке «Сохранить изменения» либо по отправке в черновики Google. Автосохранения заготовки нет — иначе в истории лежало бы то, чего партнёру не отправляли. Кнопка при этом называется «Сохранить изменения», потому что сохраняется правка человека, а не заготовка движка | Владелец дословно: «наверное, да, давай, мы оставляем письмо как бы не сохраненным. До того момента, пока мы его не нажали. Сохранить, либо загрузить в черновики. Только после этого уже оно получается, что сохраняется». И отдельно: «Давай мы кнопку переименуем в Сохранить изменения. Раз уж мы сохраняем» | owner |
| D172 | 2026-09-22 | **Бейдж «Завершена · заморожена» с карточки проверки убирается.** Слово «заморожена» объясняло, что записанная проверка не пересчитывается правкой чек-листа задним числом, но на карточке оно читается как непонятный технический статус: проверка проведена и записана — это и так видно. Свойство «не пересчитывается» остаётся в силе, объясняется там, где человек правит методику, а не рядом с оценкой. Состояние «Снята» показывать по-прежнему нужно: оно меняет смысл записи | Владелец дословно: «по поводу статуса завершена заморожена вообще непонятно что это такое учитывая что мы проверку провели она уже сохранена и уже ну по сути мы не можем менять проверку или что или как ну как бы странная информация я ее не понимаю и пользователь ее тоже не не поймет» | owner |
| D173 | 2026-09-22 | **Человек в системе — одна сущность: учётка админки, почта и Telegram в одной строке.** Сегодня один и тот же человек представлен трижды и нигде не связан: `mcp_admins` знает его по `telegram_id` (первичный ключ — сам Telegram), `web_users` — по логину и почте Google (T332), а проверка помнит его ИМЕНЕМ СТРОКОЙ (`inspections.auditor`). Из этого не выводится ни «кто проводил проверку» как ссылка, ни «от чьего имени формировать письмо». Заводится сущность человека, к которой цепляются способы его опознать; проверка и письмо ссылаются на неё, а не на строку. Письмо и черновик создаются от имени того, чья учётка их сформировала — то есть того, кто проводил проверку | Владелец дословно: «по сути письмо формируется от того, ну по чьим аккаунтам оно было сформировано. Ну и соответственно, кто проводил проверку. Тут как бы видимо нам нужно связать Telegram снова, да, как, как в Swarm, допустим. Нужно связать Telegram и связать э, почту. То есть сделать некую, ну, учетку пользователя, в которой будет почта, Telegram, это все будет одна сущность» | owner |
| D174 | 2026-09-22 | **Черновик Gmail заводится тем же приложением Google, что и вход, и работает на Internal без проверки Google.** Отдельного приложения, сервисного аккаунта и подтверждения домена для черновика не нужно: у Internal-приложения организации чувствительный скоуп почты доступен без проверки — ровно то ограничение, из-за которого D168 и выбрал Internal. Добавляется право `gmail.compose` (создать черновик, не читать переписку) и запрашивается в момент нажатия кнопки, а не при входе: вход остаётся опознанием и права на почту не просит. Долгоживущий токен не хранится — черновик создаётся сразу в той же сессии | Вопрос владельца: «В чем проблема? Почему мы не смогли сделать все сразу, когда настраивали коннектор? Нам просто нужно права добавить или еще что-то придется?» Ответ: права добавить — и переписать поток входа с опознания по `id_token` на обмен кода, потому что API почты `id_token` не принимает. Партнёрский вход (External, D168) чувствительный скоуп потянет за собой проверку Google — значит черновики остаются правом сотрудника, а не партнёра | owner |
| D175 | 2026-09-22 | **Письмо партнёру не подписывается и не обращается по имени.** Подпись из письма убрана целиком: у каждого сотрудника она стоит в самой почте, и вторая подпись поверх неё делает письмо личным там, где отправить его должен любой человек команды. Обращение по имени снято вместе с контактом (D170): письмо уходит группе. Следствие для экрана: из оговорок «отправлять как есть нельзя» уходят все поля шапки, кроме города, — письмо остальных не печатает, и снять такую оговорку человек не мог ничем | Владелец дословно: «Подписи в письме быть не должно. У нас подписи у всех есть в самом Гугле, поэтому я думаю, что нужно просто письмо сделать максимально безличное, чтобы просто любой человек мог спокойно его отправить» | owner |
| D176 | 2026-09-22 | **Черновик создаётся в почте того, кто зашёл, а не того, кто проводил проверку.** Решающим становится аккаунт вошедшего: письмо формируется от его учётки и ложится в его черновики. Telegram для этой задачи не нужен вовсе, поэтому единая сущность человека (D173, #334) перестаёт быть условием для черновиков и остаётся отдельной работой на будущее | Владелец дословно: «ты прав, что уходить в почту, оно должно тому, кто зашел под какой учетку. То есть Telegram ID здесь уже не важен, по сути нам важно, какая почта Какой аккаунт у зашедшего, и, соответственно, после этого мы уже формируем это все на почту. В будущем потом еще поправим» | owner |
