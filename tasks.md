<!-- managed by forge: формат строк ниже не менять — файл парсят forge-build и forge-status -->
# Задачи

<!-- id: Tnnn (T001, T002, ...). статус: todo | in_progress | done | failed | blocked:Qnnn (Qnnn — id открытого вопроса из questions.md). "зависит от" — id через запятую или «—». блок: имя блока продукта из plan.md, либо chores для служебных задач, которые не относятся ни к одному блоку (добор исследования, выяснение доступов, ответы владельца) — их не отдают блок-агентам. issue: номер задачи в трекере проекта (#N) или «—», если её там нет. Колонка нужна, чтобы сверка графа с трекером была механической: без неё связь держится текстом коммита, и граф отстаёт от трекера молча — на живом проекте так встала стройка на полтора месяца. -->

| id | блок | зависит от | статус | задача | issue |
|---|---|---|---|---|---|
<!-- | T001 | api | — | todo | Пример: спроектировать схему БД | #12 | -->
| T001 | chores | — | done | Получить токен Telegram-бота и положить в `.env` как `TELEGRAM_BOT_TOKEN`; проверить смоуком `getMe` | #3 |
| T002 | chores | — | done | Положить ключ OpenAI в `.env` как `OPENAI_API_KEY`; проверить смоуком один настоящий вызов | #4 |
| T003 | chores | — | done | Поставить конфиги проверок качества до первой строки кода: ruff (наборы E,F,I,B,S,ASYNC,RUF), mypy --strict, pytest+cov с отчётом reports/junit.xml, vulture, deptry, import-linter с контрактами из графа блоков | #5 |
| T004 | chores | T003 | done | Завести Makefile: `make regress` (прогон движка на эталонных данных), `make check` (все гейты), `make demo` | #6 |
| T010 | engine-fix | — | done | Дописать `audit.py edit`: смена кода, класса, зоны и формулировки существующей записи; несуществующий номер — внятная ошибка | #10 |
| T011 | engine-fix | — | done | Вернуть честные коды возврата: `report.py pdf` падает ненулевым кодом, если файл не создан; `letter` — если письмо пустое | #1 |
| T012 | engine-fix | — | done | Сделать запись состояния атомарной: временный файл, `os.replace`, блокировка на чат. Тест на одновременную запись двух кадров альбома | #11 |
| T013 | engine-fix | — | done | Отклонять повторный `add` с той же парой «код + зона»; сделать номера записей сквозными, не переиспользуемыми после удаления | #12 |
| T014 | engine-fix | — | done | Валидировать `init`: пустая пиццерия и дата не в ISO отклоняются. Добавить команду правки шапки уже начатой проверки | #13 |
| T015 | engine-fix | — | done | Печатать сроки устранения по каждому нарушению в PDF — письмо на них ссылается, а в отчёте их нет | #14 |
| T016 | engine-fix | — | done | Развести письмо на два шаблона: чистая проверка (нет D2 и D3) — короткое поздравление; с нарушениями — с планом действий | #15 |
| T017 | engine-fix | — | done | Выводить партнёра из `init --partner` в шапку отчёта | #16 |
| T018 | engine-fix | — | done | Зафиксировать рендерер: WeasyPrint 69.x, кириллический шрифт в проекте с явным путём, `requirements.txt` со всеми зависимостями движка | #2 |
| T019 | engine-fix | T010,T011,T012,T013,T014,T015,T016,T017,T018 | done | Обновить `docs/04-engine.md`: убрать исправленные дефекты, описать новые команды и поведение | #17 |
| T020 | domain | T003 | done | Читать чек-лист, зоны и допустимые классы из `AUDIT_DATA_DIR`; отсутствие или неполнота каталога — падение на старте с внятным сообщением | #18 |
| T021 | domain | T012,T020 | done | Модель состояния проверки и слой доступа к нему: единственная точка, открывающая `inspection.json` | #19 |
| T022 | domain | T021 | done | Операции над находками: `add_finding`, `edit_finding`, `drop_finding`, `attach_photo` с инвариантами «код+зона уникальны» и «класс разрешён для пункта» | #20 |
| T023 | domain | T021 | done | `score()` через вызов `audit.py score` подпроцессом; тест сверяет результат с прямым запуском движка на данных `examples/` | #7 |
| T024 | domain | T020 | done | Обнаруживать форк `checklist_data/` в рабочем каталоге и отказывать в запуске: разные чаты не должны считать по разной методике | #8 |
| T025 | domain | T021 | done | Хранить раздельно язык интерфейса, язык речи аудитора и язык отчёта; записывать в проверку версию чек-листа | #9 |
| T030 | recognize | T020 | done | Сужать 136 кодов до кандидатов до вызова модели: по словам комментария и карте `data/photo-cues.md` | #21 |
| T031 | recognize | T002,T030 | done | Вызов модели со строгой схемой ответа: код строго из переданного перечня, класс из разрешённых, фото передаётся тем же запросом | #22 |
| T032 | recognize | T031 | done | Зашить правила формулировок из `docs/03-recording-rules.md` в промпт целиком; тесты на запрещённые формулировки — масштаб вне кадра, коррозия вместо налёта, похвала без основания | #23 |
| T033 | recognize | T002 | done | Транскрипция голосовых: перекодировка OGG/Opus через ffmpeg, вызов транскрипции, сбой не роняет проверку а просит написать текстом | #24 |
| T034 | recognize | T031 | done | Деградация без модели: бот предлагает выбрать пункт кнопками вручную, проверка продолжается | #25 |
| T040 | report | T018,T021 | done | Сборка PDF через WeasyPrint внутри образа, без обращения к рендерерам на хосте | #26 |
| T041 | report | T040 | done | Кириллический шрифт в образе с явным путём и нумерация страниц через CSS Paged Media | #27 |
| T042 | report | T041 | done | Сверить вид отчёта глазами с эталонами из `examples/` на тех же данных и устранить расхождения правкой CSS | #28 |
| T043 | report | T040 | done | Сделать пропавшую фотографию заметной: сборка сообщает о промахе вместо молчаливой пустоты | #29 |
| T050 | bot | T003 | done | Каркас бота на aiogram 3.31 с роутерами и FSM; доступ только для Telegram ID из `ALLOWED_TELEGRAM_IDS` | #30 |
| T051 | bot | T021,T050 | done | Старт проверки: пиццерия текстом (D051), вид проверки, язык отчёта, дата сегодняшняя | #31 |
| T052 | bot | T051 | done | Продолжение незавершённой проверки: показать чья и от какого числа, предложить «Продолжить» или «Начать новую», не перезаписывать молча | #32 |
| T053 | bot | T051 | done | Приём материала: связь комментария с фото тремя способами — подпись, сообщение следом, ответ на раннее фото. Фото без комментария вызывает вопрос | #33 |
| T054 | bot | T053 | done | Склейка альбомов по `media_group_id` с окном 1–2 секунды в одну запись с несколькими фотографиями | #34 |
| T055 | bot | T022,T034,T053 | done | Показ кандидатов кнопками и фиксация только после подтверждения; подтверждение одной строкой: пункт, класс, зона, накопленный процент | #35 |
| T056 | bot | T022,T055 | done | Правки в чате: удалить последнюю запись, сменить зону, класс, переписать формулировку — с пересчётом процента | #36 |
| T057 | bot | T055 | done | Замеры и информационные записи уровня `D0` (`INF09`, `INF10`, `INF11`) среди находок; блок `info` не используется | #37 |
| T058 | bot | T040,T055 | done | Завершение проверки: итог и список зафиксированного с возможностью поправить, затем PDF файлом и письмо сообщением | #38 |
| T059 | bot | T054 | done | Корректная обработка пачки сообщений, пришедшей с задержкой после потери связи, без путаницы в привязке комментариев к фото | #39 |
| T070 | infra | T018,T033 | done | Dockerfile на `python:3.12-slim` с ffmpeg, системными библиотеками WeasyPrint и кириллическим шрифтом | #40 |
| T071 | infra | T070 | done | docker compose: том под состояние проверок, монтирование каталога методики снаружи, явный часовой пояс, свой префикс проекта | #41 |
| T072 | infra | T070 | done | `.env.example` со всеми именами переменных; проверить, что настоящий `.env` в git не попадает | #42 |
| T073 | infra | T001,T071 | done | РАСКАТКА, не стройка (D060): поднять бота на домашний сервер. Требует явного «да» владельца в том же разговоре — ночное окно снято D088. Выполнено 05.09.2026, коммит 34ac5d9, смоук зелёный (scripts/smoke.sh) | #43 |
| T074 | infra | T071 | done | Демо-набор на английском: вымышленная точка, синтетический чек-лист, идемпотентный сид | #44 |
| T080 | chores | T019,T042,T058,T105 | done | Сквозной смоук: пройти проверку целиком через бота на тестовых данных и сверить оценку с `audit.py score` | #45 |
| T035 | recognize | T031,T032 | done | Замерить точность разбора на боевых данных двух проверок (31 кадр с известными кодами, классами и зонами) на трёх моделях — флагман, средняя, дешёвая — и зафиксировать выбор числом, а не мнением | #46 |
| T037 | chores | — | done | Настроить измерение покрытия для подпроцессов (COVERAGE_PROCESS_START либо parallel-режим): сейчас 88 тестов гоняют движок через CLI, и построчная метрика его не видит | #51 |
| T097 | chores | — | done | Привести спеку, техплан и дорожную карту в соответствие с решениями созвона 01.09 (D015-D028): база и MCP из «не делаем» в работу, справочник пиццерий обязателен, общие виды зон возвращены, срок 20.09 записан | #55 |
| T090 | db | — | done | Поднять Postgres под слив завершённых проверок: свой префикс проекта, свободный порт, чужое на площадке не трогать | #56 |
| T091 | db | T090 | done | Схема базы и миграции: арендатор, точка по идентификатору, версия чек-листа в проверке, находки кодами, переводы строками `(entity_id, lang, text)`, связь повторной проверки с исходной | #57 |
| T092 | db | T091 | done | Справочник пиццерий с картой синонимов, после MVP (D051): выбор точки из списка вместо ввода текстом: «БГ2» → «Белград 2», проверка связывается с точкой по идентификатору, а не по введённой строке | #53 |
| T093 | db | T091,T058 | done | Слив завершённой проверки в базу одной функцией `domain`; файл остаётся рабочим состоянием, падение базы не роняет проверку, непролитые проверки видны | #58 |
| T094 | db | T093 | done | Выгрузка кадров при завершении: `file_id` телеграма снаружи не открывается и в базе станет мёртвой ссылкой | #59 |
| T095 | mcp | T091,T110 | done | MCP-сервер поверх базы, только чтение: список проверок, проверка целиком, находки по точке, история точки, сводка по сети | #60 |
| T096 | mcp | T095,T114 | done | Смоук MCP: три вопроса через Claude, ответы сверены с прямым запросом к базе | #61 |
| T060 | bot | T051 | todo | Режим проверки по чек-листу: пункты по зонам в порядке обхода, рядом со свободной фиксацией (D031). После первой версии, не к 20.09 (D041) | #62 |
| T061 | domain | T020 | done | Порядок чек-листа по маршруту обхода: зоны и пункты внутри них выстроены как идёт аудитор, порядок — данные, не код | #63 |
| T062 | bot | T060 | todo | Подсказки и нормы по пунктам, включая температуры; пометка критичных пунктов. После первой версии (D041) | #64 |
| T063 | bot | T051 | done | Проверяющий подставляется по Telegram ID, дата — сегодняшняя; руками ничего не вводится (D032) | #65 |
| T064 | recognize | T031,T053 | done | Разбор кадра без комментария по нажатию «Разобрать»: бот выделяет проблему и предлагает пункты, фиксация только после подтверждения (D043, D046) | #66 |
| T065 | domain | T022 | done | Источник записи — комментарий аудитора или распознавание по кадру; подсветка распознанных при предвычитке отчёта (D044) | #67 |
| T066 | bot | T053 | done | Отменена решением D047: отдельного шага «выбор зоны» нет, зона берётся из слов аудитора | #68 |
| T067 | bot | T064,T033 | done | Вопрос «Разобрать?» кнопкой на пришедший кадр: разбор только по нажатию, комментарий следом отменяет вопрос и разбирается по словам (D046) | #69 |
| T068 | bot | T058,T067 | done | Кадры без записи показываются при завершении проверки: не нажали «Разобрать» и не прокомментировали — кадр не теряется молча | #70 |
| T098 | mcp | T095,T091 | done | Пишущие инструменты MCP: заводить и править чек-листы через агента, правка в новую версию с валидацией и журналом (D049) | #72 |
| T026 | domain | T025 | done | Составной идентификатор версии: имя набора, дата публикации и отпечаток данных; правка данных не проходит под прежним именем (D050) | #73 |
| T099 | db | T093 | done | Проверка без версии методики не должна сливаться молча: отказ или явная отметка «версия неизвестна» вместо пустой колонки | #76 |
| T100 | infra | T074 | done | Демо целиком на английском: имя собранного отчёта начинается с русского «Аудит»; сид пишет мимо STATE_DIR | #78 |
| T101 | infra | T105 | done | Измерить потолок нагрузки числом и устранить узкие места: одновременные проверки, очередь разбора кадров, блокировка event loop сборкой PDF, конкурентная запись состояния (D058) | #79 |
| T102 | infra | T074 | done | Демо разворачивается из `main` и с теми же доступами, что основной стенд: отличаются только данные — синтетические и английские (D059) | #80 |
| T103 | engine-fix | — | done | Молчаливая подмена долей зон: сумма `share_pct` в `zones.csv` мимо 100 переписывает ВСЕ доли на равные без единого слова — нужен отказ с внятной причиной | #71 |
| T104 | engine-fix | — | done | Сборка отчёта пишет PDF рядом с эталоном под тем же именем и затирает его молча: нужен отдельный каталог вывода либо отказ от перезаписи чужого файла | #74 |
| T105 | infra | T074 | done | Проверить на тестовой площадке то, что раскатки не требует (D060): `docker compose up` поднимает бота локально, перезапуск и пересоздание контейнера не теряют незавершённую проверку, состояние живёт в томе | #81 |
| T106 | engine-fix | — | done | Тихие нормализации в движке: нечисловой `share_pct` → 0, нечисловой `days` → 0 (срок «сегодня»), дубль `id` в stderr, битая дата в `try/except: pass` → отчёт без сроков вовсе. Каждый случай — отказ либо решение терпеть в decisions.md | #82 |
| T107 | bot | T101 | done | Вызовы движка блокируют цикл событий: add_finding+score = 47 мс на подтверждение, очередь растёт линейно (20 аудиторов → 1 с). Обернуть подпроцессные вызовы в `asyncio.to_thread` | #84 |
| T108 | bot | T065 | done | Подключить источник записи в боте: `add_finding(..., source=...)` вместо `sidecar.remember_source`, показ из `finding.source`. Пока не сделано — в базу уезжает пустой источник | #86 |
| T109 | engine-fix | — | done | `manage.py write_rows` молча теряет колонки вне FIELDS: любая правка методики стирает добавленное без единого слова | #87 |
| T110 | db | — | done | `list_inspections` отдаёт проверки ВСЕХ арендаторов и без предела: `tenant_code` обязательным параметром, явный LIMIT, тест на чужого арендатора. Делать ДО T095, иначе MCP закрепит дефект в контракте | #89 |
| T111 | db | — | done | Завершённую проверку можно переписать и удалить задним числом — схема не защищает. Запрет статусом в политике, `as restrictive`, отдельная роль без bypassrls (суперпользователь обходит RLS всегда) | #90 |
| T112 | engine-fix | — | done | `zone-add`/`zone-remove` молча уравнивают доли всех зон на 100/N: заданный вес 20% превращается в 9.09% с первой правкой списка. Плюс промах по коду зоны и `restore` несуществующего печатают успех | #91 |
| T113 | recognize | — | done | Быстрый путь без модели (D063): однозначное срабатывание по словам показывает пункт сразу кнопкой. Основная работа в данных — усилить `data/photo-cues.md`; критерий — доля «ровно один верный код», сейчас 0% из 17 | #92 |
| T114 | db | — | done | Слой чтения не отдаёт находки вовсе: нужны `get_inspection(id)` и `findings_by_unit`, плюс фильтр по датам В SQL — сейчас период отбирается после чтения и начнёт врать, когда зальют историю за три года (D035). tenant обязательным, предел явным | #93 |
| T115 | mcp | — | done | `compare_digest` падает на не-ASCII токене: сервер молча отдаёт пустой ответ вместо отказа. Сравнивать байты либо отказывать на старте; необработанное исключение не должно давать пустой ответ | #94 |
| T116 | mcp | — | done | Гигиена токена: query-string эхом уходит в лог (сегодня не течёт — авторизация через query не принимается), авторизация принимает голый токен без схемы `Bearer` | #95 |
| T117 | bot | T113 | done | Подключить быстрый путь: звать `fast_path` ДО модели, показывать рядом с кнопкой слова аудитора и сработавшую строку карты, держать вторую кнопку «разобрать моделью». Без неё правило 11 (два нарушения в одной фразе) покроет только первое | — |
| T118 | chores | T113 | done | РАЗБЛОКИРОВАНА решением D082: владелец таблицу не правит, применять можно. Черновик 275 строк лежит вне git, измерен: быстрый путь 6% → 41%, неверных ноль. Условие применения (починка отрицания) выполнено — T195 и T197 закрыты | — |
| T119 | mcp | T114 | done | Подключить новый слой чтения: `get_inspection` и `findings_by_unit` (пятый и четвёртый инструменты), выбросить свой отбор по датам и чтение страницей по потолку — период теперь в SQL. Это же чинит `network_summary`, считавший сводку по потолочной странице | — |
| T120 | mcp | T098 | done | Отказы пишущих инструментов отдают агенту полные пути с диска — ответ уходит в модель. Причину без путей агенту, полный путь в журнал сервера. Тест: в ответе нет абсолютных путей | #96 |
| T121 | bot | T117 | done | Снять подтверждение при фиксации ТЕКСТОМ (D064): запись появляется сразу, показ остаётся заметным. Для кадров подтверждение остаётся. Промах сопоставления станет тихим — показ обязан быть виден | #97 |
| T122 | chores | — | done | Сверка построенного со спекой свежим взглядом, итерация 1: сквозные сценарии через несколько блоков, критерии готовности MVP, пути отказа и права — как пользователь, а не чтением кода | — |
| T123 | bot | — | done | Завершённая проверка никуда не сохраняется: `push_inspection` и `upload_photos` не зовёт НИКТО кроме тестов, в базе ноль строк. Плюс вторая проверка затирает первую. Тест обязан падать при снятии вызова | #98 |
| T124 | bot | — | done | Зона берётся из памяти о прошлой записи, а не из слов: «в зале лужа» записывается в горячий цех молча, вопреки спеке и D047. Вычет уезжает в чужую зону отчёта партнёру | #99 |
| T125 | recognize | T124 | done | Замер быстрого пути подставлял эталонную зону, которой у бота нет. Починен: считает три способа добыть зону и подписывает каждый. Как зовёт бот — 1 из 17 (6%), только слова — 1 (6%), эталон — 3 (18%) с подписью «верхняя граница, живому боту недостижима» | #100 |
| T126 | bot | — | done | Бот отвечает на ЛЮБОЙ форме порчи состояния. Чтение проверки собрано в один модуль (15 мест), любая неудача приводится к отказу домена; каталог на месте файла отличается от «не начата», чужая структура — по отсутствию даты. Приёмка: своя решётка 7 форм × 7 входов дала 0 немых сочетаний из 49 | #101 |
| T127 | bot | — | done | Отказ движка уходит аудитору с командной строкой («audit.py photo 1 --add»), кодом зоны вместо названия и всегда по-русски. Случай частый — повторная фиксация того же пункта | #102 |
| T128 | bot | — | done | Имя файла отчёта не вылезает за предел файловой системы: бюджет замерен (постоянная часть 31 байт, точке 120, аудитору 100 — худший случай 251 из 255), формула сверена с настоящим движком | #103 |
| T129 | chores | T123,T124,T126,T127 | done | Сверка со спекой, итерация 2 проведена свежим взглядом: три починки первой итерации держатся, две оказались неполными (немота бота и предел имени файла — обе переоткрыты и с тех пор закрыты), плюс десять новых расхождений заведены задачами. Лимит сверок 3, использовано 2 | — |
| T130 | chores | T123,T124,T126,T127,T128 | done | Пройти живого бота ДВАЖДЫ критическим взглядом человека из общепита: не «работает ли», а удобно ли на точке с занятыми руками. Находки оформить задачами. Указание владельца 2026-09-04 | #104 |
| T131 | bot | — | done | Язык интерфейса до начала проверки стал параметром развёртывания `BOT_UI_LANG`; надписи кнопок мастера переехали из кода в каталог. Демо ставит `en` профилем compose. Умолчание осталось русским намеренно | #83 |
| T132 | chores | — | done | СНЯТА решением D083: парадокс расчёта остаётся как заложено в методике, правка ожидается в будущем. Владелец: «идем так как было заложено в скиле» | #54 |
| T133 | chores | — | cancelled | СНЯТА решением D126 (маршрута от УК нет и не будет, порядок остаётся порядком методики). Была: положить в `data/route.csv` настоящий порядок обхода. Механизм T061 остаётся — настоящий маршрут это подмена одного файла данных, без правки кода | #88 |
| T134 | bot | — | done | Устаревшая кнопка получает ответ: последний рубеж в диспетчере отвечает всплывающим окном — у нажатия старше двух суток сообщения нет вовсе, и это единственный канал | #105 |
| T135 | bot | — | done | Подтверждённая запись показывает вопрос пункта словами и формулировку, которая уйдёт партнёру. Требование спеки «подтверждение одной строкой» снято явно, а не обойдено: оно опиралось на то, что пункт прочитан на кнопке, где до T136 стояла голая цифра | #106 |
| T136 | bot | — | done | Кнопка кандидата называет действие: «Записать» при одном варианте, «Записать №N» при нескольких; ряд ужат с пяти кнопок до трёх | #107 |
| T137 | bot | — | done | Занятый пункт помечен в перечне модели по ПАРЕ «пункт + зона», а не по коду, с номером занявшей записи — той же, к которой приводит отказ движка | #108 |
| T138 | bot | — | done | Брошенный кадр возвращается в чат самим кадром, ответом на своё сообщение: номер сообщения человеку не показывается, и найти по нему было нельзя | #109 |
| T139 | bot | — | done | Команда показа записанного, тем же содержимым, что первая часть завершения. Блок решил сам: одной команды мало, аудитор не наберёт её по памяти — команды объявлены в меню телеграма, там же стали видны отмена и завершение | #110 |
| T140 | bot | T133 | done | Кнопки зон по одной в ряд, предел подписи поднят вдвое. Считано по методике: самое длинное название 36 знаков при прежнем пределе 34 — резалось ровно одно и ровно то, о котором задача. Хвостов не осталось ни на одном языке, запас 32 знака; предел не снят, а поднят, и обрезка по-прежнему видна | #111 |
| T141 | chores | — | done | Набор идёт по СВОЕЙ методике (`tests/methodology/`), боевая нужна только семи файлам под явной меткой. Масштаб измерен: перевод фикстур уронил 51 тест — ровно столько читало боевые данные. Проверено подменой: переименованная зона и новая строка карты набор не красят, 1234 выполнено | #112 |
| T142 | recognize | — | done | Порог различающего слова больше не зависит от размера карты: слово различает, если ведёт не более чем к шести кодам. Прежняя доля переставала работать ровно тогда, когда карта дорастала до нужного размера — на черновике в 275 строк различающими становились ВСЕ 417 основ | #113 |
| T143 | recognize | — | done | Названная зона сужает коды ВНУТРИ покрытой строки карты, а не отбрасывает строки: отбрасывание замерено и отвергнуто — на 17 боевых записях оно даёт 8 срабатываний вместо 7, но одно из них НЕВЕРНОЕ. Словари колонок дополняются из карты, отсутствие раздела не отказ | #114 |
| T144 | mcp | — | done | Карта слов правится через агента и версиями, тем же хранилищем, что чек-лист: правка, публикация, откат. Проверено на копии боевой карты — версия сменилась, быстрый путь поднял новый пункт на новой версии и не поднял на старой | #115 |
| T145 | chores | — | done | ЗАКРЫТА решением D085: запись в эталоне верна, кассовая зона — спорная территория. Продукт такую запись не отклоняет, а помечает «зона не из списка пункта» — проверено запуском. Остаётся вопрос к УК, добавлять ли кассовую зону в список зон пункта, но продукт от этого не зависит | #154 |
| T146 | chores | — | done | Цитирование методики в тестах закрыто: файлов с цитатами было 20, стало 14, в тестах с восьми до четырёх. В синтетическом наборе настоящие только идентификаторы — коды, классы, ставки; формулировки и названия выдуманы. Структуру боевого набора в git намеренно не переносили: полный список пунктов был бы новой утечкой вместо старой | #117 |
| T147 | bot | — | done | Пометка нетипичной зоны видна везде, где видна запись: одно правило на фиксацию, правку и список перед сборкой. Три отдельных условия в трёх местах и были причиной, по которой два из трёх забыли | #118 |
| T148 | domain | — | done | Оценка не считается по методике, отличной от записанной в проверке: сверка при подсчёте, отказ вместо молчаливого пересчёта, перевод на действующую методику — явным вызовом со следом в самой проверке. Дефект блок воспроизвёл и через настоящее хранилище версий: публикация переставляет каталог прямо под живой проверкой | #119 |
| T149 | domain | — | done | Карта кадров включена в отпечаток версии методики. Проверено порчей: добавленная в карту строка меняет идентификатор версии (`local-b9e1adbc238e` → `local-34f722a53f3d`), карта восстановлена побайтно. Сделано ДО применения новой карты T118, как и требовалось | #120 |
| T150 | infra | — | done | Демо-карта слов: 31 подсказка, все демо-коды, английский целиком. Половина была вылечена раньше (отсутствие карты больше не отказ), но без карты в демо не работал быстрый путь — показ упирался в модель на первом же комментарии, а демо-стенд живёт без боевого ключа. Проверено смоуком и мной независимо | #121 |
| T151 | bot | — | done | Отказ сборки отчёта говорит с человеком: текст движка в журнал, в чат — что случилось, что записи целы и как повторить. По ходу блок нашёл два дефекта того же рода: неудача письма шла текстом «отчёт не собрался», хотя PDF уже отдан, и старт отдавал полный стек движка | #122 |
| T152 | bot | — | done | Вид проверки связан КОДОМ: таблица перевода переехала в предметную область, в проверке и базе лежит код, отказ на формулировку идёт до вызова движка. Правлено 130 вызовов в 59 файлах. Основной путь верен в обе стороны — проверено смоуком; пересборка на ДРУГОМ языке осталась и вынесена в T177 | #123 |
| T153 | bot | — | done | Сданная проверка не называется незавершённой. Признак — число записей на момент сдачи в заметках бота, а не «да/нет»: аудитор вправе дописать запись в сданную проверку, и отданный отчёт её уже не содержит, поэтому признак сверяется с делом сам | #124 |
| T154 | bot | — | done | Сообщение о недоступной модели и об отказе разбора больше не показывает аудитору внутренности: сырая причина уходит в журнал, человеку — что случилось и что делать. Доделано диспетчером из работы упавшего блока | #125 |
| T155 | bot | — | done | Заметки бота под блокировкой: тройка «прочитать, дополнить, записать» целиком под замком, тем же механизмом и тем же именем, что у файла проверки — второй протокол разошёлся бы с первым. Сброс тоже под замком: снятие файла в обход воскресило бы кадры прошлой проверки. Замерено на неисправленном коде: три процесса по двадцать кадров дали 36 из 60 | #126 |
| T156 | bot | — | done | Оговорка про зону из памяти теперь на всех путях записи, а не только на быстром | #127 |
| T157 | recognize | — | done | Отсутствие карты слов деградирует в обычный разбор моделью (D068): пустой текст и строка в журнал вместо отказа. Тест написан до правки упавшим блоком и был красным; правку и порчу сделал диспетчер. Демо-карта остаётся за T150 | #130 |
| T158 | bot | — | done | Информационная часть в конце проверки: кнопка «Собрать отчёт» открывает семь вопросов, отчёт собирается после них. Вопрос берётся ИЗ МЕТОДИКИ, а не из каталога текстов — партнёр видит в отчёте ту же формулировку, и второй текст об одном разъехался бы. Голос показывается расшифровкой до записи, «да/нет» уезжает на языке отчёта, пропущенное поле не пишется вовсе | #131 |
| T159 | engine-fix | — | done | Скрытые поля отчёта сняты, все информационные поля печатаются партнёру. Блок сверх задания собрал оба эталонных отчёта старым и новым движком на двух языках — совпали знак в знак | #132 |
| T160 | bot | — | done | Фотофиксация обязательна всегда (D078), и аудитор это ПОНИМАЕТ: правило звучит до первой ошибки — первым сообщением проверки, при её продолжении и абзацем под отказом, из одного источника на три места. Прежняя жалоба продукта на себя заменена фактом о записи. Запрета в коде не прибавилось: по одному комментарию запись не создавалась и раньше | #133 |
| T161 | domain | — | done | СНЯТА решением D078: признак «фото обязательно по пунктам» не нужен — обязательно везде. Работа не начиналась | #133 |
| T162 | bot | — | done | Процент во время обхода снят в четырёх местах вместе с расчётом: подпроцесс движка 26 мс ради выбрасываемого числа. Оценка осталась в итоге при завершении, и это отдельный случай в тестах | #134 |
| T163 | engine-fix | — | done | Запись без кадра помечена в отчёте партнёру. Граница пометки выбрана блоком: в разделе нарушений подчиняется режиму показа кадров, в приложении печатается всегда; разведена с отметкой промаха «фото привязано, но не найдено» | #133 |
| T164 | db | — | done | Место под предложение модели заведено и слив умеет его писать; «что аудитор поменял» не хранится, а считается сравнением — третья копия факта разъехалась бы молча. Предложение НЕ входит в отпечаток: иначе слив до и после его появления дал бы две строки в истории точки. Данных пока нет — их не отдаёт домен, заведена T181 | — |
| T165 | mcp | T164 | done | Предложения для управляющей компании: одинаковые расхождения складываются, называются строки карты, ведущие к неверному коду, даётся готовый вызов правки. Граница D077 держится тремя вещами — ответ несёт «не применено», применяет человек отдельным вызовом, и даже он кладёт версию рядом, не публикуя | — |
| T166 | recognize | T183 | done | Признак «это чистота» сделан чистой функцией в дополнение к разбору, поток не перехватывает. Замер честный: на 1027 текстах признак не сработал ни разу, но померено «как редко встречается», а не «как часто ошибается» — настоящей выборки сырых слов накоплено ноль, и замер печатает это словами, а не показывает успокоительный ноль | — |
| T167 | bot | — | done | Расхождение версии методики показано человеку: обе версии и две кнопки — перевести проверку или разбираться с методикой. За аудитора блок не решает | #135 |
| T168 | engine-fix | — | done | Движок и его проверка методики читают файл одинаково: трактовка комментариев вынесена в одну функцию. Чинён валидатор, а не читатель — иначе комментарии перестали бы работать там, где работают | #136 |
| T169 | domain | T148 | done | Остаточный риск, названный блоком честно: записи, добавленные ПОСЛЕ переиздания методики, сверкой не ловятся — она стоит на подсчёте, а не на записи (отказ на записи запер бы аудитора посреди выезда). Проверка может оказаться смесью двух методик. Закрывается подсчётом по снимку версии из хранилища — это архитектура, а не правка блока | Уточнение блока: сверка на записи ловит расхождение раньше, но запирает аудитора посреди выезда — починить оттуда он ничего не может. Полное закрытие требует обратной зависимости слоёв (домен обратился бы к хранилищу версий, а сегодня зависимость только в одну сторону), поэтому это вопрос владельцу, а не правка | — |
| T170 | engine-fix | — | done | Служебный маркер класса в подписи информационного поля срезается тем же правилом, что у формулировки нарушения. До T159 поля не печатались, и партнёр этого не видел; найдено блоком, доделано диспетчером | #139 |
| T171 | mcp | — | done | Письмо партнёру по записанной проверке доступно агенту — ПЕРЕСБОРКА, а не пересчёт: методика прибита к версии проверки, расхождение с записанной оценкой это отказ. Проверено на боевых данных: та же проверка с изменённой ставкой даёт 90% B вместо 97.5% A, партнёр получил бы другую оценку под прежней датой. Отчёт намеренно НЕ добавлен: PDF по этому каналу не передаётся, а всё, ради чего его открывают, отвечают три существующих инструмента | — |
| T172 | engine-fix | T158 | done | Кадр к информационному полю: поле хранится парой «текст + кадры», кадр печатается в приложении под текстом своего поля независимо от режима показа. Старые проверки со строкой читаются по-прежнему — форма приводится в одном месте | #140 |
| T173 | bot | — | done | Занятая пара помечена и в ручном перечне — строкой над кнопками, а не на кнопке: там 34 знака, и пометка съела бы формулировку. Названы только пункты текущей страницы, связывает строку с кнопкой код. Пункт из перечня не убран: это предупреждение, а не решение за человека | — |
| T174 | chores | — | done | СНЯТА: решение D081 меняет привязку комментария целиком — правка идёт ответом на сообщение бота, и прежняя развилка про удержание текста не возникает | #141 |
| T175 | engine-fix | — | done | Неизвестный язык отчёта — отказ с ненулевым кодом и перечнем доступных, а не молчаливая подмена русским. Проверяется и язык из шапки, и явно переданный, до расчёта | #142 |
| T176 | db | — | done | Четыре поля шапки читаются из базы — письмо по записанной проверке подписывается именем аудитора, а не прочерком. Смоук в обе стороны на живой базе: со снятыми колонками письмо честно объявляет себя неготовым, с колонками — готово | #143 |
| T177 | engine-fix | T152 | done | Вид проверки связан кодом на всём пути: движок принимает код и переводит при печати по языку печати, старая таблица перевода строк снята. Домен отдаёт код — доделано диспетчером на приёмке (#145). Проверено обоими направлениями запуском: русское письмо «Плановая», английское «Planned» | #144 |
| T178 | db | T152 | done | Схема объясняет, что в колонке вида лежит код; ограничения перечня в базе намеренно нет — перечень живёт в предметной области, копия в схеме отказывала бы на сливе, то есть на точке. Идемпотентность проверена ПЕРЕСЧЁТОМ строк, а не совпадением идентификатора. Расхождение версии методики отделено от отказа базы | — |
| T179 | bot | T172 | done | Кадр информационной части доезжает ДО ОТЧЁТА, а не до команды: подпись становится ответом, ссылка уезжает движку, кадр попадает в карту при сборке. Половина правки была бы ХУЖЕ нынешнего — ссылка мимо карты печатается партнёру красной отметкой «фото не приложено». Проверено моим смоуком: картинка внутри PDF есть | #146 |
| T180 | engine-fix | — | done | Вид проверки печатается и в отчёте, не только в письме (D084). Место в шаблоне заведено и пустует | #147 |
| T181 | domain | T164 | done | Предложение модели доезжает до базы: бот передаёт первый ответ модели (а не нажатую кнопку — иначе тройки совпадали бы всегда и промах перестал бы существовать как явление), домен запоминает рядом с записью по образцу источника, слив пишет. Проверено живой базой: в строке лежат обе тройки, правка считается сравнением | #148 |
| T182 | bot | T178 | done | Расхождение версии методики показано человеку отдельной веткой ДО общего отказа базы (он её предок — после была бы недостижима), тем же текстом и клавиатурой, что на подсчёте | #149 |
| T183 | bot | — | done | Сырые слова аудитора хранятся рядом с записью, как источник и предложение. Словами считается весь материал, каким пришёл, включая расшифровку голоса — промах разбирают по тому, что ВИДЕЛА система. Слова пишутся один раз и не меняются: заменённые правкой, они стирали бы сигнал ровно тогда, когда он появляется | — |
| T184 | chores | — | done | Боевые секреты убраны из двенадцати рабочих копий блоков и заменены заглушками; правило отправлено в задачи инструмента (GarroV/forge#89). Владелец решение о ротации принял сам | — |
| T185 | db | T183 | done | Сырые слова аудитора доезжают до истории и читаются обратно. Не входят в отпечаток проверки — отпечаток это содержимое документа, ушедшего партнёру, а сказанное на точке партнёру не предназначено; иначе одна проверка, слитая до правки и после, дала бы две строки в истории точки. Неизменность держит схема (запись запечатанной проверки заморожена политикой), а не второй заслон рядом — его нельзя было бы снять и увидеть красное | — |
| T186 | infra | — | done | Русское слово в английской колонке: размер посчитан — ровно три ячейки, единичный случай, а не система. Решение блока — ПОМЕТКА, а не отказ: отказ на сборке отчёта означал бы, что аудитор уезжает без документа из-за справочного поля, которое он не может исправить. Ищется письменность, а не равенство колонок: совпадение законно, весь демо-набор совпадает построчно | #153 |
| T187 | mcp | — | done | Найдена НАСТОЯЩАЯ причина падений под нагрузкой: не стартует сам интерпретатор в подпроцессе движка, до первой строки скрипта. Хранилище считало любой ненулевой код отказом методики, и машинная беда приходила как «движок не принял вашу методику». Причину нашла правка теста, который печатал одну и ту же строку на любой отказ | #155 |
| T188 | chores | — | done | Детектор утечки методики пропускал цитаты, разорванные переносом строки: блок нашёл дословную строку карты в комментарии модуля, которую проверка не видела. Читаю текст плоско — снимаю знаки комментариев и схлопываю пробелы. Порчей проверено: разорванная цитата ловится и называется файлом. ПЯТАЯ дыра в этой проверке, и все пять нашли чужие глаза | #152 |
| T189 | chores | T187 | done | Несостоявшийся старт движка повторяется, а не выдаётся за отказ по существу. Вылечено в ОДНОМ месте — общем вызове домена, через который в движок ходит весь бот. Дубль проверен отдельно НАСТОЯЩИМ движком со счётом заходов: по числу записей дубль не виден, движок сам отвергает вторую запись той же пары, поэтому проверка без счёта заходов ничего не различала бы | #156 |
| T190 | report | T189 | done | Пять попыток с растущей паузой, политика объявлена ОДИН раз и импортируется тремя вызывающими. Замер честный: саму фатальную смерть воспроизвести НЕ УДАЛОСЬ (262 запуска при нагрузке до 792 — ни одной), и блок не выдал вероятность за измеренную. Измерено то, что покупает пауза: медиана окна вытеснения под нагрузкой 405 мс, p90 768 мс, максимум 1919 мс; за 2 с кончались все окна. Сумма пауз ограничена четырьмя секундами — молчание дольше аудитор читает как зависший бот | — |
| T191 | report | T189 | done | Сборка отчёта повторяет несостоявшийся старт движка; признаки смерти интерпретатора не скопированы, а взяты у домена — третья копия тех же строк была бы тем расхождением, от которого лечим. Довод «повтор здесь безопаснее» проверен фактом: тест собирает отчёт настоящим движком после несостоявшегося старта и сверяет файл проверки побайтно | — |
| T192 | recognize | — | done | Правила языка разбора переехали в данные: стоп-слова, окончания, отрицания, словарь колонок, заголовки разделов — на каждый язык. Третий добавляется записью в файле, кода со списком языков в продукте не осталось. Русский путь не просел — замер дословно тот же; основы слов сверены со старым разбором на 1220 текстах, расхождений ноль. Английский мерить не на чем, и блок сказал это прямо вместо показа нуля как успеха | #157 |
| T193 | db | T185 | done | Источник записи вне отпечатка, и решение НЕ «удалить поле»: блок отверг прямое удаление (смена рецепта объявила бы всё слитое неизвестным, а разделить две запечатанные проверки нечем) и пересчёт миграцией (рецепт живёт в Python поверх двух таблиц; повторить в SQL можно, убедиться в верности — нет, при необратимой цене). Взято третье: рецепты накапливаются, слив сверяется с прежним ДО записи | #158 |
| T194 | mcp | T185 | done | Предложения для УК называют ФРАЗУ аудитора дословно, а не только строку таблицы: по строке УК понимает, что править, по фразе аудитор видит, что сказал. Оговорка «слов нет» снята в трёх местах, включая описание инструмента для агента — пока оно говорило, что фразы нет, агент про неё бы и не спросил. Готовый вызов правки из фразы блок намеренно НЕ собирает: раздел таблицы из слов не выводится, и подстановка решила бы за УК ровно то, что D077 оставляет ей | — |
| T195 | recognize | — | done | Быстрый путь больше не записывает отрицаемое. Правило выбрано ЗАМЕРОМ четырёх вариантов: грубое «есть отрицание — отказ» отключило бы 13 строк карты про отсутствие, соседнее слово без учёта знаков теряло 30 строк из 32. Взятое ловит 94% отрицаний на боевой карте и 98% на черновике, НЕ потеряв ни одного срабатывания: 1→1 на боевой, 7→7 на черновике, неверных 0→0. Проверено мной: «не смешаны» больше не даёт пункт | #160 |
| T196 | recognize | T166 | done | Связка признака процесса на двух языках. Блок ПРОВЕРИЛ довод предыдущего («английский мерить не на чем») и опроверг: английская колонка вопросов методики лежала рядом всё время — корпус того же рода, каким меряется русская связка. 136 вопросов, в 31 есть связка, признак не сработал ни разу; русские корпуса не сдвинулись | #161 |
| T197 | recognize | T195 | done | Остаток отрицания закрыт: основа, сказанная и отрицаемая в одной фразе, не считается ни сказанной, ни отрицаемой — отказ, а не выбор стороны. Пропусков на боевой карте 25 → 17 из 412, поймано 94% → 96%, срабатывания и неверные не изменились (1/0). Списки пропусков сверены построчно: ушли ровно восемь известных и ни одной другой | #160 |
| T198 | chores | — | done | Детектор утечки методики был чувствителен к регистру: восемь цитат лежали в репозитории, отличаясь заглавной буквой, включая формулировку вопроса методики. Правило сравнения разведено: зоны сверяются С УЧЁТОМ регистра (произносимые формы строчными живут в коде по правилу проекта и цитатой не являются), формулировки и строки карты — без. Три настоящие цитаты убраны | #162 |
| T199 | chores | — | done | Сверка со спекой, итерация 3. Все 24 обязательных требования прогнаны запуском: выполнены, три с оговорками (процент снят решением D072, подтверждение словами снято D064, три языка задаются одним вопросом мастера). Восемь расхождений, два высоких. Стык трёх поверхностей сошёлся без единого расхождения: числа, коды, зоны и языки одинаковы в боте, базе и MCP | — |
| T200 | db | — | done | ВЫСОКОЕ: информационная часть не сливается в базу вовсе — ни таблицы, ни колонки. Партнёр получает от УК другой срок плана действий, чем от аудитора, и обе бумаги подписаны системой как верные. Хуже потери: ответ инструмента утверждает, что оценка сверена, и срока в перечне невосстановленного нет | #163 |
| T201 | bot | — | done | Сданный отчёт не правится (D080): вернуться к завершённой проверке и дописать находку нельзя — только удалить целиком или завести новую. Это и убирает задваивание истории: вторая строка о том же обходе возникала именно из дописывания. Бот сейчас сам предлагает «Продолжить её — можно дописать и собрать отчёт заново» | #164 |
| T202 | bot | — | done | ПЕРЕСТРОЕНО решением D081: кадр с комментарием разбирается ПО КОММЕНТАРИЮ, в модель не уходит; кадр без комментария разбирается по кадру. Это и закрывает расход, о котором был вопрос | #165 |
| T203 | db | T193,T304 | todo | Снять прежний рецепт отпечатка, когда `src/db/recipe_audit.py` скажет «снимать МОЖНО» на всех трёх базах (D127: вердикт машинный, а не со слов). Прогон встроен в порядок наката, поэтому задача ждёт наката на площадку (T304), а не ответа владельца; в задаче записано, чего при снятии делать нельзя | #170 |
| T204 | bot | T202 | done | Правка ОТВЕТОМ на сообщение бота (D081): аудитор отвечает на сообщение с разобранной записью и вносит корректировку словами — система ищет пункт уже по этому комментарию. Пример владельца: система написала «стол в холодном цехе», аудитор отвечает «горячий цех, линия начинения» | — |
| T205 | bot | T204 | done | Кадр ответом на своё же голосовое (D081): аудитор наговорил, бот разобрал, затем аудитор отвечает на СВОЁ голосовое фотографией — система подхватывает её в ту же запись | — |
| T206 | bot | T204 | done | Пачка кадров: отбивка приходит списком ОТДЕЛЬНЫМИ сообщениями, ответ на любое из них правит именно ту запись (D081). Сегодня пачка даёт одну запись на все кадры — это другое поведение, надо развести | — |
| T207 | mcp | T200 | done | Чтение одной проверки через MCP не отдаёт информационную часть: письмо из истории срок несёт, а карточка проверки его не показывает — читающий решит, что срока нет. Добавить раздел в ответ инструмента и тест на чтение записанного обратно | #171 |
| T208 | mcp | — | done | Комментарий у набора полей письма описывает состав до появления информационной части: перечисляет то, чего нет, и молчит о том, что добавилось | #172 |
| T209 | bot | — | done | Скрывающееся меню бота (D087): «Новая проверка» и «Установка MCP» с готовой командой для терминала названы владельцем; остальные пункты предложены в задаче трекера и ждут его слова. Удаления сданной проверки в меню быть не должно — по D086 это операция УК через MCP | #176 |
| T210 | db | — | done | Снятие сданной проверки из истории (D086): сегодня операции удаления в src/db нет вовсе. Миграция под признак снятия, операция с тестами, уборка кадров в хранилище, снятая проверка не участвует в чтении и в сверке отпечатка. Инструмент MCP — задача T211 | #177 |
| T211 | mcp | T210 | done | Инструмент MCP «снять проверку» для управляющей компании (D086) с внятным подтверждением. Бот такого входа не получает | #177 |
| T212 | recognize | — | done | Тест живого замера быстрого пути падает вместо пропуска, когда нет эталонных проверок: одно и то же отсутствие данных даёт в наборе два разных исхода — соседний тест молча пропускается, этот выглядит поломкой продукта | #180 |
| T213 | domain | — | done | Два представления одной пары «каталог методики + хранилище версий» (`checklist.Store` и `letters.Papers`). Долг: разойдутся молча. Малый приоритет, правка контракта | —. СНЯТА РАЗБОРОМ: `Store` несёт право арендатора, `Papers` не несёт никакого; обязательность полей у них разная по смыслу, `live` отвечает на разные вопросы, а общее уже сведено в T236 и T243. Канонического места в `domain` быть не может — зонд уронил контракт границ |
| T214 | db | — | done | Тест ролей базы меняет пароль роли приложения на весь кластер: роли в Postgres — объект кластера, а не базы, поэтому прогон роняет девяносто чужих тестов в соседних копиях. Сегодня обходится запретом запускать его руками — это не решение | #128 |
| T215 | chores | — | done | Контракты импортов описаны от корневого пакета и каталог инструментов в граф не входит вовсе: правила на него не действуют | #129 |
| T216 | infra | — | done | Демо-набор содержит дословное боевое название зоны, причём в обеих языковых колонках. Демо синтетическое по построению — это утечка | #137 |
| T217 | bot | — | done | Ручной перечень тратит треть кнопки на служебный префикс класса: под формулировку остаётся 34 знака, дальше надпись разъезжается на телефоне | #150 |
| T218 | mcp | — | done | Правка удаления строки карты слов отказывает ложной причиной: совпал не запрос, а получившееся состояние — откат даёт прежний отпечаток методики, и хранилище считает это повтором | #166 |
| T219 | bot | — | done | Быстрый путь подписывает формулировку партнёру как «ваши слова», хотя это текст модели. После правки зоны документ противоречит сам себе, и аудитору негде было это увидеть | #167 |
| T220 | mcp | — | done | Письмо на другом языке переводит шапку и зоны, а формулировки находок оставляет на языке записи (это решение) — но перечень невосстановленного про язык находок молчит, и письмо выглядит готовым к отправке | #169 |
| T221 | domain | — | done | Довод в комментариях блока неверен: номер записи после удаления не освобождается, движок номера не переиспользует. Чистка безвредна, неверно её обоснование | #151 |
| T222 | chores | — | done | Копии блоков провижинятся по-разному: без эталонных проверок регрессионный якорь молча пропускается, а устаревшая копия данных делает детектор утечки слабее настоящего | #175 |
| T223 | chores | — | done | Прямая речь владельца в комментариях кода. Приток закрыт сторожем `tests/test_owner_quotes_no_new.py` (потолок, а не ноль; проверен порчей). По старым 25 местам решение владельца D116: не чистим — из публичной истории это всё равно не убрать | #178 |
| T224 | recognize | — | todo | Качество разбора по комментарию живой моделью не измерено: оснастка готова и проверена вхолостую, замер платный (34 вызова) и ждёт решения владельца | #179 |
| T225 | bot | T169 | done | Потребители домена в боте зовут справочники без привязки к изданию проверки: после переиздания аудитору можно предложить пункт, которого в его издании нет, и движок запись не примет. Правка механическая — добавить chat_id в вызовы | #181 |
| T226 | recognize | T169 | done | То же в разборе: справочники и карта слов читаются из действующего каталога, а карта входит в отпечаток издания | #181 |
| T227 | bot | — | done | Ответ словами на сообщение-отказ уходит в очередь материалов и может завести новую запись из чужого ждущего кадра: сообщение-отказ называет номер записи и предлагает её поправить, но в карту записей не попадает | #182 |
| T228 | bot | — | done | Тесты альбома закрывают пачку таймером в 10 мс, таймер срабатывает между кадрами — гонка, а не проверка. В новых тестах пачка закрывается событием, тот же приём перенести сюда | #183 |
| T229 | bot | — | done | Голосовое до кадра (D090): вместо отказа бот отвечает, что ждёт фото, придерживает расслышанные слова и собирает запись в момент прихода кадра | #184 |
| T230 | bot | — | done | Отбивка о сохранении для ВСЕХ добавлений (D090), а не только для голосового: единая пара «принял / сохранено», формулировка одна на все пути | #185 |
| T231 | bot | T230 | done | Ответ СЛОВАМИ на отбивку правит запись, включая зону (D090): «это был горячий цех» после «грязные полы в ХЦ сохранен, -1 балл» — бот правит и подтверждает новой отбивкой. Сначала сверить, что уже умеет правка ответом из D081/T206: правка зоны сегодня есть кнопкой (EDIT_ZONE), нужна словами | #186 |
| T232 | bot | — | done | Предел разбираемой пачки — пять кадров (D091, D093): разбираются пять, про остальные бот ГОВОРИТ словами «пришлите отдельной пачкой», молча не отбрасывает | #187 |
| T233 | db | T210 | done | Снятие проверки (D089): причина снятия — обязательное поле, снятые проверки базово видны только администраторам, кадры убираются из хранилища | #188 |
| T234 | report | — | done | Служебный префикс класса из методики печатается партнёру в отчёте: при ручном выборе пункта по кадру без комментария формулировкой записи становится вопрос пункта целиком. На боевой методике так размечен 31 пункт из 136. С кнопки префикс снят T217, в отчёте — решение владельца | #190 |
| T235 | chores | T215 | done | Контракты импортов не покрывают связь «инструменты → бот»: замер зовёт бота напрямую, и правка сигнатуры уронила десять его тестов — поймал только полный прогон. Решить не «запретить», а как правильно | #191 |
| T236 | mcp | — | done | Карточка проверки читает каталог издания без сверки отпечатка: у письма расхождение ловит пересчёт движком, у карточки компенсации нет — каталог с правильным именем и чужим содержимым подпишет ответы аудитора формулировками другой методики. Рядом сводится с полкой изданий домена | #192 |
| T237 | mcp | — | done | Письмо печатает находку с пустой формулировкой и молчит: строки перевода может не быть вовсе, а перечень невосстановленного об этом не говорит. Штатным путём не достигается, но чтение к этому готово, а письмо нет | #193 |
| T238 | mcp | — | done | Отказы хранилища версий (занятое имя, пустая правка) в журнал правок не попадают: по журналу не видно, что агент УК пытался и получил отказ. Правка меняет контракт возврата | #194 |
| T239 | recognize | — | done | Замер быстрого пути без каталога состояния врёт кодом возврата: единица в этом инструменте означает «есть неверное срабатывание», а на деле не задана переменная окружения. Лечится тем же, чем вылечен соседний замер по T224 | #195 |
| T240 | db | — | done | Тест роли приложения сторожит состояние роли в кластере, а не то, что делает миграция: роль создаётся только если её нет, поэтому правка миграции на всесильную роль тестом не ловится. Закрывается накатом на роль с уникальным именем | #196 |
| T241 | bot | T229 | done | Придержанные слова, не дождавшиеся кадра, при завершении проверки не называются: кадры без записи бот называет, а потерянную словами находку — нет | #197 |
| T242 | recognize | — | done | Зона узнаётся только в именительном падеже: замер на боевой методике 10/10 против 7/10 в предложном, стеммер даёт «участок» против «участк». Задевает и обычную запись, и новую правку зоны ответом | #198 |
| T243 | mcp | — | done | Издание с нелатинским именем набора не читается инструментами вовсе — ни письма, ни подписей полей. Заведено соседней сессией | #200 |
| T244 | chores | — | todo | Проверка типов не смотрит на каталог тестов: типовая краснота в тестах не видна ни человеку, ни прогону. Заведено соседней сессией | #199 |
| T245 | chores | — | done | Проверка сторожа пуша делает жёсткий откат поверх незакоммиченного и уносит чужую работу. Заведено соседней сессией | #189 |
| T246 | infra | — | done | (#201, ведёт соседняя сессия) На сервере код в каталоге расходится с образом контейнера, и `git log` в каталоге выглядит доказательством обновления. В смоук — сверка версии ВНУТРИ контейнера; в бот — видимая с той стороны строка о версии сборки | #201 |
| T247 | chores | — | done | Сторож утечки методики смотрит только файлы под git, то есть НОВЫЙ файл не видит вовсе — а именно такие каждый блок и создаёт. Проверено опытом: полное название зоны в неотслеживаемом файле проходит насквозь | #204 |
| T248 | engine-fix | — | done | Движок подставляет в письмо формулировку ВОПРОСА методики вместо ненаписанных слов аудитора, переведённую на язык письма: у отклонения оказывается текст, читающийся как «всё в порядке», и в английском письме он выглядит нативнее честной записи. В PDF такой строки просто нет — письмо и отчёт расходятся на одних данных | #205 |
| T249 | bot | T229 | done | ВАЖНО перед живой пробой: очередь ожидания переживает смену проверки — кадр НОВОЙ проверки забирает придержанные слова старой, и запись уезжает в отчёт с чужой формулировкой. Хранилище материалов ключуется только по чату и ни стартом, ни завершением не чистится | #202 |
| T250 | bot | — | done | Команда меню, набранная в мастере начала проверки, становится названием пиццерии. Пункт «Установка MCP» делает случай вероятнее | #203 |
| T251 | chores | — | done | Сторож полноты прогона не видит базу: без строки подключения в оболочке 282 теста базы и MCP пропускаются, и хвост выглядит обычным зелёным | #207 |
| T252 | chores | — | done | Список исключений vulture приехал отсортированным, причины оторвались от групп имён вопреки шапке файла | #206 |
| T253 | bot | — | done | Админка в боте (D098, D099): команда настройки Claude Desktop, видная в меню ИЗБРАННОМУ кругу; админы добавляют админов; доступ выдаётся человеку, а не стороне — выпуск на идентификатор, хранение отпечатком, показ один раз, поимённый отзыв, след «кто кому когда». В MVP малое и работающее | #208 |
| T254 | chores | — | todo | Покрытие по отдельным файлам пляшет между прогонами при неизменном коде: один файл дал 99/43/99 в трёх подряд полных прогонах. Гейт по покрытию при таком разбросе спрячет настоящую просадку | #209 |
| T255 | infra | — | done | MCP-сервер не поднимается как сервис площадки: не входит в compose, живёт до конца сессии запустившего, порт 8265 сейчас никто не слушает. Нужен сервис рядом с ботом с автоперезапуском и здоровьем; наружу порт не публикуется | #210 |
| T256 | infra | T255 | done | Доступ к MCP снаружи туннелем (D100): сервер остаётся на петле, снаружи виден только туннель с TLS. Адрес для строки настройки берётся из настройки стенда, а не прибит петлёй. Смоук обязан отвечать «доступен ли сервер с чужой машины» | #211 |
| T257 | bot | T256 | done | Мост требует копию репозитория на машине человека: строка настройки заканчивается путём к файлу из нашего кода, и сторонний видит русскую заглушку посреди команды терминала. Подключение не должно требовать нашего репозитория | #212 |
| T258 | infra | — | done | Сервер MCP не стартует без карты токенов стороны, хотя личные токены её заменяют: раньше это видел человек, запускавший сервер руками, теперь это вечный перезапуск при внешне здоровом стенде | #214 |
| T259 | infra | T256 | todo | После пилота: переезд с Cloudflare на свой канал (D101). Нужен публичный узел — площадка за домашним NAT; отдельно решить, не меняется ли сама идея «дать коллеге строку», если ему понадобится доступ к узлу | #215 |
| T260 | infra | T256 | done | Звено между общим входом площадки и сервером держится руками: поднято отдельным контейнером мимо compose, после пересоздания сервера его надо поднимать заново, на новой площадке его не будет вовсе. Нужен сервис в сетевом пространстве сервера, своим профилем | #216 |
| T261 | bot | — | done | Пункт настройки различает Claude Code и Claude Desktop: команда для приложения правит его файл настроек, сохраняя уже настроенные серверы. Выбор клиента — решение блока, не владельца | #217 |
| T262 | binding | — | done | Колонка «Зона» в карте кадров `data/photo-cues.md`: парсер читает её по заголовку (`src/recognize/cues.py`), пустое значение означает «спросить» и не является ошибкой данных. Пополнение объектами, названными владельцем (станция закусок, миксер для коктейлей, линия начинения, станция упаковки) — только с его явного «да» | #227 |
| T263 | binding | T262 | done | `resolve_zone`: зона из слов аудитора в живой форме («в холодном», «на кассе», «в горячем») и обиходных названиях мест, затем из словаря объектов. Порядок источников — слова, словарь, спрос; пятого нет | #225 |
| T264 | binding | T263 | done | Прошлая запись перестаёт быть источником зоны ни в одной ветке: `sidecar.remember_zone` снят как источник, `UNKNOWN` от модели подсказкой не подменяется (`src/recognize/classify.py:67-69`) | #218 |
| T265 | binding | T263 | done | Зона сортирует, но не отсекает: снять фильтрацию в `fastpath.py`, `shortlist.py`, `prompt.py`, `manual.py`. Быстрый путь не отказывает `WRONG_ZONE`, когда зона выведена словарём; ручной перечень содержит пункт чужой зоны | #218 |
| T266 | binding | T263,T265 | done | Спрос зоны кнопками, когда она не вывелась: только зоны, допустимые методикой для выбранного пункта. Расхождение названной зоны со словарём называется до фиксации с выбором «зона объекта или другой пункт» | #225 |
| T267 | binding | T265 | done | Поиск пункта словом вместо листания: `search_items` поверх `match_cues` даёт короткий список без модели и сети. Листание 123 пунктов — запасной вход, не основной | #226 |
| T268 | binding | T262 | done | Накопитель непокрытых формулировок: то, что не дало записи, пишется в `STATE_DIR` рядом с заметками бота и уезжает в историю при завершении проверки. Состав записи — по таблице блока | #228 |
| T269 | binding | T268 | done | Кадр без записи перестаёт теряться молча: при завершении по каждому такому кадру названа причина, сигнал уходит в накопитель | #219 |
| T270 | binding | T268 | done | Чтение накопителя владельцем через MCP: список кандидатов «слово → зона, встретилось N раз» там же, где предложения для управляющей компании. Перенос строки в карту кадров — ручное действие, автопополнения нет | #228 |
| T271 | binding | T262,T265 | done | Движок отказывает на паре «пункт + зона», недопустимой методикой, в `add` и `edit` вместо записи с предупреждением. `score` не трогается, регресс дословный: 97.5% A 5×D1 и 97% A 6×D1 | #239 |
| T272 | binding | T264,T265,T266,T267 | done | Повторные замеры D013 (`tools/bench_run.py`, точность по коду не ниже 94%) и D063 (`tools/fastpath_measure.py`, доля быстрого пути выше, промахов не появилось) плюс новый замер покрытия словаря. Числа записываются рядом с прежними | #240 |
| T273 | binding | T264,T265,T266,T267,T268,T271 | done | Доки `recognize` и `bot` приведены в соответствие, карта кадров описана с новой колонкой | #241 |
| T274 | recognize | — | todo | Разбор комментария ждёт 3–11 секунд: глубина рассуждения не задана, а вход — около 6000 токенов на каждую находку | #221 |
| T275 | infra | — | done | Дока о потере файла связок доступа расходится с кодом, и сам файл не попадает в бэкап | #233 |
| T276 | db | — | done | Тест ролей смотрит только атрибуты роли: расширение права с колоночного на табличное (`grant update (retracted_at, …)` → `grant update`) при первом накате не ловится ничем. Оснастка уникальных ролей это умеет, проверка её не использует. Там же — самозаслон оснастки без своей регрессии | #247 |
| T277 | chores | — | todo | `AUDIT_DATA_DIR` в рабочем `.env` смотрит на внешний каталог с неполной методикой: живой запуск падает, 2618 тестов молчат (берут методику из фикстур). Пример окружения корректен — дефект только в файле владельца, и правка вслепую запрещена: каталог методики вне git, там может быть боевая. Решено D124: вернуть путь на `./data` в репозитории, внешний каталог владельца не трогать; плюс проверка полноты методики на старте бота | #248 |
| T278 | chores | — | done | `make test-honest` не экспортирует `DATABASE_URL` и пароли, в отличие от `make test`: «честный» прогон идёт без базы и молча снимает все тесты блока db, оставаясь зелёным. Тот же класс, что #207, и в цели, само имя которой обещает честность | #250 |
| T279 | infra | — | done | Снаружи MCP отдаёт 502: звено funnel→контейнер не поднято, а healthcheck этого не видит — проверка зелёная при неработающем снаружи сервисе | #243 |
| T280 | chores | — | done | Около 57 ссылок на несуществующий каталог `docs/forge/` после переименования в `furca`: доки ведут в никуда | #245 |
| T281 | infra | — | done | Сервис db в compose не помечен для бэкапа площадки — проверить фактически, попадает ли база проверок в дампы | #246 |
| T282 | mcp | — | done | `add_photo_cue` неприменим к разделам объектов: `_check_codes` считает кодовой каждую колонку кроме первой (находка блока binding). Закрыта работой T289: колонки узнаются по заголовку, кодовой считается та, в которой коды есть | #249 |
| T283 | domain | — | todo | Аналитика по странам: интеграция с Pointer API (внешние оценки с карт). После MVP — задел, не первая версия | #251 |
| T284 | db | — | done | Хранилище синонимов формулировок: таблица «пункт методики → формулировка аудитора», с языком и происхождением записи. Схема — блок db (D119) | #252 |
| T285 | binding | T284 | done | Непрямое совпадение формулировки складывается в базу синонимом пункта и участвует в следующем поиске; прямое совпадение синонима не заводит. Учёт опечаток и иных формулировок аудитора (D119). Контракт слоя доступа объявлен блоком db: `lookup_phrase`, `remember_phrase`, `list_phrases`, `normalize_phrase`; исход `REMEMBERED | ALREADY_KNOWN | CONFLICT`, отказ `SynonymError` отдельный от `PushError`. ВАЖНО: `src.recognize` и `src.db` — пиры в слоевом контракте, импорт карты синонимов в быстрый путь уронит `lint-imports`; звать слой надо из `src.bot` и передавать карту дальше данными | #253 |
| T286 | mcp | — | done | `src/mcp/letters.py` устарел после правки T248: строка состояния отправителю говорит, что слой подставляет формулировку вопроса методики и переводит её, и комментарий у `BLANK_TEXT_FIELD` описывает подмену как текущее поведение. Предупреждать о находке без формулировки слой обязан и дальше — меняются только слова | — |
| T287 | bot | — | done | Соседняя дверь того же класса, что T248: при выборе пункта по кадру без комментария бот пишет за аудитора формулировку вопроса пункта (`src/bot/routers/record.py`) — формулировка «есть», и партнёр читает утверждение нормы вместо слов аудитора. Под T248 не подпадает: ни перевода, ни расхождения с PDF. РЕШЕНО D128: в запись идёт ссылка на пункт, а не текст нормы — тем же порядком, каким T248 починила письмо | — |
| T288 | chores | — | cancelled | СНЯТА решением D123 (методика не тайна, репозиторий остаётся публичным). Была: расширить её на `.md` (кроме `data/` и `examples/`) и привести документы в соответствие. Формулировки методики в документах заменяются описанием смысла, а не цитатой | — |
| T289 | mcp | — | done | Колонка «Зона» в карте кадров сломала правку карты через MCP: `src/mcp/photo_cues.py` считает колонки кодов количеством (`columns=len(заголовок) - 1`, строки 306 и 350), поэтому `add_photo_cue` и `edit_photo_cue` требуют код пункта в колонке зоны и отказывают на любой строке. Задевает боевую карту — все 17 таблиц. Чинится знанием колонки зоны по заголовку (`zone_column` из `language_rules.json`) с исключением её из колонок кодов. Красный тест `test_mcp_suggestions_db.py::test_предложенный_вызов_правки_действительно_принимается` оставлен красным намеренно — он и есть предъявление дефекта | #254 |
| T290 | bot | — | done | Три синхронных теста в `tests/test_bot_zone_from_item.py` помечены `pytestmark = pytest.mark.asyncio` и дают предупреждение на каждом прогоне. Дефект старый, к зоне отношения не имеет | — |
| T291 | mcp | — | done | `photo_cues.add` кладёт строку в раздел, у которого шапка есть, а строк ещё нет, ПОСЛЕ ПЕРВОЙ СТРОКИ ВСЕГО ФАЙЛА: `куда = (свои[-1].index if свои else _scan(текст)[0][0].index) + 1`. Раздел назван верно, отказа нет, `_verify` такую вставку пропускает. На сегодняшней карте не стреляет — пустых разделов в ней нет | — |
| T292 | db | T284 | done | Неверный синоним формулировки нечем убрать и нечем переправить: слой доступа намеренно не выдаёт роли UPDATE и DELETE. Твоя часть — право и слой: снятие записи и правка её пункта, с тем же разделением ролей, что у остального хранилища. Инструмент MCP поверх этого слоя — отдельная задача T294 | #255 |
| T293 | mcp | — | done | Заполнение колонки «Зона» в карте кадров через MCP: сегодня инструменты её берегут и показывают отдельным полем, поставить значение нечем. Форма выбрана решением D125: отдельный инструмент `set_photo_cue_zone`, а не расширение `edit_photo_cue` | — |
| T294 | mcp | T292 | done | Просмотр карты синонимов и снятие записи через MCP: управляющая компания должна видеть, что выучено, и убирать неверное. Поверх слоя из T292 | — |
| T295 | engine-fix | — | done | В состоянии без ключа `seq` (файлы старше счётчика) движок номер записи ПЕРЕИСПОЛЬЗУЕТ после снятия. Сквозная нумерация объявлена в спеке как must, и для ссылок на номер такой файл её не держит. Находка блока domain, проверена запуском | #256 |
| T296 | mcp | — | todo | Имена переменных окружения и приведение пути написаны по второму разу: `AUDIT_DATA_DIR` трижды, `MCP_CHECKLIST_STORE` и `STATE_DIR` дважды, `Path(os.path.abspath(os.path.expanduser(raw)))` в трёх копиях. `letters.sources()` читает окружение мимо `Settings` — переименованная переменная даст там не отказ, а ответ «этого издания на машине нет». Чтение на вызове менять не надо, оно намеренное (граница арендаторов): свести имена и приведение | — |
| T297 | bot | — | done | Неверный довод про освобождающийся после снятия номер записи остался в контракте блока (`docs/furca/blocks/bot.md`, строка Definition of Done). Тот же довод в блоке domain исправлен под T221 | — |
| T298 | bot | — | todo | Тот же дефект, что чинила T290, остался в `tests/test_bot_version.py`: три синхронных теста под общей `pytestmark = pytest.mark.asyncio` дают три предупреждения на каждом прогоне. Находка блока binding | — |
| T299 | infra | — | done | Накат схемы на стенде встал: `0007_model_suggestion.sql` изменён после применения, отпечаток не сходится — значит 0012 и 0013 на площадке НЕ накатаны, хотя код второй волны там стоит. Вернуть 0007 к применённому виду, правку вынести новым файлом, накатить 0012/0013 и проверить фактом | #258 |
| T300 | report | — | done | Секция «Итог по процессам» в отчёте и разбивка аналитики по процессам. Сделано соседней сессией владельца: cc45778, 52b532f, b4a171b | #260 |
| T301 | bot | — | done | Команда `/resend` — прислать отчёт по проверке ещё раз (D122). Сделано соседней сессией владельца: 093ada2 | #261 |
| T302 | domain | — | todo | РАЗБОР СДЕЛАН (docs/10-checklist-configuration.md): состав проверки УК уже настраивает через MCP, упирается всё в scoring.json. Дальше ждёт ответа на Q056. Гибкая настройка чек-листа управляющей компанией — требование владельца в ответе на Q055 («нужно дать возможность гибко настраивать чеклист»). Объём не определён: сегодня методика читается из `AUDIT_DATA_DIR` и правится файлами. Начать с разбора того, что именно настраивается (пункты, зоны, классы, сроки) и чем — прежде чем писать код | — |
| T303 | domain | — | done | `process_en` у INF09–INF11 переведён: в колонке 11 значений `Information` вместо восьми. Сделано соседней сессией, проверено счётчиком | #259 |
| T304 | infra | — | todo | Накат 0012/0013 и пересборка образов на площадке MUSPELHEIM: код починки #258 в main, площадка отстаёт — таблиц под стоящим там кодом нет. Порядок в `docs/08-deploy.md` §4.5.1, метка бэкапа оживает пересозданием контейнера (§4.8). РАСКАТКА: только по явному «да» владельца в том же разговоре и только в окно 23:00-06:59 | #264 |
| T305 | domain | — | done | Оговорка в `src/domain/state.py` (docstring `forget_suggestion`, «в состоянии старого поколения номер после снятия выдаётся заново») после T295 неверна: счётчик поднимается до максимума когда-либо выданных номеров при чтении состояния, поэтому и в старом состоянии номер не переиспользуется. Привести текст к факту, проверить запуском. Находка блока engine-fix, волна 3 | — |
| T306 | chores | — | done | D129/#267: планка числа выполненных тестов в репозитории — прогон краснеет, когда выполнено меньше планки. Пропуск по причине «нет окружения» считается падением, а не пропуском: трижды гейт был зелёным при невыполненных проверках (339 тестов блока базы молчали без переменной с доступами) | #267 |
| T307 | chores | T306 | done | D129/#267: свести цели прогона к одной правде о состоянии проекта. `make test` и `make test-honest` отличаются набором переменных, то есть вторая цель зелёная ровно тогда, когда проверяет меньше. Сверить доступы у обеих и убрать расхождение | #267 |
| T308 | chores | — | todo | НЕ НАЧИНАТЬ (D130, решение владельца 17.09: стройка остановлена, фокус ушёл в оснастку проверок вместо продукта). D129/#267: срезать журналы блоков `docs/furca/blocks/*.md` (6652 строки при 9 блоках) до файла состояния: где стоим, что дальше, чем проверено, что открыто. Потолок 120 строк, свежая запись заменяет прежнюю, хронику хранит `git log`. Контракт блока и Definition of Done не трогать — режется только хроника прозой. Проверено чтением `package.test.sh`: обязательных разделов ровно два — «API-контракт» и «Definition of Done блока», остальное срезается свободно. Замер, который является фактом о продукте (а не хроникой), переносить в `docs/`, а не удалять | #267 |
| T309 | chores | — | todo | НЕ НАЧИНАТЬ (D130, решение владельца 17.09: стройка остановлена, фокус ушёл в оснастку проверок вместо продукта). D129/#267: удалить тесты-налог — переписанные пачками под изменение поведения (следы T264, T271, T290: 27 падений за один заход). Признак: тест повторяет реализацию другими словами и переписывается при каждой правке. Удалять с проверкой, что после удаления число выполненных не падает ниже планки T306 | #267 |
| T310 | chores | — | todo | НЕ НАЧИНАТЬ (D130, решение владельца 17.09: стройка остановлена, фокус ушёл в оснастку проверок вместо продукта). Цель `make porcha`: вносит известную порчу в ядро, требует красноты прогона (зелёный на испорченном коде — провал цели), восстанавливает копией файла и сверяет md5. Сегодня проверка гейта порчей держится на дисциплине диспетчера, и 17.09 она не была сделана — гейт объявлен зелёным без неё. Обязательно `PYTHONDONTWRITEBYTECODE=1`: кэш байткода даёт 12 промахов из 12 | #268 |
| T311 | engine-fix | — | todo | Докстрока `test_запись_совпавшая_со_строкой_методики_остаётся_записью` в `tests/test_engine_letter_no_wording.py` утверждает, что бот кладёт в запись вопрос пункта — после T287 (D128) это неверно: запись ложится без формулировки, связь держится кодом пункта. Тест верен и зелёный, устарело пояснение. Находка блока bot | #269 |
| T312 | engine-fix | — | done | `manage.py validate` не ловит нарушение класса D1/D2 без срока устранения: партнёр прочтёт «устранить немедленно» там, где срок просто не проставлен. На боевой методике не стреляет (без срока там три строки, все D0). Чинить отказом, а не подстановкой умолчания — находка блока domain при разборе T302. Сделано 2026-09-18: `validate` отказывает с указанием цены ошибки, тест до кода, порча в обе стороны | — |
| T313 | mcp | — | done | Через MCP нельзя настроить долю существующей зоны и переименовать её, нельзя задать порядок обхода (`route.csv`) — эти части чек-листа правятся только текстовым редактором. Находка разбора T302. Сделано 2026-09-18 в части долей и переименования (`set_zone_shares`, `rename_zone`); порядок обхода вынесен в T317 | — |
| T314 | domain | — | todo | Третий язык продукта — правка кода примерно в восьми местах, главный упор `pick_text` в `src/domain/models.py`. Находка разбора T302: сегодня два языка зашиты структурно, а не читаются из словаря | — |
| T315 | mcp | — | done | Партнёр видит исходник чек-листа, по которому его проверяют: чтение эталона (пункты, зоны, классы, сроки) партнёрским токеном, без права правки. Решение D132, дословно: «базово он должен будет видеть исходник». Сегодня доступ к методике даётся правом `MCP_CHECKLIST_TENANTS` — проверить, что чтение открывается отдельно от правки, а не заодно с ней. Принята 2026-09-18: слито в main (`2933e13`), смоук живым запуском — партнёрским токеном 136 пунктов, 10 зон с долями, классы, сроки, критерии; четыре двери правки отказали, право УК цело | — |
| T316 | chores | — | todo | Пересмотреть сторож планки числа тестов (`scripts/check_test_floor.py`, `test_floor` в pyproject.toml) — он ставился мной по #267 ДО того, как правило проверок владельца назвало такую планку крайней мерой для молча усохшего набора и сказало, что она цементирует тот самый налог. Планка краснеет на уменьшение числа выполненных и на любой skip, то есть валит ровно чистку переборки (T309). Решить: опускать вместе с чисткой или снимать целиком. Поднято сессией `dodo-pnl-service-f0` 2026-09-18, она же может взять T309 | #267 |
| T317 | mcp | — | done | Порядок обхода пиццерии (`route.csv`) через MCP задать нельзя — правится только редактором. Хвост T313. Осторожно: `manage.py validate` о `route.csv` не знает вовсе и движок его не читает, поэтому проверка кандидата плохую правку НЕ поймает — инструмент обязан проверять коды зон сам, иначе повторится расхождение дверей (T302). В файле живут строки-пометки человека (`#`), их надо сохранить при перезаписи. Партнёру маршрут не отдан — Q057 открыт | — |
| T318 | web | — | done | Каркас веб-админки: сервер в том же образе и compose, серверный рендеринг без Node, стиль из `GarroV/forma` (D136), навигация 9 разделов прототипа. Непостроенное выглядит непостроенным (D138): лента в навигации и экран «ещё в разработке» при входе; признак «построен» — данные, а не разметка на каждом экране. «Календарь» и планирование в объём не входят, показываются непостроенными (D139) | #282 |
| T319 | web | T318 | done | Раздел «Проверки»: реестр проведённых проверок, карточка проверки, снятие. Снятие ложится на существующее `src/db/retract.py` (D086/D089) — второго понятия «удаление» в админке не заводить; проценты и буквы берутся готовыми, заново не считаются | #275 |
| T320 | web | T318 | done | Конструктор чек-листов, первая версия: правка состава зовёт те же двери, что MCP и CLI (D049, D050) — версия на каждую правку, публикация отдельным шагом. Вторую копию правил версионирования не заводить; незакрытые места настройки перечислены в `docs/10-checklist-configuration.md` | #277 |
| T321 | web | T319 | done | Правка письма партнёру и выгрузка в почту. Расширение объёма, а не доделка: сегодня письмо собирается движком и пересобирается по записанной проверке (T171), правки человеком и отправки нет — отправка писем из системы прямо исключалась из первой версии | #276 |
| T322 | bot | — | todo | Бот на согласованном чек-листе от команды методологии: настроить под актуальную редакцию. Со спринт-доски владельца, срок «после 15 сентября» | #281 |
| T323 | web | — | done | Вход в админку: логин и пароль, сессия в подписанной куке, защита всех маршрутов, выход; учётки заводятся командой проекта, регистрации снаружи нет; тексты на ru и en; тесты на права обязаны падать на испорченном входе (D154, D155) | #292 |
| T324 | web | T323, T325, T328 | done | Публикация админки наружу туннелем (Tailscale Funnel) после входа: сервер остаётся на петле, снаружи только туннель с TLS — как у MCP (D100); смоук СНАРУЖИ по образцу `make mcp-outside`; отзыв доступа одной командой; запись в `docs/12-web-admin.md` (D154, D155) | #293 |
| T325 | web | T323 | done | Ограничить перебор пароля на `/login`: счётчик неудач по адресу и логину, растущая задержка или временная блокировка. Блокер публикации наружу — пока его нет, единственная преграда снаружи это пароль. Тест обязан краснеть при снятии ограничителя (разбор безопасности на приёмке T323) | #294 |
| T326 | infra | — | todo | Продукт не переживает перезагрузку сервера: 18.09 MUSPELHEIM ребутнулся в 20:15, все четыре контейнера остались лежать, простой 2 часа, заметила случайная проверка. Нужна политика перезапуска в `docker-compose.yml` И автостарт Docker Desktop — по отдельности не работает. По D156 веб живёт там постоянно, поэтому это блокер, а не уборка | #296 |
| T327 | web | T325 | done | Тест-якорь на боевые пороги ограничителя перебора: сами значения (5 неудач по логину, 20 по адресу, шаги 1/5/15/60 мин) ничем не сторожатся — тесты считают порог от самой константы, поэтому снятие ограничителя оставляет набор зелёным. Найдено проверкой порчей на приёмке T325 | #297 |
| T328 | web | T325 | done | Запертый вход нечем открыть: посторонний, знающий логин администратора, держит его запертым бесконечно одним запросом раз в ~70 минут, пароль не нужен. Нужна команда снятия запрета либо осознанное решение владельца принять цену. Плюс оттуда же: гонка проверки и записи даёт лишние попытки в пределах пула потоков, адрес не канонизируется перед хешированием. Блокер публикации наружу | #298 |
| T329 | chores | — | todo | `make test` не ставит `PYTHONDONTWRITEBYTECODE=1` (в отличие от `test-honest`): после проверки порчей остаётся `.pyc` испорченного модуля, и прогон идёт по нему, оставаясь зелёным — на волне T327/T328 так прошли 3166 тестов при ограничителе со сроками в секундах. Бьёт по обязательному шагу приёмки | #299 |
| T330 | web | T328 | todo | Снятие запрета не оставляет следа: команда удаляет строку счётчика, журнала нет — кто снял и когда, не восстановить. Для админки, открытой наружу, это дыра в разборе инцидента | — |
| T331 | web | T328 | todo | Попутная чистка счётчика входа (`_DROP_STALE_SQL`) не ограничена ни арендатором, ни ключом и идёт внутри каждой транзакции попытки — встречные транзакции дают deadlock и 500-ю на форме входа. Существовало и до T328. Заодно: DeadlockDetected заворачивается в общий AccessError вместо тихого повтора | #300 |
| T332 | web | T323 | doing | Вход в админку по учётке Google: OAuth ВНУТРИ приложения (Cloudflare Access отпадает — домен не заводится, D163). Кнопка рядом с паролем, а не вместо: пароль остаётся запасным входом. Допуск по списку почт владельца в существующей `web_users`, незнакомая почта — отказ, а не автозавод учётки. Проверка обязана краснеть при снятии списка допущенных. **Сделано 22.09:** колонка `email` (0021), опознание по почте, разбор и проверка `id_token`, оба маршрута, кнопка рядом с паролем, тексты на двух языках; 22 теста, порчей проверены шесть заслонов. **Осталось:** секрет и адрес возврата в окружение площадки, почты девяти допущенных в базу стенда, смоук живым входом сотрудника — до него задача не `done` | #308 |
| T333 | web | T321 | done | История писем: правленое письмо нигде не остаётся, хранения нет вовсе. D035 говорит, что история писем живёт в системе; сегодня письмо пересобирается каждый раз, и ответить «что именно мы отправили партнёру» нечем — а партнёр считает отправленное фактом. Нужны таблица отправленных, фиксация человеком и показ зафиксированного вместо пересборки | #310 |
| T334 | bot | — | todo | Шапка письма партнёру пуста: НИ ОДНО письмо сегодня не готово к отправке (смоук на боевых Белград-1 и Тбилиси-1). ДИАГНОЗ ИСПРАВЛЕН 21.09: слой `db` ни при чём — слив пишет `auditor, city, partner, contact` (`_INSERT_INSPECTION_SQL`), чтение их отдаёт с T176 (#143), место в состоянии есть (`src/domain/state.py`). Пусто, потому что заполнить нечем: в `src/bot/` этих полей НЕТ ВОВСЕ — мастер их не спрашивает. Где собирать — решает владелец: мастер бота лишними шагами на точке, экран письма в админке (человек уже за компьютером) или справочник точек по названию пиццерии (T335, ждёт ключа) | #311 |
| T335 | db | — | doing | Страновой справочник точек: откуда брать и где хранить. Запрос владельца 21.09.2026 (Европа, Кавказ, Монголия, Бали, Нигерия). Ключа нет ни к одному источнику; правильный — Dodo IS Partner API (единые справочники и адрес заведения лежат в ПУБЛИЧНОМ сегменте, ключ самообслуживанием в кабинете разработчика), Поинтер про отзывы и страну не фильтрует. В схеме страны нет вовсе: `units` хранит только название и код. Блокер — ключ, это действие владельца | #312 |
| T337 | db | T321 | done | В карточке проверки нет самого отчёта: ни просмотра, ни скачивания. Запрос владельца 21.09. Кнопкой не решается: `build_pdf` собирает по ИДУЩЕЙ проверке из состояния чата и разрешает кадры через телеграм, а в базе лежат `file_id`, своего хранилища фотографий нет сознательно (спека). Решать: хранить собранный PDF при завершении проверки или собирать по запросу, и кто достаёт кадры. Первое закрывает и «что именно мы отправили партнёру» (T333) | #317 |
| T336 | db | T335 | todo | Дубли точек после загрузки справочника: «Тбилиси-1» (с историей проверок) и «Tbilisi-1» (с витрины) — одна пиццерия двумя алфавитами, для базы две строки. Следующая проверка ляжет на вторую, и динамика точки разорвётся молча. Механизм есть (`unit_aliases`, T092), но сопоставлять надо ПО АДРЕСУ: «Белград-1» против `Zeleni venac` и `Zemun` по названию не определяется, гадать нельзя | #316 |
| T338 | web | T321 | todo | Справочник точек с экрана админки: сейчас точка заводится только командой `make units ARGS="add ..."`, то есть человеком с доступом к серверу. Владелец 21.09 просил «дать возможность вносить новые если будет» — просьба закрыта наполовину. Нужен раздел «Точки»: список с отбором по стране, заведение руками (название, страна, город, код, синонимы), правка географии у заведённых (у «Белград-1» и «Тбилиси-1» её нет), связывание дублей через `unit_aliases` с показом адреса — это же закроет T336. Удаления на экране быть не должно: со строкой уйдёт история проверок точки | #319 |
| T339 | web | T323 | todo | Профиль: человек меняет свой пароль сам, с экрана. Текущий пароль обязателен — иначе чужая открытая вкладка означает отъём учётки. После смены остальные сессии этого человека закрываются, своя живёт: смена пароля это реакция на «меня взломали», и она обязана выбивать вошедшего. До кода решить развилку по правам: роль приложения имеет на `web_users` только `select`, и писать хеш ей нечем — грант `update (password_hash)`, узкий путь через роль обслуживания или функция в базе с `security definer` (рекомендация — последнее: не расширяет то, что может пробитое приложение). Проверки прогнать на порче | #324 |
| T340 | web | — | todo | Команда `password <логин>` в `tools/web_user.py`: сегодня пароль учётки не меняется ничем, только заведение второй и отключение первой. Нужна для «пароль забыт совсем», когда экран из T339 не поможет — туда надо войти. Пароль спрашивается вводом без эха, в аргументах командной строки не принимается: он виден в `ps` и остаётся в истории оболочки | #323 |
| T341 | mcp | — | todo | Хранилище получает два уровня — пространство и чек-лист: `<store>/<пространство>/<код>/versions/<издание>/`, `meta.json` рядом с изданиями (состояние не входит в отпечаток), журнал на чек-лист. Верхний `current` остаётся на месте и начинает вести на `<пространство>/<код>/current`, поэтому `AUDIT_DATA_DIR` на площадке не меняется. Миграция существующего хранилища в `<пространство УК>/bizdev/` обратима перемещением назад. Спека: `docs/superpowers/specs/2026-09-22-multi-checklist-design.md` (D177, D180, D181, D183) | #262 |
| T342 | mcp | T341 | todo | Заведение чек-листа с нуля из бланка: пустой `checklist.csv`, одна зона 100%, бланк `scoring.json`, пустые критерии, имя и дата. Бланк лежит в репозитории ДАННЫМИ, его правка не трогает существующие чек-листы. Новый чек-лист рождается `draft` | #262 |
| T343 | mcp | T342 | todo | Заслоны применения к проду: чек-лист без пунктов вида `violation` и снятый (`retired`) не применяются, отказом со словами. Проверено запуском 22.09: пустая методика проходит `validate`+`init`+`score` и даёт 100%/A — то есть сбой выходит наружу не ошибкой, а высшей оценкой. Каждый заслон обязан краснеть на порче | #339 |
| T344 | mcp | T341 | todo | Крючки MCP: новые инструменты `checklists`, `checklist_meta`, `create_checklist`, `rename_checklist`, `set_checklist_state`, `apply_checklist`; существующие 24 получают НЕОБЯЗАТЕЛЬНЫЙ параметр `checklist` — не передан, берётся применённый к проду, поэтому старые вызовы агента работают слово в слово. Правило: ни одна возможность не появляется только на экране | #262 |
| T345 | db | T341 | todo | Колонка `checklist_code` в `inspections`, миграция, заполнение существующих строк кодом `bizdev` (запись того, что и так было правдой, а не переписывание задним числом — D033). Колонка дописывается В КОНЕЦ списка выборки: разбор в `src/db/queries.py` позиционный, вставка в середину молча подставит в письмо партнёру одно вместо другого. Ограничение снимается, если раньше закроется #338 | #262 |
| T346 | domain | T341 | todo | Полка снимков становится двухуровневой: `STATE_DIR/methodology/<код>/<издание>/`. Разные чек-листы могут дать одинаковое имя издания и сегодня легли бы друг на друга | #262 |
| T347 | web | T344, T345 | todo | Экраны множественности: список чек-листов, заведение с нуля, смена состояния, применение к проду. Перед применением — ПОКАЗ РАЗНИЦЫ («сейчас в проде вот этот, будет вот этот: столько пунктов, такие зоны, такие ставки»): ролей нет по D182, и ошибку ловит экран, а не право. След применения в журнал как `web:<логин>` | #262 |
| T348 | web | T347 | todo | Настройка чек-листа в полном смысле: зоны с долями, веса (`scoring.json`), порядок обхода, карта кадров — пять файлов из семи, которых у экрана нет сегодня. Каждая возможность парой «экран + инструмент MCP» в одном изменении (T344). Правка весов идёт УК как автору эталона и не отменяет D132: партнёру эталон по-прежнему чтением | #262 |
| T349 | db | T345 | todo | Признак сравнимости ряда оценок: два издания одного чек-листа сравнимы, если совпали веса и состав влияющих на оценку пунктов; разные чек-листы несравнимы всегда. Разрыв ряда обязан быть ВИДЕН читателю сводки. Оценку не пересчитывать — признак строится по тому, что уже записано у проверки | #337 |
| T350 | chores | T347 | todo | НЕ НАЧИНАТЬ без отдельного разговора с владельцем (он сказал «на данный момент усложнять ролями не вижу смысла»). Пространства: сущность, принадлежность учётки, проверка принадлежности НА СЕРВЕРЕ отдельно от навигации, прослойка видимости разделов, админ, заводящий пространства и пользователей. Видимость годится внутри пространства, на границе она защитой не является — скрытый раздел открывается по прямому адресу (D182) | #340 |
